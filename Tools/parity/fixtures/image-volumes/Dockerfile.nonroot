FROM alpine:3.22

RUN mkdir -p /nonroot-data \
    && printf 'nonroot-image-seed\n' > /nonroot-data/seed.txt \
    && chown -R 65534:65534 /nonroot-data

USER 65534:65534

CMD ["sh", "-ec", "printf nonroot-volume-write-ok > /nonroot-data/runtime-write.txt; while :; do sleep 600; done"]
