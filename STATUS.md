# Status

This file is the current Docker Compose v2 parity ledger for `container-compose`. Keep branch, release, build, and validation policy in [BUILD.md](BUILD.md), use the [Container-family parity development cycle](docs/container-family-development-cycle.md) for coherent implementation slices, keep installation guidance in [INSTALL.md](INSTALL.md), and retain the generated upstream registry plus current supporting records under `docs/upstream/`.

This is the single current programme status. It is intentionally gap-only:
each partial or unsupported row says what is not yet 100% compatible, why it is
not compatible, and what implementation or evidence must change before the gap
can close. Completed delivery history and exact accepted heads live in the
linked designs and handoff records rather than a duplicate progress register.
No design checkbox or local implementation upgrades a support marker without
the pinned executable evidence required by that row.

## Project Goal: macOS Docker Compose Parity and Performance

The key product goal is 100% observable parity with Docker Compose v2 on
macOS, with comparable or better performance on the same macOS host. Parity
covers command and option discovery, configuration resolution and rendering,
exit statuses and errors, resource and lifecycle results, and user-visible
output; schema acceptance alone is not sufficient.

The target follows Docker Compose's documented local macOS behavior. Every
change to the pinned Docker Compose reference must update the reference
oracle and this ledger in the same reviewed change. A project-owned
difference may not become an implicit exception: it remains partial or
unsupported until a behavior-level oracle is green. A behavior that Docker
Compose cannot exercise on macOS may be excluded only when it is explicitly
recorded as a platform or product non-goal with supporting evidence.

Performance is part of parity, not a later optimization. The macOS
comparator must measure median and P95 results against Docker Compose on the
same host, with the same images, fixtures, isolated state, and recorded
versions. Its representative workloads are single-service and 10/50-service
startup, logs, sync, build-context transfer, and teardown. Comparable means
no material regression outside the declared measurement-noise band; better
means improvement in each metric's declared direction: lower latency, CPU,
RSS, allocation/copy bytes, and other costs; higher throughput; and no loss of
correctness, durability, isolation, or output quality. A mixed score must not
average an unacceptable regression away. Each benchmark record must identify the Mac and
macOS version, component revisions, Docker Compose reference, fixture state,
sample count, and warm or cold conditions.

This goal is complete only when every macOS-exercisable Docker Compose
behavior is accounted for by a green executable oracle or an explicit
platform non-goal, and the representative performance gate is comparable or
better than the reference.

The current prioritized assessment is the [macOS Compose parity and
performance review](docs/reviews/MACOS-COMPOSE-PARITY-AND-PERFORMANCE-REVIEW-2026-07-30.md).

## What Prevents 100% Parity

This is the authoritative short list of known macOS-local gaps. Every other
section in this file supplies the surface-level detail and oracle history for
these rows. A green maintained suite does not close any row below.
The [remaining macOS parity closure design](docs/remaining-macos-parity-closure-design.md)
maps this list to the focused designs, supplies the missing performance plan,
and separates actionable provider work from oracle reconciliation, evidenced
platform non-goals, and stock-Apple external dependencies.

| Incompatible or incomplete behavior | Why it is not 100% compatible | Required change to close it |
| --- | --- | --- |
| Comparable performance evidence | The counterbalanced lifecycle/logging matrix now retains 29 fixtures covering 1/10/50-service startup/teardown, detached and attached startup to first log, fixed stdout/stderr/mixed throughput, exact 16 KiB and 1 MiB records, blocking slow-sink backpressure, three non-blocking buffer sizes with observed drop proof, compressed `json-file` and `local` rotation writes with exact retained reads, tail 10/1,000/all, exact since/until boundaries, follow across forced rotation, dual-cache remote delivery/read, and 1/10/50-service foreground aggregation. It retains raw schedule position, monotonic duration, direction, semantic assertion digests, sink results, JUnit, exact fingerprints, median/P95 ratios, the 10× regression guard, and a separate comparable-or-better result. Docker-reference smokes pass, but a release-artefact run has not yet supplied paired results; cold-runtime CPU/RSS/I/O collectors, `develop.watch` sync, and build-context transfer still lack equivalent executable lanes. In the current [devcontainer exact-head runtime-parity run](https://github.com/stephenlclarke/devcontainer/actions/runs/30857210794), the Container Compose lane met the Docker target only on C03, passed E03 in 2.521 seconds (1.317× Docker), failed C04 functionally, and completed the other fixtures at 1.126–7.609× Docker; D05 was the largest candidate slowdown. | Complete the focused [remaining macOS parity closure design](docs/remaining-macos-parity-closure-design.md#comparable-or-better-performance-design): add the cold/resource, sync, and build-context lanes; run the counterbalanced matrix with release artefacts; retain raw evidence; profile regressions; and optimise until no candidate fixture has a functional failure and every release metric is comparable to or better than Docker outside the noise band. |
| Shared namespaces and Docker-complete privileged isolation | The one-VM-per-container runtime cannot join PID, IPC, or network namespaces without collapsing network isolation. It also lacks host-device, device-cgroup, security-profile, and host-isolation behavior for full Docker `privileged`. | Implement the [shared namespaces and privileged isolation design](docs/shared-namespaces-privileged-isolation-design.md): add the durable multi-container sandbox, per-workload network lifecycle, device/cgroup/profile/isolation controls, and Docker oracles for `network_mode`, `pid`, `ipc`, and privileged forms. |
| Live terminal candidate performance | Signed local Engine API `c7973ac641fb6f6e07df1358114f36222bd9ca59` and Container `a10ad44d8675b27a665d72fbe054f12f403f8412` close the pre-start attach, TTY resize, detach, re-attach, exit, and cleanup behavior gap. The same-MBP candidate fell from 4.359129 seconds to 1.238173 seconds cold and 0.920554 seconds warm, so it passes the programme's <10× failure guard against the 0.184992-second Docker 29.2.1 fixture but remains 6.69×/4.98× slower rather than comparable or better. | Profile and optimise the VM-backed first attach plus create/delete lifecycle and provider connection overhead until the release candidate is inside the declared comparable-performance noise band, while retaining the exact semantic and <10× regression gates. |
| Advanced network and IPAM semantics | The vmnet backend has no custom network/IPAM drivers, disabled IPv4, IPv6 allocation ranges or auxiliary addresses, or lossless multiple-pool/provider semantics. | Implement the cross-stack [advanced network and IPAM parity design](docs/advanced-network-ipam-design.md): lossless ordered models, named providers, optional IPv4, durable IPAM, Docker-matched reconciliation, and behavior/performance oracles. |
| Non-local volumes, advanced mounts, and `use_api_socket` | Arbitrary volume drivers currently degrade silently to local ext4, existing writable ext4 volumes are not safe Docker-style multi-VM shared storage, and bind propagation is rendered without live lower-runtime proof. Released `container-engine-api` 0.3.5 supplies the shared executable, generated API 1.44 through 1.53 ledger, gateway-owned ping/WebSocket transport, exclusive provider identity, private out-of-process provider session, and fail-closed shared-response composition. Signed local Engine API `4949e743675f` and Container `259878a427de` now add Docker-compatible system version, container/image discovery, public unauthenticated image pull, tag, and delete through the native image catalog. Paired Docker CLI 29.7.1 certificates pass against Docker Engine 29.2.1 and exact release candidates; the cached pull/tag/inspect/remove candidate is 17.79% faster than Docker. Registry credentials, push, search, history, build/session, typed guest socket grants, explicit authority handoff, Testcontainers/devcontainer adoption, and the complete external-client matrix remain absent. | Continue the cross-stack [non-local volume, advanced mount, and API socket parity design](docs/non-local-volumes-advanced-mounts-api-socket-design.md): complete the remaining route/credential/grant lifecycle and external-client matrix, then implement provider-backed shareable storage, typed guest-kernel mount semantics, migration, and behavior/performance oracles before enabling `use_api_socket`. The verified subsets and remaining field-level gaps are recorded in the [container discovery](docs/parity/docker-rest-discovery-oracle.md), [image discovery](docs/parity/docker-rest-image-discovery-oracle.md), and [image mutation](docs/parity/docker-rest-image-mutation-oracle.md) oracles. |
| Docker logging-driver semantics | The same-MBP Docker Compose 5.3.1 and exact signed candidate stack now pass readable/`none` non-TTY foreground output, successful empty static `none` history, restart retention, three-replica aggregation, tails, signal forwarding, and disconnected-client persistence as well as the TTY resize/detach/re-attach oracle. Compose `eb9b43c0` and Container `267991f22171ce6e438703f68a12159c1c57839c` also pass one Docker-CLI Bash certificate against Docker Engine 29.2.1 and the public Container socket for non-TTY create/start/inspect/static/follow/tail/second-start/stop/delete, direct `none` errors, native authority visibility, and exact cleanup. Direct Docker Engine 29.2.1 GELF fixtures now freeze UDP-default-gzip/TCP-NUL delivery, core option validation, and selected metadata/tag lifecycle semantics. Wire/configuration/metadata provider-component evidence passes on the exact local graph through an identity-preserving editable SwiftPM overlay; the refreshed metadata Docker oracle also passes in 3.225985 seconds (1.05× the captured baseline). None of this is candidate-runtime or external-client certification. The row remains incomplete because the synchronized stack is unpublished and Testcontainers, devcontainer Engine adoption, the complete provider/failure/migration/security/cold-resource/release-performance matrices, and candidate-runtime/external-client lanes have not passed. | Add candidate-runtime and external-client lanes, cold CPU/RSS/I/O collectors, and the remaining provider/failure/migration/security matrix. Optimise the counterbalanced release-artefact logging matrix to comparable or better median/P95 results, then publish the synchronized stack only after all programme development completes. See the [Docker logging driver semantics parity design](docs/docker-logging-driver-semantics-design.md). |
| Devcontainer logging-handoff release composition | **Blocked.** Signed devcontainer `fe9e8a5b1a094d09f8c0266fd22be5d539845b68` has an adapter-level proof against the exact local Engine API `4949e743675f00ec102f7acacdb4e990409e383f`, Container `259878a427de7021b52e40e759d3b261150cc514`, and Containerization `77f06d4c44341e04241941072fb69e2b85a6f5c1` graph: 11 focused handoff tests pass at 90.67% adapter-file line coverage and the real public/private provider-service proof passes 2/2. This is not release-compatible: the newer published Engine API 0.3.5 lacks the `ProviderHandoffPortableLogging*` types, public Apple Container 1.1.0 lacks `ContainerLogRecord`, `ContainerLogReplayOptions`, and `ContainerClient.logRecordStream`, and SwiftPM warns that the Apple and matched Containerization URLs collide on identity. | Publish or otherwise make one compatible Engine API/Container/Containerization graph resolvable under a single package identity; update devcontainer's declared pins and lockfile without local overrides; then rerun the handoff, public/private service, Docker-facing external-provider, and remaining Testcontainers/failure/migration/security/performance proof. The detailed evidence and closure conditions are in the [Docker logging driver semantics parity design](docs/docker-logging-driver-semantics-design.md#current-devcontainer-release-composition-blocker). |
| Devices and GPU parity | The runtime supports one virtio GPU/DRM subset only; it lacks vendor GPUs, multiple GPUs, driver capabilities/options, CDI selectors, generic device reservations, and arbitrary hardware passthrough. | Implement generic request/CDI/DeviceBroker/lease semantics through the [Local Deploy subset design](docs/local-deploy-device-resource-subset-design.md) and privileged inventory through the [shared namespaces design](docs/shared-namespaces-privileged-isolation-design.md). Then apply the [provider feasibility boundary](docs/remaining-macos-parity-closure-design.md#device-and-gpu-feasibility-boundary): positively advertise only live-proven host-feasible providers and retain exact unavailable/platform evidence for hardware the same-host reference cannot exercise. |
| Remaining resource and security controls | The runtime lacks realtime CPU controls, swappiness/OOM-kill control, custom user-namespace mappings, profile-based security options, and storage options. | Implement the [remaining resource and security controls design](docs/remaining-resource-security-controls-design.md): Linux guest cgroups, engine ID maps, immutable profiles, rootfs storage providers, requested/effective inspection, and Docker reference tests. |
| Docker lifecycle states and actions | `dead`, `restarting`, and `removing` inspect states, and explicit restart, OOM, rename, resize, update, attach/detach event actions are not emitted by the runtime. | Implement the [Docker lifecycle states and actions design](docs/docker-lifecycle-states-actions-design.md): immutable identity, canonical transitions/events, generation-aware operations, recovery, and `ps`/inspect/event parity fixtures. |
| Model-runner services | Compose can parse and render models, but there is no backend that starts a model or supplies an endpoint. | Implement the [Model Runner services design](docs/model-runner-services-design.md): selected-provider Container-owned supervision/store, exact ensure/injection phases, secure inference route, recovery, and live behavioural/performance oracles. |
| Local Deploy device/resource subset | Local limits and memory/GPU reservations are mapped, but valid CPU/generic reservations are rejected instead of preserved/ignored and non-GPU device reservations are rejected instead of reaching a general DeviceBroker. The job-specific `replicated-job`/`global-job` wait and restart-policy gates are now verified removed: the exact main graph builds, five focused regressions pass, and the strict Docker Compose 5.4.0/Engine 29.2.1 versus candidate CLI certificate passes on this MBP. Reservation PIDs and device/generic limits are not Compose schema fields. | Continue the [Local Deploy subset design](docs/local-deploy-device-resource-subset-design.md): preserve/ignore scheduler-only reservations, route every valid reservation `DeviceRequest` through DeviceBroker, and retain compose-go errors for schema-invalid fields. |
| Stock Apple `container` release support | Runtime-backed behavior depends on the matched `stephenlclarke` stack; released Apple APIs do not yet expose equivalent primitives. This is an external compatibility dependency, not a supported enhanced-lane functionality defect. | Follow the [stock Apple compatibility lane](docs/remaining-macos-parity-closure-design.md#stock-apple-compatibility-lane): keep generic gaps as Apple-shaped local handoffs without upstream submission, adopt equivalent released APIs when available, and rerun behaviour/performance evidence without blocking truthful enhanced-provider parity. |

Platform exclusions remain visible rather than silently treated as gaps:
Windows-only settings and Docker Swarm scheduling are not exercisable by
local macOS Docker Compose. They must remain explicitly documented and must
not be used to exempt any macOS-local behavior above.

## Current Integration Assumption

`container-compose` is supported as part of the matched `stephenlclarke` runtime bundle. Keep each package lane pinned to the matching `stephenlclarke/container`, `stephenlclarke/containerization`, and `stephenlclarke/container-builder-shim` surfaces until equivalent Apple upstream APIs are accepted and the plugin has been updated to those upstream surfaces.

The main drift risks are logs, events, restart policy, health, exit/completion metadata, networking identity, IPAM/DNS, dynamic ports, copy/archive behavior, build inputs, mounts, secrets/configs, blkio, sysctls, and runtime API shape changes.

Current refs should come from checked-in source-of-truth files rather than duplicated prose: [Tools/release/stack-refs.json](Tools/release/stack-refs.json) records stack component refs, `Package.swift` and [Package.resolved](Package.resolved) record exact SwiftPM dependency resolution, and `container system version` / `container compose version` report installed runtime and plugin provenance after installation.

### Current Architecture Boundary

Runtime collaborators and create-time DTOs are defined in Compose-owned types.
`ComposeCore` depends only on `ComposeRuntimeSPI`, imports no Apple modules,
and exposes no Apple package types. `ComposeContainerRuntime` owns Apple DTO,
archive, OCI, and live API translation. `make core-runtime-neutrality`
enforces the package and source-import boundary.

The parity claims in this ledger still describe tested behaviour on the
matched `stephenlclarke` stack. Runtime-neutral Core does not imply that an
alternate provider implements every capability. See [DESIGN.md](DESIGN.md)
and the
[Compose Runtime Coupling Audit](docs/upstream/COMPOSE-COUPLING-AUDIT.md).

## Current Validation

Use this validation floor when parity or runtime behavior changes:

- `container-compose`: `make ci`; targeted tests while iterating; full `make docker-compose-parity` whenever Compose, Dockerfile/build, CLI, or runtime behavior changes; and `make release-gate` before stable package dispatch.
- Normal CI explicitly reports the 25 runtime smoke tests as skipped when the isolated runtime lane is not enabled. Coverage gates `ComposeCore`, `ComposeRuntimeSPI`, the Apple-backed provider, `ComposePlugin`, aggregate first-party Swift, and Go separately.
- Performance-sensitive changes: `make docker-compose-performance-matrix` runs the local, counterbalanced warm-image Docker Compose comparator for detached 1/10/50-service startup/teardown plus attached/detached logging startup, fixed-volume and remote-pressure writers, retained/filter/follow reads, built-in rotation/compression, dual-cache delivery/read, and 1/10/50-service aggregation. It retains raw monotonic TSV with schedule position/direction, semantic assertion digests, sink summaries, JUnit, exact runtime fingerprints, and a Markdown median/P95 comparison with separate comparable-or-better and regression-guard results. Cold runtime CPU/RSS/I/O collection, `develop.watch` initial/incremental sync, and build-context transfer remain explicit `PERF-003` work. `make docker-compose-host-namespaces-parity` retains the bridge-lifecycle comparator.
- Apple-backed repositories: each affected repository's full source checks and unit tests, plus integration tests for changed runtime behavior.
- Documentation-only changes: the repository Markdown gate over every tracked `.md` file.

## Latest Controlled Full-Suite Evidence

On 30 July 2026, all 62 targets declared by `DOCKER_COMPOSE_PARITY_TARGETS` completed successfully in one uninterrupted, controlled same-host run. The exact revisions were container-compose `e969e4796690f88425488ad2b9ca9f795ca4f9c1`, Container `88460ab2ab0ca2f3fa9f91b2911b3b77647596c1`, Containerization `d7377b962af724f8d7c2b640f3ab12184d33f1af`, and container-builder-shim `61832d4ca91715180a84dec0eab091170174c43c`. The reference was Docker Compose 5.3.1 with Docker Engine 29.2.1 on an arm64 Mac17,9 running macOS 26.5.2.

The aggregate suite took 1,152.03s real time (19m12.03s), 290.26s user time, and 408.57s system time. The complete local log is `.build/logs/full-live-parity-final-e969e479.log`; `.build/parity/final-e969e479-host-namespaces/` retains exact runtime fingerprints, raw monotonic bridge samples, JUnit XML, and the generated Markdown comparison matrix.

The embedded equivalent warm-image bridge lifecycle comparator ran three repetitions per engine:

| Fixture | Docker Compose median | container-compose median | Candidate/reference | Behavioural regression guard | Comparable-performance goal |
| --- | ---: | ---: | ---: | --- | --- |
| `network_mode: bridge` up | 0.153s | 1.228s | 8.01× | Pass | Fail |
| `network_mode: bridge` down | 10.178s | 5.916s | 0.58× | Pass | Pass |

The new three-repetition service-discovery and links comparators also passed every behavioral assertion. Service-discovery startup measured 0.490196s for Docker and 4.317872s for container-compose (8.81×); this is 13.0% faster than the 4.965816s pre-optimization candidate baseline. Links startup measured 0.603993s and 5.209138s (8.62×). The `cp` oracle preserved content, ownership, modes, timestamps, hard links, and sparse allocation, but its observed operations remained 4.69× to 14.95× slower. The complete per-operation timing tables are recorded in the [macOS Compose parity and performance review](docs/reviews/MACOS-COMPOSE-PARITY-AND-PERFORMANCE-REVIEW-2026-07-30.md#latest-timing-detail).

The executable bridge timing rule fails only on timeout, incomplete execution, or a candidate statistic at least 10× its corresponding Docker statistic. The 8.01× startup result therefore passes that regression guard, but it does **not** satisfy the product goal of comparable or better performance. `PERF-003`/`PERF-607` remains open until startup and archive operations are materially improved, the implemented startup/teardown/logging matrix passes median and P95 comparison on release artefacts, and the remaining sync/build-context lanes retain equivalent evidence.

On 31 July 2026, the new lifecycle matrix completed one warm-image diagnostic repetition using the exact matched stack, but that stack was a debug build and the run is not release-grade evidence. It measured Docker/container-compose startup at 0.191s/1.487s (7.77×), 0.523s/6.644s (12.71×), and 2.093s/30.745s (14.69×) for 1, 10, and 50 independent services. Teardown was 1.256s/1.931s (1.54×), 1.528s/15.915s (10.42×), and 2.637s/68.944s (26.14×). Raw data, JUnit, the generated matrix, and fingerprints are retained in `.build/parity/performance-matrix-smoke/`. The result both confirms that the lane works and records the optimization work still required; it must not be presented as a passing comparable-performance result.

This result also does not convert known partial surfaces into full parity. The CLI oracle still records four documented intentional command-surface differences, and the service/runtime gaps below remain authoritative. Two SwiftNIO warnings about scheduling on an already-shut-down event loop appeared during image-volume builder teardown; every affected build and parity assertion completed, so they are retained as backend cleanup evidence rather than hidden or misclassified as a Compose parity failure.

The authoritative run acquired the shared macOS runtime lock and used the retained exact init image because isolated app roots do not isolate Container's per-user launchd/XPC service namespace. [BUILD.md](BUILD.md#isolated-macos-runtime-ownership) documents the advisory host lock, retained init image, bounded recovery, and the requirement that every host runtime user cooperate.

## Parity Legend

- ✅ Yes: green tick; Docker Compose v2 parity is implemented for the current `stephenlclarke` runtime lane.
- ⚠️ Partial: orange exclamation; a Docker Compose-compatible subset is implemented and the details list the remaining gap.
- ❌ No: red cross; the surface is intentionally rejected before side effects or has no implementation.

Runtime-backed commands preflight the installed stack and service readiness before work begins. Apple stock or mismatched Homebrew installs fail with [INSTALL.md](INSTALL.md) guidance instead of a late unsupported-feature or runtime error, and stopped services fail before Compose model loading or build/create side effects with `container system start` and Homebrew restart guidance.

Library consumers must supply runtime collaborators before executing
runtime-backed orchestration. The public unconfigured provider fails closed
for every operation, including image-volume metadata preparation and declared
volume discovery, so a missing provider cannot silently omit Dockerfile
`VOLUME` planning or copy-up.

Surface names follow the current Docker Docs [Compose file reference](https://docs.docker.com/reference/compose-file/), [Services reference](https://docs.docker.com/reference/compose-file/services/), [Build Specification](https://docs.docker.com/reference/compose-file/build/), [Deploy Specification](https://docs.docker.com/reference/compose-file/deploy/), [Develop Specification](https://docs.docker.com/reference/compose-file/develop/), [Dockerfile reference](https://docs.docker.com/reference/dockerfile/), and [Compose CLI reference](https://docs.docker.com/reference/cli/docker/compose/). This file records current state only; it is not a release history.

## Compose Surface Matrix

| Surface | Parity | Details |
| --- | --- | --- |
| Project discovery and source loading | ✅ Yes | Default local discovery, stdin, environment files, Git resources, and `oci://` project artifacts are implemented. Runtime-backed Compose file attributes are tracked separately below. |
| Service attributes and runtime behavior | ⚠️ Partial | The complete grouped service surface is in [Service Attribute Surface](#service-attribute-surface), including details for every runtime-limited group. |
| Dockerfile and build behavior | ✅ Yes | The complete instruction and local Build Specification surface is in [Dockerfile And Build Surface](#dockerfile-and-build-surface), including file, environment, and macOS Keychain-backed external build secrets. |
| CLI commands | ⚠️ Partial | 40 commands are ✅, 6 are ⚠️, and 0 are ❌. These HELP markers cover command invocation and visible command behavior; the full runtime gap register is below. Every command is listed in [CLI Command Surface](#cli-command-surface). |
| CLI long options | ⚠️ Partial | 262 documented long options are ✅, 1 is ⚠️, and 0 are ❌. Every option is listed in [CLI Option Surface](#cli-option-surface). |

## Compose File Surface

The Docker Compose v2 file reference is a rolling Compose Specification surface: top-level project elements, services, networks, volumes, configs, secrets, optional Build/Deploy/Develop specifications, provider/model extensions, fragments, merge behavior, interpolation, profiles, and include behavior. The current parity state is:

| Compose File Surface | Parity | Details |
| --- | --- | --- |
| Project file discovery and sources | ✅ Yes | Default local discovery, explicit and repeated `--file`, `COMPOSE_FILE`, `COMPOSE_ENV_FILES`, `COMPOSE_PROFILES`, `.env`, `--env-file`, project directory/name, profiles, interpolation controls, path-resolution controls, stdin, Git repository resources, and `oci://` Compose project artifacts are implemented. Explicit `--env-file` values override the comma-separated `COMPOSE_ENV_FILES` fallback, including from source-checkout normalizer runs. Git URLs accept Docker's `URL#ref:subdir` syntax, locate canonical Compose filenames in repository directories, resolve relative env/build paths from the checkout, and work in top-level `-f`, `include`, and `extends.file`. OCI project artifacts load Docker Compose project manifests, compose-file layers, env-file layers, OCI 1.0 fallback manifests, OCI 1.1 artifact manifests, image digest override layers, and image-index wrappers. `compose publish` pushes service images and writes OCI project artifacts, image digest override layers, and application image indexes for image-backed projects after Docker-compatible bind-mount, sensitive-data, env-declaration, literal-config, build-only-service, and unresolved-local-include preflights. |
| Top-level `name` and legacy `version` | ✅ Yes | `name` participates in project naming precedence, and legacy `version` is accepted by the Compose Specification loader without driving behavior. |
| Top-level `services` | ⚠️ Partial | Service definitions are parsed and normalized across the current Docker Compose service attribute surface. Runtime-backed gaps are listed in [Service Attribute Surface](#service-attribute-surface), the current-state matrix, and the CLI tables. |
| Top-level `networks` | ⚠️ Partial | `name`, `external`, `internal`, `labels`, the default bridge `driver`, and one IPv4 `ipam` `config.subnet` with optional IPv4 `gateway`, `ip_range`, and `aux_addresses`, plus one IPv6 subnet with an optional IPv6 `gateway`, are applied. The `attachable` field is retained as `true` in `config`/`convert` output and accepted for the macOS local vmnet network, whose standalone `container run --network` attachment is already available; the Swarm/overlay restriction that makes the field behaviorally significant is out of scope. The `enable_ipv6` field accepts `true` with or without an explicit IPv6 subnet: vmnet automatically assigns an IPv6 prefix when no subnet is supplied. `enable_ipv6: false` is also applied on macOS 26: Compose preserves the source IPv6 pool in `config`/`convert`, omits it from the effective network create, and the runtime disables NAT66/router advertisements. `driver_opts` are retained for `config`/`convert` but are not operational in the vmnet backend, so known Docker bridge option behavior remains part of the [advanced network and IPAM design](docs/advanced-network-ipam-design.md). `ipam.options` are retained and intentionally omitted from the effective local create request, matching Docker Compose 5.3.1. Custom drivers, `enable_ipv4` set `false`, IPAM `driver`, IPv6 allocation ranges, IPv6 auxiliary addresses, and multiple same-family pools are not applied. |
| Top-level `volumes` | ⚠️ Partial | `name`, `external`, `labels`, and local-volume creation are implemented. The runtime records arbitrary `driver` and `driver_opts` metadata but creates a local ext4 volume in every case; only local `size` and `journal` options have behavior. Non-local driver/plugin semantics are unavailable. |
| Top-level `configs` | ✅ Yes | `file`, `environment`, inline `content`, and `external` configs are materialized as read-only service mounts with Compose metadata. External config `name` lookup uses the Compose-owned filesystem backend; see [External Compose Resources](docs/external-resources.md). |
| Top-level `secrets` | ✅ Yes | `file`, `environment`, and `external` secrets are materialized as read-only service mounts and build secrets. External `name` lookup uses the Compose-owned caller Keychain backend; see [External Compose Resources](docs/external-resources.md). |
| Extensions, fragments, merge, and include | ✅ Yes | YAML anchors/fragments, `x-*` extension fields, interpolation, multi-file merge behavior, and local or Git-backed Compose include/extends handling are delegated to `compose-go`. Include short syntax and long-syntax `path`, `project_directory`, and `env_file` are supported; extension data is preserved in normalized config output. |
| Compose Build Specification | ⚠️ Partial | See [Dockerfile And Build Surface](#dockerfile-and-build-surface) for every build attribute and Dockerfile-adjacent behavior. The detailed register currently names no macOS-feasible Build gap, so the partial aggregate is an [oracle/coverage reconciliation item](docs/remaining-macos-parity-closure-design.md#surface-table-inconsistencies), not implicit implementation scope. |
| Compose Deploy Specification | ⚠️ Partial | Local behavior maps `replicas`, `labels`, `endpoint_mode`, CPU/memory/PID limits, memory and GPU reservations, and some `restart_policy` metadata. `resources.reservations.memory` projects through the generic soft-memory runtime primitive, matching Docker Compose local mode. Valid CPU/generic reservations and other scheduler-only fields are currently rejected instead of preserved/ignored, while non-GPU reservation devices do not reach a general DeviceBroker. The extra `replicated-job`/`global-job` wait/restart restrictions are verified removed by the exact-main-stack focused tests and strict Docker/CLI certificate. Reservation PIDs and device/generic limits are schema-invalid rather than missing runtime mappings. `mode`, `placement`, `update_config`, and `rollback_config` require compatible preservation, not a Swarm scheduler. |
| Compose Develop Specification | ✅ Yes | `develop` `watch` rules support `path`, `action`, `target`, `ignore`, `include`, `initial_sync`, and `exec` metadata for `rebuild`, `restart`, `sync`, `sync+restart`, and `sync+exec`, including prune, `watch --no-up`, `up --watch`, and `up --menu --watch`. |
| Provider services and models | ⚠️ Partial | Provider services run through the Compose provider `type`/`options` protocol, receive the normalized project environment, and inject prefixed `setenv` plus exact-name `rawsetenv` variables into dependents with visible override diagnostics. Top-level model `model`, `context_size`, and `runtime_flags` plus service model `endpoint_var`/`model_var` are parsed and rendered, but model-runner startup and endpoint injection are rejected until a backend exists. |

## Service Attribute Surface

Docker Compose service attributes are grouped here by runtime behavior so every current service surface has a yes/no/partial indicator without turning this handoff into generated API documentation.

| Service Attribute Surface | Parity | Details |
| --- | --- | --- |
| Identity, image, process, and profile attributes | ⚠️ Partial | `image`, `platform`, `pull_policy`, `profiles`, `attach`, `container_name`, `hostname`, `domainname`, `command`, `entrypoint`, `working_dir`, `user`, `stdin_open`, `tty`, `init`, `runtime`, and `scale` are mapped into local orchestration; `extends` is resolved during project loading. Explicit empty-list `command: []` is preserved and explicit `entrypoint: []` maps to the generic runtime entrypoint-clear primitive. This row currently names no failing behaviour and is an [oracle/ledger reconciliation item](docs/remaining-macos-parity-closure-design.md#surface-table-inconsistencies), not implementation scope until a concrete delta is proved. |
| Labels, annotations, and extension metadata | ✅ Yes | `labels`, `label_file`, service `annotations`, service `x-*` extensions, container names, project labels, and service metadata are retained. Labels and annotations are separate OCI metadata channels, so either may use the same key without overwriting the other. |
| Environment and env files | ✅ Yes | `environment` and `env_file` short/long syntax are implemented, including optional missing env files and `format: raw` values. Service env files are resolved during normalization like Docker Compose and are not forwarded as runtime `--env-file` arguments. |
| Build-backed services | ✅ Yes | `build` string syntax and the local Build Specification are mapped through the supported Dockerfile/build surface. Existing alternate Dockerfiles inside or outside the effective local context are supported. File, environment, and external build secrets are supported; Docker Compose local mode ignores build-secret `uid`, `gid`, and `mode` metadata. |
| Dependencies and links | ⚠️ Partial | `depends_on` and `external_links` ordering/discovery are implemented, including `condition: service_completed_successfully`, `condition: service_healthy`, and dependency restart/required metadata where local mode can honor it. Legacy service `links` uses source-scoped DNS on the first normalized shared Compose network. The link alias selects the first target replica, follows target address changes without recreating the source, remains private to the linked source, and injects no legacy environment variables, matching Docker Compose v5.3.1. A zero-scaled target leaves the alias unresolved while allowing the source to start. Aliases colliding case-insensitively with source `extra_hosts`, another linked service, or an external link are rejected before side effects. `external_links` now uses source-scoped DNS on every source network. The external target may be absent when the source starts, then appear, disappear, and reappear on zero, one, or multiple shared runtime networks without recreating the source; no static host entry is injected. The remaining discovery and legacy-environment wording is internally inconsistent and must follow the [links oracle reconciliation](docs/remaining-macos-parity-closure-design.md#links-and-external-discovery-reconciliation) before any new implementation. |
| Ports and exposure | ✅ Yes | `ports` short/long syntax, dynamic host ports, ranges, host IPs, protocols, named/app-protocol metadata, and `port` lookup are implemented. Service `expose` persists container exposed-port metadata without creating host listeners, matching Docker Compose's distinction between `expose` and `ports`. |
| Network attachments and discovery | ⚠️ Partial | Multiple `networks` attachments, `network_mode` values `none`, `host`, and `bridge`, MTU-only endpoint `driver_opts`, `mac_address`, route/connection priority, interface name, static IPv4/IPv6 addresses, `link_local_ips`, `dns`, `dns_opt`, `dns_search`, and `extra_hosts` are applied where matching primitives exist. `network_mode: bridge` maps to Container's built-in `default` network and intentionally has no Compose service-name DNS. On named networks, the matched runtime provides container-facing network-scoped DNS for unique container names, implicit Compose service names, shared scaled-service answers, direct `networks.aliases`, source-scoped legacy and external link aliases, `run --use-aliases`, and attachment lifecycle updates. Arbitrary endpoint `driver_opts`, `network_mode` values `service:NAME`/`container:NAME`, and arbitrary custom-name network modes are unavailable. |
| Volumes, mounts, configs, and secrets | ⚠️ Partial | Named, bind, anonymous, `tmpfs`, read-only image, `configs`, and `secrets` mounts work for the implemented local `volumes` subset, including `volumes_from`, local `volume_driver`, and supported subpaths. Deterministic anonymous volumes are scoped separately to each service replica and one-off container. Image metadata is read for `up`, `create`, and one-off `run` after honoring the active pull policy (preparing a missing default-pull image when needed). For every Dockerfile-declared target, Compose creates an implicit local volume when unmasked. For every eligible local volume mount, declared or ordinary, it seeds an empty volume from the selected image path, including named, anonymous, and `volumes_from` inherited mounts, and preserves the selected directory's mode and UID/GID on the volume root so image users can write there after copy-up. It honors `volume.nocopy: true`, leaves a mount at a missing image path empty, and reuses populated volumes across `down`/`up`; `down --volumes`, `rm --volumes`, and `--renew-anon-volumes` discover generated image volumes through Compose labels. A `volume.subpath` must exist before mounting and deliberately skips image copy-up, leaving its existing content unchanged. Non-local volume drivers, recursive bind modes, consistency/cache modes, and `use_api_socket` remain unavailable. Bind propagation is preserved and rendered but lacks live lower-runtime flag evidence, so it remains part of the [cross-stack closure design](docs/non-local-volumes-advanced-mounts-api-socket-design.md). SELinux relabeling, `npipe`, and cluster/CSI mounts are platform or Swarm-specific. |
| Runtime resources and security | ⚠️ Partial | Positive fractional `cpus`, `cpu_period`, `cpu_quota`, `cpu_shares`, `cpuset`, byte-accurate `mem_limit`, `mem_reservation`, `deploy.resources.reservations.memory`, `memswap_limit`, `pids_limit`, `cgroup`, `pid`, `ipc`, and `uts` modes `host` and `private`, `userns_mode: host`, `cgroup_parent`, `blkio_config`, `sysctls`, `ulimits`, `shm_size`, `oom_score_adj`, `cap_add`, `cap_drop`, `read_only`, restart, numeric/named `group_add`, and `security_opt` no-new-privileges forms (including bare enabled), `systempaths=unconfined`/`systempaths:unconfined`, unconfined seccomp/AppArmor forms, and `label=disable`/`label:disable` are handled. Bare no-new-privileges is normalized to the generic runtime's explicit true value. Either systempaths spelling maps to the generic guest control that clears only the OCI masked/read-only path lists; it does not add capabilities or alter the macOS host boundary. The profile and label no-ops are consumed by Compose because the guest workload baseline has neither profile nor SELinux labels; no synthetic runtime flag is needed. Docker Compose V2 normalizes `cpus: 0` / `0.000` to no runtime CPU limit; container-compose does the same, while the generic runtime also accepts explicit `--cpus 0` as cgroup v2 unlimited (`cpu.max = max PERIOD`). Fractional and explicit CFS settings are applied through the macOS Linux guest's cgroup v2 `cpu.max`; OCI CPU shares are converted to cgroup v2 `cpu.weight`; `cpuset` is applied to the macOS Linux guest cgroup after initializing its required memory-node set; `cgroup`, `pid`, `ipc`, and `uts` host modes omit the matching OCI namespace so they select the sandbox VM namespace, while private remains the default. `cgroup_parent` validates a non-empty relative path with no empty, `.` or `..` components and creates each container as a leaf under the sandbox VM's managed `/container` cgroup v2 root; it never selects a macOS-host cgroup. `userns_mode: host` retains the sandbox VM's existing user namespace. The current `private` path creates an identity-mapped guest namespace, but the canonical oracle contract records Moby 29.2.1 accepting only empty/`host`; this path is migration-only legacy state and is not parity evidence. Source/config/hash must retain literal `private`, then the authority must reproduce the pinned container-create result. `stop_signal` and `stop_grace_period` are persisted as per-container runtime defaults. Docker Compose's local Deploy memory reservation reuses the generic soft-memory runtime primitive. For initial `privileged` containers, the generic runtime restores all Linux capabilities and clears Containerization's standard OCI masked/read-only paths inside the sandbox VM. `cpu_rt_period` and `cpu_rt_runtime`; `mem_swappiness`, `oom_kill_disable`, IPC sharing (`ipc: shareable`, `ipc: service:NAME`, and accepted `ipc: container:NAME`), PID sharing (`pid: service:NAME` and `pid: container:NAME`), engine ID-map/remapping policy, profile-based `security_opt` forms, and `storage_opt` are unavailable. Containerization's experimental `LinuxPod` can share selected PID and IPC namespaces in one VM, but the production Container runtime creates one VM per container and the pod path has no per-container network-namespace lifecycle; using it for a Compose sharing request would silently collapse network isolation. Correct sharing therefore needs a generic multi-container sandbox runtime, not a Compose-layer adapter. `privileged` containers still cannot provide Docker's host-device, device-cgroup, security-profile, or host-isolation behavior. `cpu_count`, `cpu_percent`, `isolation`, and `credential_spec` are Windows-specific. |
| Devices and GPU | ⚠️ Partial | `devices`, `device_cgroup_rules`, service `gpus`, and Deploy generic GPU reservations are implemented through the `stephenlclarke` runtime. GPU requests attach the single Apple virtio-gpu VM device and project Linux DRM character-device metadata when the running guest kernel exposes `/dev/dri`; vendor drivers such as NVIDIA/CUDA, multiple GPUs, driver options, non-GPU device reservations, arbitrary macOS hardware passthrough, and verified hardware-accelerated rendering remain runtime gaps. Windows-only `credential_spec` is not a macOS parity requirement. |
| `logging` | ⚠️ Partial | Signed local Compose `main` through `c7a50e28438ca0c5bd5a668d3b8e87db25c4a176`, Container `bfa8b361901e33bc427d5bb551d19b2a224ca3f2`, Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, and Engine API `c7973ac641fb6f6e07df1358114f36222bd9ca59` pass the same Docker Compose 5.3.1 foreground/history fixture on the MBP. The verified cases cover readable and `none` non-TTY foreground output, successful empty static history for `none`, restart retention, three-replica foreground/history aggregation with complete identifiers, tails, signal forwarding, and attached-client disconnect while logging continues; 19 focused Swift tests and three harness regressions pass. Earlier signed heads retain the distinct built-ins, remote/provider planes, public REST lifecycle, durable recovery, protected handoff, and fixed-ceiling-free framed history transfer. It is still not 100% compatible because the synchronized stack is unpublished and the complete external-client, provider, failure, migration, security, cold-resource, and release-performance matrices remain unverified. See the [logging design](docs/docker-logging-driver-semantics-design.md). |
| Healthchecks | ✅ Yes | `healthcheck` and Dockerfile `HEALTHCHECK` defaults/overrides are projected into typed runtime configuration. Probe cadence and state, `service_healthy`, health-aware `up/start --wait`, transition events, and `ps` health output are implemented. |
| Lifecycle hooks | ✅ Yes | `pre_start` runs sequential ephemeral helpers before a fully stopped service starts, inheriting its mounts, networks, environment, platform, and hook user/workdir/privileged overrides; helper failures gate startup and helpers are removed. `post_start` and `pre_stop` cover detached/managed paths, regular foreground `up`, and foreground `run`. Interactive lifecycle-managed `run` reattaches through the pinned init-process stream primitive, proxies signals through `pre_stop`, preserves detach-key behavior and exact process status, and retains runtime auto-removal after detach. Docker Compose itself rejects `pre_start.per_replica: true`. |
| Deploy Specification attributes | ⚠️ Partial | `deploy` replicas, labels, endpoint metadata, CPU/memory/PID limits, memory reservation, GPU reservation metadata, and ordinary local job-mode convergence are handled locally. `resources.reservations.memory` reuses the existing soft-memory primitive. `replicated-job` and `global-job` now retain ordinary restart projection, detached startup, and readiness-based `--wait` without a job-completion gate. CPU/generic reservations and scheduler fields still need preservation/ignore behaviour instead of false local rejection; every valid non-GPU reservation device still needs the common DeviceBroker. Reservation PIDs and device/generic limits remain compose-go schema errors. Docker Compose local mode does not provide Swarm placement, rolling-update, rollback, or job scheduling. |
| Develop Specification attributes | ✅ Yes | `develop` watch rules support `rebuild`, `restart`, `sync`, `sync+restart`, and `sync+exec`, including `path`, `action`, `target`, `ignore`, `include`, `initial_sync`, and `exec` metadata. |
| Provider and model attributes | ⚠️ Partial | `provider` services use `type` and `options`, run metadata and lifecycle commands with the normalized project environment, and inject prefixed `setenv` plus exact-name `rawsetenv` variables into direct dependents. Raw replacements emit an override diagnostic. Service `models` with `endpoint_var`/`model_var` and top-level `models` with `model`/`context_size`/`runtime_flags` are parsed and rendered by `config`, but runtime model-runner startup and endpoint injection are rejected until a backend exists. |

## Dockerfile And Build Surface

| Dockerfile / Build Surface | Parity | Details |
| --- | --- | --- |
| Dockerfile instruction set and parser directives | ⚠️ Partial | The `stephenlclarke/container build` BuildKit path parses `syntax`, `escape`, and `check` directives, here-documents, shell/exec forms, and the current Dockerfile instructions: `ADD`, `ARG`, `CMD`, `COPY`, `ENTRYPOINT`, `ENV`, `EXPOSE`, `FROM`, `HEALTHCHECK`, `LABEL`, `MAINTAINER`, `ONBUILD`, `RUN`, `SHELL`, `STOPSIGNAL`, `USER`, `VOLUME`, and `WORKDIR`. OCI image `VOLUME` metadata is retained. Compose creates and seeds each unmasked declared volume target for `up`, `create`, and one-off `run`; it also seeds every eligible ordinary local volume mount from an existing image path. `volume.nocopy: true`, a pre-existing `volume.subpath`, and a missing image path deliberately leave the volume unchanged, while populated volume data persists over `down`/`up`. The row currently names no failing instruction and the detailed register says no feasible Build gap remains; resolve it through the [coverage/oracle reconciliation](docs/remaining-macos-parity-closure-design.md#surface-table-inconsistencies) before creating work. |
| `.dockerignore` context filtering | ✅ Yes | Build contexts use the `stephenlclarke/container-builder-shim` filter path, including negation patterns that re-include descendants below excluded parent directories. |
| Build context string syntax | ✅ Yes | `build: ./dir` resolves to a context directory with the default `Dockerfile`, matching Docker Compose local mode. |
| `build.context` | ✅ Yes | Local relative and absolute contexts are resolved, and remote BuildKit references are passed through to the builder. |
| `build.dockerfile` | ✅ Yes | Alternate paths are resolved relative to the effective local context, existing Dockerfiles outside that context are transferred through the generic Builder file-sync path, and remote-context values pass through. Docker Compose V2 model, bake, build, and run parity is enforced by `make docker-compose-build-external-dockerfile-parity`. |
| `build.dockerfile_inline` | ✅ Yes | Inline Dockerfiles are materialized for live builds and rendered as `dockerfile-inline` in `build --print` bake output. |
| Underlying `container build -o type=tar,dest=...` | ✅ Yes | The generic Builder validates and delivers a direct archive destination, an existing directory as `out.tar`, and repeated directory exports as `out.tar.1`; a missing `dest` remains a structured error. Compose's normal image output continues to use the default image lane. |
| `build.additional_contexts` | ✅ Yes | Local, image, Git/URL-style, and `service:NAME` contexts are mapped to BuildKit `--build-context` or bake contexts; service contexts are built in dependency order. Stock Apple tracking for the same `container build` primitive is [apple/container#1930](https://github.com/apple/container/issues/1930). |
| `build.args` and `build --build-arg` | ✅ Yes | Compose-file and CLI build arguments merge with Docker Compose-compatible environment lookup for key-only CLI args. |
| `build.cache_from` and `build.cache_to` | ✅ Yes | Cache hints are forwarded to live builds and bake output. |
| `build.entitlements` | ✅ Yes | Entitlements are forwarded as BuildKit `--allow` values. |
| `build.extra_hosts` | ✅ Yes | Build-time host entries are forwarded to the builder. |
| `build.isolation` | ✅ Yes | The field is accepted and preserved in normalized config; local Docker Compose omits it from Buildx bake output on this platform, and this plugin mirrors that behavior. |
| `build.labels` | ✅ Yes | Build labels are forwarded to live builds and bake output. |
| `build.network` | ✅ Yes | BuildKit network mode is forwarded to live builds and bake output. |
| `build.no_cache` and `--no-cache` | ✅ Yes | File and CLI no-cache controls are applied to live builds and bake output. |
| `build.no_cache_filter` | ✅ Yes | Named stage filters are preserved in normalized config, rendered as Buildx-compatible `no-cache-filter` bake arrays, and forwarded to the generic filter-valued Container/Builder no-cache primitive for live builds. |
| `build.platforms` | ✅ Yes | Target platforms are forwarded to live builds and bake output. |
| `build.privileged` | ✅ Yes | Privileged build mode is forwarded to the `stephenlclarke` builder. |
| `build.provenance` | ✅ Yes | Compose-file and CLI provenance attestations are forwarded, including Docker Compose-compatible false/disabled handling. |
| `build.pull` and `--pull` | ✅ Yes | File and CLI pull controls are applied to live builds and bake output. |
| `build.sbom` | ✅ Yes | Compose-file and CLI SBOM attestations are forwarded, including Docker Compose-compatible false/disabled handling. |
| `build.secrets` | ✅ Yes | File-backed and environment-backed BuildKit secret IDs are supported. External secrets resolve through the Compose-owned macOS Keychain reader into invocation-private 0400 files that are deleted immediately after the Apple build; the secure-store bytes never enter normalized JSON, bake output, labels, or command-line values. `build --print` omits external secrets like Docker Compose V2, whose local engine has no external secret store. Docker Compose local mode ignores `uid`, `gid`, and `mode` metadata for build secrets. Swarm secret drivers are not a local macOS Compose primitive. |
| `build.ssh` and `build --ssh` | ✅ Yes | Compose-file and CLI SSH forwarding entries are merged with Docker Compose-compatible CLI override behavior by SSH ID. |
| `build.shm_size` | ✅ Yes | Build shared-memory size is forwarded to the builder. |
| `build.tags` | ✅ Yes | Additional image tags are forwarded and de-duplicated with the service image tag. |
| `build.target` | ✅ Yes | Target stages are forwarded to live builds and bake output. |
| `build.ulimits` | ✅ Yes | Build ulimits are forwarded to the builder. |
| `build --builder` | ✅ Yes | Named `stephenlclarke` builders are selected for live builds; `build --print` omits builder selection from bake JSON like Docker Compose. |
| `build --check` | ✅ Yes | BuildKit lint/check mode runs without exporting an image; `build --print --check` emits bake `call: "lint"`. |
| `build --print` | ✅ Yes | Buildx bake JSON is rendered without build side effects and includes supported contexts, args, cache, labels, tags, target, platforms, pull/no-cache, secrets, SSH, attestations, outputs, and lint calls. |
| Dockerfile `HEALTHCHECK` inheritance | ✅ Yes | Dockerfile healthcheck metadata is inherited through the `stephenlclarke` image metadata API, explicit Compose overrides merge with image defaults, and the resulting probes drive dependency gating, health-aware waits, events, and status output. |

## CLI Command Surface

| Command | Parity | Details |
| --- | --- | --- |
| `alpha` | ✅ Yes | Experimental namespace help and the Docker-documented dry-run, scale, and watch aliases are implemented. |
| `alpha dry-run` | ✅ Yes | Experimental `alpha dry-run -- COMMAND` wraps the requested Compose command with root `--dry-run` while preserving project/global options. |
| `alpha scale` | ✅ Yes | Experimental `alpha scale` is implemented as an alias for the stable `scale` command. |
| `alpha watch` | ✅ Yes | Experimental `alpha watch` is implemented as an alias for the stable `watch` command. |
| `attach` | ⚠️ Partial | Interactive init-process stream reattachment, `--sig-proxy`, Docker-compatible `--detach-keys`, output-only `--no-stdin`, non-TTY signal handling, and disconnect isolation are implemented; the live pre-start TTY resize/detach/re-attach and Compose signal/log reliability fixtures match Docker 29.2.1/Compose 5.3.1. The remaining partial marker is synchronized publication plus the complete provider, failure, migration, security, and release-performance evidence in the [logging design](docs/docker-logging-driver-semantics-design.md#foreground-output-is-not-historical-read-back). |
| `bridge` | ✅ Yes | The complete Compose Bridge CLI runtime is implemented for the `stephenlclarke` runtime lane. |
| `bridge convert` | ✅ Yes | Models include image ports, published target ports, and text or binary config and secret content; Kubernetes, Helm, custom templates, repeated transformations, and empty-output current-directory mode run through local transformer images. |
| `bridge transformations` | ✅ Yes | Bridge transformation image management is implemented. |
| `bridge transformations create` | ✅ Yes | A stopped transformer rootfs is exported, only `/templates` is securely extracted, and a standard rebuildable Dockerfile is written. |
| `bridge transformations list` | ✅ Yes | Local transformer images labelled `com.docker.compose.bridge=transformation` are listed in Docker-shaped table, JSON, and quiet modes. |
| `bridge transformations ls` | ✅ Yes | Alias for `bridge transformations list`. |
| `build` | ✅ Yes | Build execution and CLI options cover the local Build Specification, including invocation-private macOS Keychain resolution for external build secrets. |
| `commit` | ✅ Yes | Stopped service containers and running containers commit to OCI images through export, archive creation, and image load. The generated image resolves the complete inherited OCI config from the selected service platform in one metadata read, including user, environment, process defaults, working directory, labels, exposed ports, stop signal, `Healthcheck`, and `VOLUME` declarations; additive `--change VOLUME` targets and effective Compose healthcheck overrides are preserved. The default `--pause=true` running path briefly freezes the root filesystem; `--pause=false` uses the generic best-effort APFS copy-on-write snapshot in the pinned `stephenlclarke/container` fork and leaves the filesystem writable. Omitted `--index` and `--index=0` use Docker Compose's default service-container selection. |
| `config` | ✅ Yes | Compose project rendering and config query options preserve the implemented Compose Specification surface, including `build.no_cache_filter`. |
| `convert` | ✅ Yes | Docker Compose's config-compatible model conversion projections are implemented for the documented local output modes. |
| `cp` | ✅ Yes | Local-to-service, service-to-local, service-to-service, stdin tar archive, and stdout tar archive copies are implemented, including archive, follow-link, replica-index, one-off-container, trailing `/.`, and `--all` modes. The matched runtime streams archives directly and preserves content, ownership, modes, timestamps, symlinks, hard links, sparse allocation, long paths, and large files. `--all` stages raw input bytes once for replay without host extraction. Automated Docker Compose parity records per-operation timings and applies a bounded hang/material-slowdown policy. |
| `create` | ✅ Yes | Service creation, build/pull/recreate controls, scaling, and orphan handling are implemented. |
| `down` | ✅ Yes | Container, network, image, volume, timeout, orphan, and service-scoped cleanup are implemented. |
| `events` | ⚠️ Partial | Event output, JSON mode, and time filters are implemented. The runtime emits create/start/pause/unpause/stop/delete/health plus Docker-shaped `kill` (signal/process attributes), `die` (exit code where observed), `destroy`, and `exec_create`/`exec_start`/`exec_die` (exec ID and terminal exit code) actions. Automatic policy restarts match Docker by emitting `die` then `start`; explicit restart, oom, rename, resize, update, and attach/detach actions remain unavailable. |
| `exec` | ⚠️ Partial | Service exec options, indexes, env, user, workdir, tty, and detach mode are implemented. `--privileged` grants capabilities but cannot provide Docker's full privileged isolation/device behavior. |
| `export` | ✅ Yes | Container filesystem export to an archive path is implemented for stopped containers and automatically uses the generic live snapshot path for running service containers. |
| `help` | ✅ Yes | A deliberate `container compose help` extension returns Compose-layer help because `container help compose` cannot dispatch to the plugin. Docker Compose-compatible `--help`/`-h` remain available for every command. |
| `images` | ✅ Yes | Image listing and formatting are implemented. |
| `kill` | ✅ Yes | Signal and orphan handling are implemented. |
| `logs` | ⚠️ Partial | Follow, timestamps, prefix/color controls, indexes, tail, time filters, restart retention, scaled aggregation, and Docker Compose 5.3.1-compatible successful empty static history for `logging.driver: none` are implemented. Direct Container/Engine readers and Compose follow retain the unsupported-reader error; only static Compose history suppresses it. The remaining partial marker is provider/cache/stopped reconstruction across the complete external-client, failure, migration, security, publication, and release-performance matrix in the [logging design](docs/docker-logging-driver-semantics-design.md#dual-logging-and-read-back). |
| `ls` | ✅ Yes | Project listing, filters, formats, quiet, and all modes are implemented. |
| `pause` | ✅ Yes | Service pause is implemented. |
| `port` | ✅ Yes | Published-port lookup by service, index, and protocol is implemented. |
| `ps` | ✅ Yes | Container listing, filters, distinct Docker `created`/`exited` lifecycle states, service selection, table/JSON output, structured field references, label lookup, nested paths, root/variable references, deterministic ranges, Go-template control actions and whitespace trimming, and Docker/Go row-formatting functions are implemented. |
| `publish` | ✅ Yes | Service image push, OCI project artifact publishing, image digest override layers, and `--app` application image indexes are implemented for image-backed Compose projects. Supported publish behavior includes all-profile image selection, `--dry-run`, `--app`, `--oci-version`, `--resolve-image-digests`, `--with-env`, Docker-compatible interactive preflight prompts, and noninteractive `--yes` prompt acceptance. |
| `pull` | ✅ Yes | Pull policy, dependency inclusion, quiet mode, and ignore-failure behavior are implemented. |
| `push` | ✅ Yes | Dependency inclusion, quiet mode, and ignore-failure behavior are implemented. |
| `restart` | ✅ Yes | Service restart, dependency control, and timeout are implemented. |
| `rm` | ✅ Yes | Stopped-container removal, force, stop, and volume cleanup are implemented. |
| `run` | ⚠️ Partial | One-off containers and Docker Compose run options are implemented, including opt-in service and explicit network aliases through `--use-aliases`. Interactive reattach, signal, detach-key, cleanup, and exit-status paths work. Non-interactive foreground `run` now prepares exact-process live attachment before start, so `none` and unreadable drivers do not depend on log history; the remaining partial marker is the logging provider/oracle/release matrix. |
| `scale` | ✅ Yes | Service scaling and dependency control are implemented. |
| `start` | ✅ Yes | Start, health-aware wait, and wait-timeout behavior are implemented. |
| `stats` | ✅ Yes | Table/JSON formatting, stopped-container inclusion, no-stream, no-trunc, structured field references, root/variable references, Go-template control actions and whitespace trimming, and Docker/Go row-formatting functions are implemented. |
| `stop` | ✅ Yes | Stop and timeout are implemented. |
| `top` | ✅ Yes | Service selection and Docker-shaped per-container process tables are implemented through the matched runtime process-metadata API, including UID, PID, PPID, CPU, STIME, TTY, TIME, and CMD columns. |
| `unpause` | ✅ Yes | Service unpause is implemented. |
| `up` | ⚠️ Partial | Create/start/attach/watch/menu/build/pull/recreate/scaling behaviour, health-aware `--wait`/`--wait-timeout`, selected-service `--exit-code-from` status propagation, service-level `pre_start` helpers, and network-scoped service discovery are implemented. Foreground services and dependency restarts attach to the exact process before start without loss, duplication, blank lines, or launch-identifier output. Pinned TTY and non-TTY fixtures now cover `none`, readable history, restart, three replicas, signals, tails, and client disconnect. The remaining partial marker is synchronized publication and the complete provider/failure/migration/security/release-performance matrix in the [logging design](docs/docker-logging-driver-semantics-design.md#foreground-output-is-not-historical-read-back). |
| `version` | ✅ Yes | Pretty, short, and JSON version output are implemented. |
| `volumes` | ✅ Yes | Volume listing, quiet, table/JSON output, structured field references, label lookup, root/variable references, Go-template control actions and whitespace trimming, and Docker/Go row-formatting functions are implemented. |
| `wait` | ✅ Yes | Container exit waiting and `--down-project` cleanup are implemented. |
| `watch` | ✅ Yes | Develop watch actions and options are implemented. |

## CLI Option Surface

`container compose --help` and `container compose COMMAND --help` are the authoritative usage views. Every documented long option surface is listed here with per-option parity markers.

A ✅ option means the flag itself is parsed and mapped for the current command behavior. Command parity is still a separate axis; command rows above describe any remaining operand or runtime limitations that are not represented as long options.

| Option Surface | Parity | Details |
| --- | --- | --- |
| Root options | ✅ Yes | ✅ `--all-resources`: selected-service `config` and `convert` output keeps unreferenced top-level networks, volumes, configs, and secrets, ✅ `--ansi` and `COMPOSE_ANSI`, ✅ `--compatibility` and `COMPOSE_COMPATIBILITY`: use Docker Compose legacy underscore separators for generated service and one-off container names, ✅ `--dry-run`, ✅ `--env-file` with comma-separated `COMPOSE_ENV_FILES` fallback, ✅ `--file`, ✅ `COMPOSE_IGNORE_ORPHANS` and `COMPOSE_REMOVE_ORPHANS`: suppress or remove project orphans, ✅ `COMPOSE_MENU`, ✅ `--parallel` and `COMPOSE_PARALLEL_LIMIT`: limit independent pull, push, and build engine operations, with `-1` as the unlimited default; dependency-sensitive lifecycle orchestration remains ordered, ✅ `--profile` and `COMPOSE_PROFILES`, ✅ `--progress` and `COMPOSE_PROGRESS`, ✅ `--project-directory`, ✅ `--project-name`, ✅ `COMPOSE_STATUS_STDOUT`: routes Compose-owned status/progress to stdout, ✅ `--verbose`. `COMPOSE_FILE`, `COMPOSE_PROJECT_NAME`, `COMPOSE_PATH_SEPARATOR`, and `COMPOSE_DISABLE_ENV_FILE` are compose-go root-loading defaults. `COMPOSE_CONVERT_WINDOWS_PATHS` is Windows-only and is not applicable to this macOS/Linux runtime. |
| `alpha` options | ✅ Yes | ✅ `--dry-run`. |
| `alpha dry-run` options | ✅ Yes | ✅ `--dry-run`: accepted and implied for the wrapped command. |
| `alpha scale` options | ✅ Yes | ✅ `--dry-run`, ✅ `--no-deps`. |
| `alpha watch` options | ✅ Yes | ✅ `--dry-run`, ✅ `--no-up`, ✅ `--quiet`. |
| `attach` options | ✅ Yes | ✅ `--detach-keys`: forwarded to the interactive runtime stream relay and ignored for output-only attach, ✅ `--dry-run`, ✅ `--index`, ✅ `--no-stdin`, ✅ `--sig-proxy`. |
| `bridge` options | ✅ Yes | ✅ `--dry-run`. |
| `bridge convert` options | ✅ Yes | ✅ `--dry-run`, ✅ `--output`, ✅ `--templates`, ✅ `--transformation`. |
| `bridge transformations` options | ✅ Yes | ✅ `--dry-run`. |
| `bridge transformations create` options | ✅ Yes | ✅ `--dry-run`, ✅ `--from`. |
| `bridge transformations list` options | ✅ Yes | ✅ `--dry-run`, ✅ `--format`, ✅ `--quiet`. |
| `bridge transformations ls` options | ✅ Yes | ✅ `--dry-run`, ✅ `--format`, ✅ `--quiet`. |
| `build` options | ✅ Yes | ✅ `--build-arg`, ✅ `--builder`, ✅ `--check`, ✅ `--dry-run`, ✅ `--memory`, ✅ `--no-cache`, ✅ `--print`, ✅ `--provenance`, ✅ `--pull`, ✅ `--push`, ✅ `--quiet`, ✅ `--sbom`, ✅ `--ssh`, ✅ `--with-dependencies`. |
| `commit` options | ✅ Yes | ✅ `--author`, ✅ `--change`, ✅ `--dry-run`, ✅ `--index`, ✅ `--message`, ✅ `--pause`: the default uses a brief filesystem-consistent live snapshot for running containers; `--pause=false` uses a best-effort no-freeze snapshot. |
| `config` options | ✅ Yes | ✅ `--dry-run`, ✅ `--environment`, ✅ `--format`, ✅ `--hash`, ✅ `--images`, ✅ `--lock-image-digests`, ✅ `--models`, ✅ `--networks`, ✅ `--no-consistency`, ✅ `--no-env-resolution`, ✅ `--no-interpolate`, ✅ `--no-normalize`, ✅ `--no-path-resolution`, ✅ `--output`, ✅ `--profiles`, ✅ `--quiet`, ✅ `--resolve-image-digests`, ✅ `--services`, ✅ `--variables`, ✅ `--volumes`. |
| `convert` options | ✅ Yes | ✅ `--dry-run`, ✅ `--format`, ✅ `--hash`, ✅ `--images`, ✅ `--no-consistency`, ✅ `--no-interpolate`, ✅ `--no-normalize`, ✅ `--output`, ✅ `--profiles`, ✅ `--quiet`, ✅ `--resolve-image-digests`, ✅ `--services`, ✅ `--volumes`. |
| `cp` options | ✅ Yes | ✅ `--all`, ✅ `--archive`, ✅ `--dry-run`, ✅ `--follow-link`, ✅ `--index`. |
| `create` options | ✅ Yes | ✅ `--build`, ✅ `--dry-run`, ✅ `--force-recreate`, ✅ `--no-build`, ✅ `--no-recreate`, ✅ `--pull`, ✅ `--quiet-pull`, ✅ `--remove-orphans`, ✅ `--scale`, ✅ `--yes`. |
| `down` options | ✅ Yes | ✅ `--dry-run`, ✅ `--remove-orphans`, ✅ `--rmi`, ✅ `--timeout`, ✅ `--volumes`. |
| `events` options | ✅ Yes | ✅ `--dry-run`, ✅ `--json`, ✅ `--since`, ✅ `--until`. |
| `exec` options | ⚠️ Partial | ✅ `--detach`, ✅ `--dry-run`, ✅ `--env`, ✅ `--index`, ✅ `--no-tty`, ⚠️ `--privileged` (capabilities only; Docker-complete privileged isolation/device behavior is unavailable), ✅ `--user`, ✅ `--workdir`. |
| `export` options | ✅ Yes | ✅ `--dry-run`, ✅ `--index`, ✅ `--output`. |
| `images` options | ✅ Yes | ✅ `--dry-run`, ✅ `--format`, ✅ `--quiet`. |
| `kill` options | ✅ Yes | ✅ `--dry-run`, ✅ `--remove-orphans`, ✅ `--signal`. |
| `logs` options | ✅ Yes | ✅ `--dry-run`, ✅ `--follow`, ✅ `--index`, ✅ `--no-color`, ✅ `--no-log-prefix`, ✅ `--since`, ✅ `--tail`, ✅ `--timestamps`, ✅ `--until`. |
| `ls` options | ✅ Yes | ✅ `--all`, ✅ `--dry-run`, ✅ `--filter`, ✅ `--format`, ✅ `--quiet`. |
| `pause` options | ✅ Yes | ✅ `--dry-run`. |
| `port` options | ✅ Yes | ✅ `--dry-run`, ✅ `--index`, ✅ `--protocol`. |
| `ps` options | ✅ Yes | ✅ `--all`, ✅ `--dry-run`, ✅ `--filter`, ✅ `--format` (the option is fully parsed and mapped; its row-template language limit is tracked on the command row), ✅ `--no-trunc`, ✅ `--orphans`, ✅ `--quiet`, ✅ `--services`, ✅ `--status`, including independent `created` and `exited` filters. |
| `publish` options | ✅ Yes | ✅ `--app`: publish an application image index linked to the Compose project artifact, ✅ `--dry-run`, ✅ `--oci-version`, ✅ `--resolve-image-digests`, ✅ `--with-env`, ✅ `--yes`. |
| `pull` options | ✅ Yes | ✅ `--dry-run`, ✅ `--ignore-buildable`, ✅ `--ignore-pull-failures`, ✅ `--include-deps`, ✅ `--policy`, ✅ `--quiet`. |
| `push` options | ✅ Yes | ✅ `--dry-run`, ✅ `--ignore-push-failures`, ✅ `--include-deps`, ✅ `--quiet`. |
| `restart` options | ✅ Yes | ✅ `--dry-run`, ✅ `--no-deps`, ✅ `--timeout`. |
| `rm` options | ✅ Yes | ✅ `--dry-run`, ✅ `--force`, ✅ `--stop`, ✅ `--volumes`. |
| `run` options | ✅ Yes | ✅ `--build`, ✅ `--cap-add`, ✅ `--cap-drop`, ✅ `--detach`, ✅ `--dry-run`, ✅ `--entrypoint`, ✅ `--env`, ✅ `--env-from-file`, ✅ `--interactive`, ✅ `--label`, ✅ `--name`, ✅ `--no-tty`, ✅ `--no-deps`, ✅ `--publish`, ✅ `--pull`, ✅ `--quiet`, ✅ `--quiet-build`, ✅ `--quiet-pull`, ✅ `--remove-orphans`, ✅ `--rm`, ✅ `--service-ports`, ✅ `--use-aliases`, ✅ `--user`, ✅ `--volume`, ✅ `--workdir`. |
| `scale` options | ✅ Yes | ✅ `--dry-run`, ✅ `--no-deps`. |
| `start` options | ✅ Yes | ✅ `--dry-run`, ✅ `--wait`, ✅ `--wait-timeout`. |
| `stats` options | ✅ Yes | ✅ `--all`, ✅ `--dry-run`, ✅ `--format` (the option is fully parsed and mapped; its row-template language limit is tracked on the command row), ✅ `--no-stream`, ✅ `--no-trunc`. |
| `stop` options | ✅ Yes | ✅ `--dry-run`, ✅ `--timeout`. |
| `top` options | ✅ Yes | ✅ `--dry-run`. |
| `unpause` options | ✅ Yes | ✅ `--dry-run`. |
| `up` options | ✅ Yes | ✅ `--abort-on-container-exit`, ✅ `--abort-on-container-failure`, ✅ `--always-recreate-deps`, ✅ `--attach`, ✅ `--attach-dependencies`, ✅ `--build`, ✅ `--detach`, ✅ `--dry-run`, ✅ `--exit-code-from`, ✅ `--force-recreate`, ✅ `--menu`, ✅ `--no-attach`, ✅ `--no-build`, ✅ `--no-color`, ✅ `--no-deps`, ✅ `--no-log-prefix`, ✅ `--no-recreate`, ✅ `--no-start`, ✅ `--pull`, ✅ `--quiet-build`, ✅ `--quiet-pull`, ✅ `--remove-orphans`, ✅ `--renew-anon-volumes`, ✅ `--scale`, ✅ `--timeout`, ✅ `--timestamps`, ✅ `--wait`, ✅ `--wait-timeout`, ✅ `--watch`, ✅ `--yes`. |
| `version` options | ✅ Yes | ✅ `--dry-run`, ✅ `--format`, ✅ `--short`. |
| `volumes` options | ✅ Yes | ✅ `--dry-run`, ✅ `--format` (the option is fully parsed and mapped; its row-template language limit is tracked on the command row), ✅ `--quiet`. |
| `wait` options | ✅ Yes | ✅ `--down-project`, ✅ `--dry-run`. |
| `watch` options | ✅ Yes | ✅ `--dry-run`, ✅ `--no-up`, ✅ `--prune`, ✅ `--quiet`. |

## Upstream Compatibility

Released Apple `container` compatibility is not a supported-lane functionality gap. The Homebrew release lane requires the matched `stephenlclarke` runtime stack and preflights for it before runtime-backed Compose commands run. Stock Apple compatibility remains an upstream/release-channel blocker until equivalent runtime primitives are accepted by Apple and this plugin is updated to consume those upstream APIs.

## Runtime Gap Register

This is the complete known runtime and adapter gap register from the Docker Compose v5.3.1 audit on 2026-07-18. It distinguishes a missing Apple/fork runtime primitive from a `container-compose` adapter gap and from a Docker platform feature that local macOS Compose does not need to emulate. The surface tables above are the concise ledger; this section is the implementation backlog behind their partial markers.

The macOS-feasible Phase 4 metadata, state, and events boundary is closed by
`make docker-compose-phase4-parity`; its exact commits, ownership split, and
remaining primitive gaps are recorded in the
[Phase 4 handoff](https://github.com/stephenlclarke/container-compose/blob/3d77ec228c7f55a04f689d5e1453752fc0c27f72/docs/upstream/container-compose/PR-phase4-metadata-state-events-closure.md).

| Area | Missing Apple/fork runtime behavior | Compose adapter or compatibility work | Scope notes |
| --- | --- | --- | --- |
| CPU and memory controls | `cpu_rt_period`, `cpu_rt_runtime`, `mem_swappiness`, and `oom_kill_disable`. | No additional adapter work remains for fractional `cpus`, Docker Compose V2's zero/no-limit normalization, `cpu_period`, `cpu_quota`, or `cpuset`. | Positive fractional `cpus`, Docker Compose V2-compatible `cpus: 0` normalization, direct generic zero/unlimited CPU limits, explicit cgroup v2 CPU quota/period, CPU sets, integral CPU counts, byte-accurate hard memory limits, CPU shares, service/Deploy memory reservation, memory swap, and pids limits work. |
| Isolation and security | Named cgroup lifecycle/control beyond Compose's `cgroup_parent`, IPC sharing (`ipc: shareable`, `ipc: service:NAME`, and accepted `ipc: container:NAME`), PID sharing (`pid: service:NAME` and `pid: container:NAME`), engine ID-map/remapping policy, SELinux/AppArmor/seccomp profiles and other `security_opt` forms, and Docker-complete privileged behavior (host devices, device cgroups, security profiles, and host isolation). | Preserve and oracle-test empty/`host`/literal-`private` user namespace requests, then remove the current false-compatible `private` execution path from new creates while retaining it only for legacy migration. No remaining adapter work exists for host/private `cgroup`, `cgroup_parent`, PID, IPC, or UTS, persisted `stop_signal`, `stop_grace_period`, no-new-privileges spellings, unconfined `systempaths`, unconfined seccomp/AppArmor spellings, or disabled SELinux labels; retain diagnostics for the remaining primitives. | `cgroup_parent` creates a validated relative child hierarchy only inside the macOS-hosted Linux VM. The generic runtime owns the `/container` root and VM teardown owns its cleanup; there is intentionally no macOS-host cgroup or operator-managed parent lifecycle. `cgroup`, `pid`, `ipc`, and `uts` host modes omit the matching OCI namespace so the container uses the sandbox VM namespace; private remains the default. `userns_mode: host` preserves the sandbox VM user namespace. Today's identity-mapped `private` guest namespace is migration-only and must not stand in for Moby 29.2.1's pinned empty/`host` contract. Compose service/container PID and IPC sharing remains blocked on a generic durable multi-container sandbox with separately attached guest networks. `cpu_count`, `cpu_percent`, `isolation`, and `credential_spec` are Windows-only. |
| Networks and names | Namespace sharing for `network_mode: service:NAME`/`container:NAME`; arbitrary custom-name network modes; custom network drivers; custom IPAM drivers. | Implement the focused network/namespace designs. Reconcile legacy link environment/discovery claims through the [pinned links matrix](docs/remaining-macos-parity-closure-design.md#oracle-before-implementation); do not implement them from this stale sentence alone. | The matched runtime provides container-facing, network-scoped DNS for container names, service names, aliases, scaled address sets, source-scoped legacy and external links, and attachment lifecycle changes. Existing runtime attachment, static IPAM, `extra_hosts`, host/none modes, the built-in `bridge` mode, MTU, priorities, interface naming, and local-network `attachable` metadata remain usable. `network_mode: bridge` maps to Container's built-in `default` network and intentionally retains Docker's default-bridge no-service-DNS boundary. |
| IPAM | IPv6 allocation ranges and auxiliary addresses, multiple pools of one family, and disabled IPv4. | No additional adapter work remains for explicit IPv6 enablement/disablement, a single IPv6 gateway, or IPAM `options`: `config`/`convert` retain source IPAM metadata, while the effective local vmnet request omits an IPv6 pool and gateway when IPv6 is disabled, matching Docker Engine. | On macOS 26, vmnet's automatic IPv6 prefix implements `enable_ipv6: true` without an explicit subnet. A selected IPv6 gateway is validated as an in-prefix usable address, applied to vmnet, reported by network/container inspection, and becomes the primary guest's default route. For `enable_ipv6: false`, the Apple-shaped runtime disables NAT66 and router advertisements before network creation and inspection reports no IPv6 subnet. Docker Compose local mode ignores IPAM `options`; it is not an Apple runtime behavior gap. |
| Volumes and mounts | Non-local volume driver/plugin behavior; recursive bind and consistency/cache modes. | Dockerfile-declared targets create deterministic anonymous local volumes when unmasked. Compose initializes an empty local ext4 volume from the selected image subtree for an eligible named, anonymous, or inherited mount at either a declared target or an ordinary existing image path; `volume.nocopy: true`, an existing `volume.subpath`, a missing image path, and bind, tmpfs, or image masks deliberately skip it. Generated image volumes are labelled for `down --volumes`, `rm --volumes`, and `--renew-anon-volumes`, while a populated volume is retained across `down`/`up`. | A `volume.subpath` is validated by the runtime and must exist before mounting; its parent volume remains unmodified. The selected-tree archive path preserves file data, mode, uid, gid, hard links, and failure atomicity in focused tests. A reused volume is never reseeded once it has entries beyond ext4's `lost+found`. SELinux relabeling, Windows `npipe`, and Swarm `cluster`/CSI mounts are platform or orchestration specific. |
| Docker API socket | Released `container-engine-api` 0.3.5 at `78cb4cb5781d6dbe9f0d34a1b925ee8dcaacdc98` owns neutral wire types, the generated 107-operation API 1.44 through 1.53 ledger, hardened Unix listener, shared `container-engine` executable, gateway ping/WebSocket transport, exclusive provider fingerprint, deterministic private-session shutdown, starvation-free frame I/O, and fail-closed response composition. Signed local Engine API `4949e743675f` and Container `259878a427de` add system version, native-authority container/image discovery, and native-authority public unauthenticated image pull/tag/delete. `/_ping`, unversioned Docker CLI `info`/`version`/`ps`/`images`/`image inspect`/`pull`/`tag`/`image rm`, `/v1.53/info`, TTY attach, and paired non-TTY logging/discovery/mutation certificates pass. Registry credentials, push, search, history, build/session, remaining route handlers, typed guest grants, explicit authority handoff, Testcontainers/devcontainer adoption, and the complete external-client matrix remain absent. | Finish `STORAGE-WP-02` performance/authority evidence, remaining route closure, credential, relay, lifecycle, grant, and oracle work in the [non-local volume, advanced mount, and API socket parity design](docs/non-local-volumes-advanced-mounts-api-socket-design.md) before enabling `use_api_socket`. | This is not yet a `use_api_socket` or full Docker REST parity claim. The [container discovery oracle](docs/parity/docker-rest-discovery-oracle.md) retains its 2.45× lifecycle gap. The [image discovery oracle](docs/parity/docker-rest-image-discovery-oracle.md) passes its declared 10-ms micro-operation noise floor while retaining list-size, shared-size, last-tag-time, and intermediate-image gaps. The [image mutation oracle](docs/parity/docker-rest-image-mutation-oracle.md) verifies the cached pull/tag/inspect/remove sequence at 17.79% faster than Docker while retaining credential/push/build-session gaps. Public service and logging lifecycle are live-proven; the socket grant, bounded route capabilities, and complete external-client matrix remain incomplete. |
| Devices and GPUs | Generic driver/options/capability/CDI/lease semantics plus provider-specific hardware support are incomplete. NVIDIA/CUDA, multiple physical GPUs, arbitrary passthrough, and accelerated rendering may not be exposable on the evidence host. | Implement generic/non-GPU semantics through DeviceBroker, then classify each concrete provider with the [same-host feasibility contract](docs/remaining-macos-parity-closure-design.md#provider-feasibility-probe). | The current single virtio GPU/DRM projection is only an advertised subset after real functional proof. Absent hardware receives the exact unavailable result; metadata-only or fabricated success is prohibited. |
| `logging` | Distinct local and remote drivers, durable lifecycle/recovery, public REST logging/lifecycle, provider-root trust, record/file-backed devcontainer handoff, TTY attach, the Compose 5.3.1 static/foreground reliability contract, direct Engine GELF wire/configuration component contracts, and the Docker-CLI non-TTY REST certificate are implemented. Coordinated publication, Testcontainers/devcontainer external-client coverage, and the complete migration, provider, failure, security, cold-resource, and release-performance evidence remain unverified. | Run the remaining matrix, optimise release performance, then release-sign and publish the synchronized stack after all programme development. | Compose `eb9b43c0`, Container `267991f22171ce6e438703f68a12159c1c57839c`, Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, and Engine API `c7973ac641fb6f6e07df1358114f36222bd9ca59` pass the Docker Compose 5.3.1 signal/log fixture, 19 focused Swift tests, paired Docker Engine/public-socket CLI certificate, and narrow direct-Engine GELF wire/configuration fixtures with matched provider-component tests. Earlier signed heads retain provider, migration, protected handoff, and public lifecycle evidence. See the [logging design](docs/docker-logging-driver-semantics-design.md) and [oracle](docs/parity/docker-logging-driver-oracle.md). |
| Container metadata and state | Docker lifecycle state values `dead`, `restarting`, and `removing`, plus their full inspect semantics. | No further adapter work is currently possible for those states: Compose now derives Docker `created` from a stopped snapshot without `startedDate` and `exited` from a stopped snapshot after a recorded start. | `ps --status` and `--filter status=` select `created` and `exited` independently. Docker Compose V2 accepts `stopped` but does not treat it as an `exited` alias; Compose preserves that no-match behavior. Labels, OCI annotations, and exposed ports are distinct runtime metadata; ports and command/entrypoint values including explicit empty-list overrides are already mapped. |
| Events | Docker actions for oom, explicit restart, rename, resize, update, attach/detach, and related attributes. | Render each additional action once the runtime emits it. | Create/start/pause/unpause/stop/delete/health events are available today. The matched runtime also emits Docker-shaped `kill` (signal/process), `die` (exit code where observed), `destroy`, and `exec_create`/`exec_start`/`exec_die` (exec ID and terminal exit code) events; Compose suppresses the preceding generic `delete` action so removal is reported once as Docker's `destroy`. Docker reports automatic policy restarts as `die` followed by `start`, not as a `restart` action. |
| Build | None for the external-Dockerfile or tar-export paths. Apple `container` commit [`d1d7635`](https://github.com/apple/container/commit/d1d763530df3c6a326dbae7f0c0a59a335808045) moved the complete Builder coverage into parallel suites while fixing the shared Builder startup race; the signed fork synchronization is [`1bc3167`](https://github.com/stephenlclarke/container/commit/1bc31674629287f3386637db4c6d8652dc36602a), followed by the fixture-only reconciliation [`abed15f`](https://github.com/stephenlclarke/container/commit/abed15fdd0cafe340f8aceb65080e4a88d0ceb0a). | None for the macOS-feasible local Build Specification. `build.no_cache_filter` uses the generic filter-valued Container/Builder primitive; external Dockerfile projection needs no runtime workaround; external build secrets use the Compose-owned secure-store SPI and invocation-private files rather than adding an Apple runtime secret store. | Docker Compose local mode ignores build-secret `uid`, `gid`, and `mode`. Its external build-secret reference is retained by `config`, omitted from bake output, and unavailable to the local Docker engine. Compose matches that visible projection while its documented macOS Keychain extension supplies live Apple builds. Swarm secret drivers are outside local macOS parity. |
| Lifecycle hooks | Stock Apple still lacks the pinned fork's interactive init-process reattach primitive. | No supported-lane Compose work remains: `pre_start` helper orchestration and interactive foreground `run` lifecycle handling are implemented. | Docker Compose itself rejects `pre_start.per_replica: true`; the committed lifecycle fixture covers inheritance, ordering, failure cleanup, idempotence, scaling, restart/start semantics, foreground output, removal, and exit status. |
| Deploy and scheduling | Lossless general reservation `DeviceRequest` projection plus preservation/ignore behaviour for valid CPU/generic reservations and scheduler-only fields. | Remove the remaining false local rejection gates, retain compose-go errors for reservation PIDs and device/generic limits, and route every valid reservation device through DeviceBroker. The job-specific wait/restart handling is already verified removed. | CPU/memory/PID limits and Deploy memory reservations already use ordinary local controls; `replicated-job`/`global-job` now use the same local convergence as ordinary services. Swarm placement, rolling update/rollback timing, and job scheduling are not missing local Compose orchestration. |
| Output templates | None. | None. | `ps`, `stats`, and `volumes` implement Go-template control actions, nested/root/variable paths, deterministic array/object ranges, label lookup, whitespace trimming, and Docker/Go row-formatting functions. The committed `compose.yaml` oracle confirms Docker Compose v2. |
| Models | A model-runner backend that can start models and provide endpoints. | Inject endpoint/model variables after the backend exists. | Parsing and `config` rendering are already supported. |

## Remaining Gap Focus

- `container compose help` is an intentional local extension: the outer `container help compose` command cannot dispatch to a plugin. It is listed in root help, documented in the CLI-surface allowlist, and returns the same Compose-layer help as `container compose --help`.
- Prioritize namespace sharing, state/events, and Docker logging drivers before expanding platform-only or Swarm-only surfaces; named-network service discovery and source-scoped link aliases are complete on the supported stack.
- When touching slow runtime paths, keep first-frame progress rendering covered so local `container compose` runs do not appear to hang before visible output.
