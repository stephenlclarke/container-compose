# feat(build): migrate Compose to the shared native Bazel workflow

Pull request: [708](https://github.com/stephenlclarke/container-compose/pull/708).

## Motivation

Implement the operator-approved [recoverable Bazel build request](ISSUE-build-bazel-workflow.md) without another coordinator or reference rebuilds.

## Implementation

Coverage-only atomic Swift/C counters and sandboxed Swift compilation prevent the reproduced counter-loss/stale-incremental-object mechanism without changing ordinary build workers, Bazel action caching, product sources, coverage denominator or the 90% threshold. Focused stock Engine/SPI coverage passes 120 functions (`0036da32`, 38.131 seconds); [scope and remaining aggregate gates](guides/BAZEL.md#validated-coverage-evidence) distinguish this component proof from full quality/release qualification. The exact-counter compiler regression is maintained once in the shared devcontainer workflow.

The enhanced component check passes Core (1,285 functions) and Container runtime (39). Its initial wrong-target selection exposed the root module's undeclared `@platforms` constraint reference; declaring the already-selected 1.0.0 module fixes visibility without upgrading dependencies or relaxing target compatibility. [Exact failure, corrected test and direct constraint-query evidence](guides/BAZEL.md#prepared-foreground-launch) distinguishes this from enhanced live qualification.

Stock E13 now passes all five signal/continued-identity/exit/removal observations using the immutable optimized packages, with zero owned residue and clear recovery. [Exact evidence](guides/BAZEL.md#prepared-foreground-launch) records invocation `347cf245-9f7e-4b75-9eb0-9f379701280b`, signed sources and measured durations. The pinned Docker oracle still duplicates USR1 and fails; its incomplete operation is not a valid performance denominator. Full parity and stable publication remain open.

Prepared foreground launch now forwards HUP/INT/QUIT/TERM/USR1/USR2 through the existing signal proxy to the immutable Engine ID. Guest exit survives signal/removal races; proxy ownership and stalled signal requests cancel and join safely before handler restoration. Fail-before mutations prove forwarding and cancellation regressions, and the corrected Core/Engine component gate passes with clean scoped lint and full independent review. [The guide](guides/BAZEL.md#prepared-foreground-launch) records exact results and the real Docker oracle's unresolved duplicate USR1 delivery. No full signal parity, coverage or stable-release claim is made.

The invalid-input path now starts dependencies before rejecting interactive TTY input, while job image/resource preparation and health-condition waiting occur afterward. `ComposeRunOptions.inputIsTerminal` carries caller context; the CLI detects it once and emits the exact pinned Docker diagnostic with exit 1. Shared dependency/job resources are prepared once. Dependency launch reuses the prepared service request builder instead of the legacy argument-only fallback, preserving gateway identity and inspection. Regression mutation `9d0685ca-6085-43dd-9bad-9b5b160fd48f` fails when rejection and prepared dependency requests are removed; corrected component `32f46429-5af0-4a4d-85dd-245edf8cfdd1` passes Core, Plugin and Engine runtime targets, with clean independent review. Docker and the packaged stock candidate both pass E12's five post-error-state observations with zero owned residue; the raw 9.04x operation ratio is informational, not quiet benchmark evidence. [The guide](guides/BAZEL.md#prepared-foreground-launch) records exact sources, immutable package and result seals, distinguishes component ordering evidence from post-error snapshots, and retains the open enhanced, full foreground and release gates.

The unreleased `run` CLI now preserves terminal flag presence, grouped short options and explicit false values, selects the default from host stdin/stdout and overrides service `tty` in both directions. Conflicting explicit terminal flags fail regardless of value. The helper retains option data and guest boundaries, including the review-found attached-value regression. Existing non-terminal lifecycle fixtures now request that mode explicitly without weakening assertions. Maintained E11 now passes all five observations on Docker and the corrected stock package, with zero residue and exact timings/seals retained. [Component and live-proof status](guides/BAZEL.md#prepared-foreground-launch) keeps this change separate from the open raw 14.94x performance concern and remaining terminal/error-phase, signal, enhanced, quality and distribution gates.

Pinned Docker quiet-mode evidence confirms that guest input/output and exit status remain intact. The legacy one-off launcher now keeps the same input mode under `--quiet`, with progress still suppressed; its piped/terminal regression fails before correction. Public help and README distinguish progress from guest streams. The maintained E10 fixture now passes all five guest-I/O/exit/removal observations on Docker and stock with zero residue; the guide retains exact timings and the open approximately 16.50x raw operation ratio without claiming enhanced, full foreground, quiet-machine performance or stable-release qualification.

Real stock E09 inspection identified the incorrect non-interactive default for one-off `run`. CLI and Swift options now default to open stdin, explicit disabled input overrides service settings, boolean normalization preserves guest arguments, and detached one-off requests retain Docker's stdin-close policy. Regression tests cover the public parser, all service/default/explicit combinations and typed detached requests; existing non-interactive lifecycle tests opt out explicitly without weakening their assertions. [Current evidence and remaining foreground gaps](guides/BAZEL.md#prepared-foreground-launch) distinguish this unreleased fix from live, quiet-output, quality and release qualification.

Prepared foreground `run` is now implemented and component-tested. The Engine client negotiates acknowledged native exit registration, attaches/registers before start, drains output during startup and restores cancellation-safe host I/O. The registered result survives auto-removal, including TTY resize/removal races. Whole-lifetime cancellation remains effective after output EOF. [The current guide](guides/BAZEL.md#prepared-foreground-launch) records reachable shared dependency `c04ed07`, source attribution, exact fail-before/corrected tests, 91 passing component functions, clean independent review, bounded-session limits and rejected instrumentation. This is an unreleased C02 checkpoint; complete live foreground/history, quality and distribution gates remain open.

The shared workflow digest lock adopts devcontainer's coverage-integrity guard: fresh gates and retained exports reject merger diagnostics, out-of-range counters and missing diagnostic logs. This protects the measured denominator without excluding product lines or changing the 90% requirement. Devcontainer's native instrumented suite still has an unresolved counter-underflow defect; these pins do not certify Compose coverage or a stable release. Runtime launch behavior is unchanged by this helper update.

The initial launch review found loss of declared image spelling in gateway inspection. The corrected implementation explicitly negotiates `ContainerImageReference=1` and sends the declaration separately from the immutable selected ID. Matching gateway metadata preserves presentation across recovery without resolving tags or trusting raw native labels. [The final paired component gates](guides/BAZEL.md#stock-compose-health-policy-candidate) pass 68 Engine-adapter and 358 affected devcontainer functions. Earlier staged/activation test counts below are historical, not final-head evidence.

Prepared create and detached run invoke gateway create/start with the complete typed request and returned immutable container identity. They no longer parse native argv or fall back to native creation after an error. Conflicting logging/empty names fail before allocation; failed startup stays inspectable without unsafe retry or deletion. Legacy argument-only callers are unchanged. Prepared foreground support is now described above. [The earlier activation evidence](guides/BAZEL.md#stock-compose-health-policy-candidate) retains the pre-foreground component results without qualifying the current source or a stable release.

Core now selects an immutable provider image before health and volume preparation. An additive optional `ComposeImageSelection` crosses the runtime interface and Codable create plan; old providers return nil and old plans remain decodable. Health metadata, rendered health arguments, image volume declarations and copy-up use the same selected reference/platform, while labels, config hashes and the declared image reference remain unchanged. The Engine request consumes that selection without reopening the mutable tag, validates its identity and both carried/requested platforms, and binds process defaults plus the encoded image to the selected ID. Offline dry-run performs no selection. Final affected stock coverage `523a972a-2b27-46ea-865b-6dc936d8a2e9` passes Core (1,123 functions), SPI (25, cached) and Engine adapter (63): 59.751 seconds including compilation; target times 3.9/0.3/1.1 seconds. Selection helpers cover 49/51 lines (96.1%). Independent final review is clean. New helpers, Engine/SPI sources and the refactored planning function pass strict lint; the older large launch function and test-support file retain their documented quality findings. Receiver support and create/start transport remain pending: the legacy CLI still launches the declared reference, so this is not end-to-end immutable launch, C02 qualification or quiet benchmark evidence.

Environment preparation now captures file bytes in order with the existing owned-process runner, a combined 16 MiB limit and a 30-second whole-operation deadline, including cancellation of unopened FIFOs. Literal file parsing preserves BOM/CRLF, empty values, later-file precedence and the current provider's explicit-environment precedence; host lookup applies only to requested bare keys. Errors do not relay file content or subprocess stderr. Request construction consumes the captured values without shell interpretation. Full Engine-adapter coverage `1a082dd7-6e3b-4ef2-99ac-72b2c17fd9cb` passes 57 functions (1.172 seconds invocation, 0.8-second target); the environment helpers cover 84/87 lines (96.6%). An initial CRLF regression is retained at `331ce756`; corrected focused runs and the full gate pass. Independent review and strict touched-source lint are clean. This completes the preparation component, not live create activation or full Docker Compose precedence parity.

Prepared gateway request construction is now staged, but deliberately not selected by `launchContainer` yet. It projects process clear/inherit semantics, environment-removal markers, concrete ports, endpoint settings, bind/volume/tmpfs/image mounts, health, resource/security/namespace/logging and ordinary restart settings without parsing a command-line vector. Image mounts remain read-only and bracketed prepared IPv6 bind addresses are normalized for the wire API. Native-only socket/init-image/block-IO/restart timing/device policies, scoped aliases/MTU/interface names, mount ownership and unresolved environment files fail explicitly instead of disappearing. Those gaps and receiver-side capability/semantic checks must be completed before activation. Focused stock coverage `4fe90545-d208-4438-bbfc-2ccc7c15195f` passes 14 functions (6.011 seconds invocation, 0.7-second test target); the six new source helpers cover 306/328 lines (93.3%). Strict touched-source lint and final independent review pass. This is staging evidence, not live C02 or expanded compatibility.

The active C02 creation-routing change now passes prepared service configuration across the runtime interface for up/create/one-off/pre-start launches. Codable/Equatable create models and defaults move to SPI while Core re-exports them. Resource/security/namespace/device/lifecycle/DNS and invocation settings join the previously retained process, networks, mounts and allocated ports. Existing argument rendering consumes the same plan; preparation is not repeated. Full affected stock coverage passes 1,120 Core, 25 SPI and 33 Engine-adapter functions. [Current evidence and remaining work](guides/BAZEL.md#stock-compose-health-policy-candidate) distinguish this provider handoff from pending gateway projection/creation and retain legacy quality findings. No gateway capability is advertised by this change.

Current C02 stock evidence (`1dca60f5`, Compose `c8a031de`, devcontainer `c7fe7d2`) confirms actual service startup and database health after the correction. Guest service-name resolution still fails; cleanup passes with zero owned resources. The optimized Compose archive/smoke gate passes in 41.120 seconds (`b0b1242a`). [The guide](guides/BAZEL.md#stock-compose-health-policy-candidate) records the remaining managed-host adoption gap, exact timings and immutable failure. No C02 parity or release success is claimed; this supersedes pending-live notes below.

C02 adds a bounded version-1 requested health-policy label for the stock Engine adapter, capability preflight before project resources and a second launch-boundary check. Guest arguments and enhanced native behavior are preserved; offline dry-run does not contact a gateway. Invalid/conflicting policy, caller reserved labels and unsupported startup-interval fields fail explicitly. Matching devcontainer PR 83 validates both adoption paths and derives health from real probes. [The guide](guides/BAZEL.md#stock-compose-health-policy-candidate) records component/coverage evidence and evaluator limits. Full source review is clean; corrected live C02 and release qualification remain pending. No fixture, pin or quality threshold is relaxed.

The C01 devcontainer integration fix preserves the logical Compose network key separately from its native resource name, using one label map for real and dry-run creation. The stock Engine launch adapter retains a digest-qualified original image operand in a reserved label without changing the actual image or application arguments. Caller label collisions and malformed digests fail before launch; tag-only behavior is unchanged. Valued/grouped short options and stock registry/DNS options remain distinct from the image operand. Devcontainer PR 83 accepts the spelling only after checking the actual native descriptor and repository. No Docker executable, registry lookup, runtime pin or test waiver is added. Logical-network coverage `22eed813-28df-4954-b8d4-e3a51cee6d3c` passes four selected functions; final full Engine-adapter coverage `240941b9-73b1-4e4a-b1c6-efa1f9b48fd1` passes 28 functions. The shared helper lock and guide admit devcontainer `014abca4cc6b1a9ed04b104a0dbfa687676d27be`'s already-tested artifact admission/private CLI preparation helpers.

Stock live C01 passes all three original observations and cleanup at Compose `1fe36f56d88a217f2dd95c5d5a0189cd1710afd5` with devcontainer `aa56f00dc8038da7cd0d23ccfa3d0fa287084930`, invocation `178bf269-d0d4-48e1-ac73-8ca375c2707d`. Setup/operation/cleanup take 7.059474833/4.164177458/1.628399333 seconds; zero owned resources remain and recovery is clear. Seal: `ca9559cb705e150b8946f09759e5641a244372969ed32a25335a56e952247d5b`. Compose's optimized package/smoke invocation `33d4f1ab-ade6-4147-bb1d-2fd212b6182b` passes in 41.339 seconds, archive `e73450b403510a42ae95951adafed0125776087681ad158ab176565ecef06942`. Enhanced qualification remains blocked on the missing pinned guest image; C01 is handed off incomplete, not full three-lane Verified. These are unsigned candidates and ordinary functional timings, not a new stable release or quiet paired benchmark.

The shared provenance correction compares captured content directly with the commit tree, covering hidden index flags, ignored Swift/catalog inputs, file modes and direct tracked symlink targets. Indirect or untracked-target links cannot attest a clean checkpoint. All 35 shared launcher/source tests pass; exact helper hashes and the operator guide pin move together. This is a build-evidence correction, not a change to Compose runtime semantics or a stable release claim.

Devcontainer review `4048788515` identified dirty-source DocC mislabelling. Compose adopts the identical analysis-time clean-checkpoint guard and updated shared launcher/test digests. Caller-supplied cleanliness cannot override the captured source snapshot; other development builds may still use dirty sources with retained provenance. Both guide and rule now require a clean committed documentation checkpoint.

The opt-in `docs` target now builds and tests a native DocC site from the existing optimized `ComposeCore` and `ComposeRuntimeSPI` Bazel modules. It reuses the identical family rule/action/test snapshot introduced in devcontainer, including per-module cacheable conversion, cross-module resolution, safe declared-link materialization and source/module inventory. No SwiftPM build, runtime service or publication is started. Final documentation publication remains after release/parity acceptance; the legacy public Pages workflow is unchanged in this slice.

Final review-thread reconciliation also found `4044080371` still applicable: both normalizer minima remained 120 despite the security regression adding its parent and two subtests. Both profiles now require 123 actual XML cases, including the unchanged telemetry-redaction regression. The existing retained XML contains all 123 cases and the three named security cases; this is a stricter discovery gate, not new test execution or altered coverage.

GitHub review `4046847265` found the launcher setup still named the older helper checkout after the admission lock changed. The guide now names full reviewed devcontainer commit `86834d7b8ab85f4623ec93512762bd3b839b2474`, matching all 28 admitted helper byte/mode entries; historical validation records remain unchanged.

Source-check job `105594475934` in run `35343448997` rejected the abbreviated SPDX-only header in `ComposeHelpDispatchTests.swift`. The fixture now uses the repository's canonical Apache-2.0 header; its test body is unchanged. The local licence-header check passes without exclusions or policy changes.

The shared-tool lock admits devcontainer `86834d7`'s additional mixed-empty licence fixture in `test_candidate_archive.py`. Independent controller review caught this single changed helper (27 of 28 were already identical) before dispatch. Only that reviewed helper hash changes; runtime dependencies, production source, coverage policy and thresholds are unchanged. This keeps exact source admission and the downstream shared-tool byte check consistent.

The final hosted style run `35334813508` rejected long lines and oversized functions in the portable copy/filesystem tests moved into both profiles. The follow-up wraps calls, extracts unchanged literal fixtures into private helpers, and uses exact-error Swift Testing assertions for three port failures. All original payloads, identity fields, ordering and success/failure assertions remain; no lint suppression, coverage exclusion or production behavior changes. Strict SwiftLint, the repository's normal SwiftFormat check, and independent full-delta review are clean. The final focused stock core target passes (`59bf4ccd-6525-4f50-b112-6184dfe1eed8`, 39.947 seconds development elapsed including test-module compilation). Full current-head remote checks and enhanced qualification remain required.

- Native Swift product targets preserve both Package.resolved profiles and Swift package-access boundaries.
- The executable links the same ComposePlugin module intended for CLI tests; no SwiftPM build subprocess is involved.
- The family launcher and evidence/cache/storage implementation are reused from devcontainer PR [83](https://github.com/stephenlclarke/devcontainer/pull/83), with every shared executable helper bound by the consumer's byte/mode lock.
- The separate opt-in Makefile avoids legacy Makefile parsing, signing discovery or runtime lifecycle operations during native graph qualification.
- Native Go helper and Linux ARM64/AMD64 initializer products use the existing Go version and dependency pins. Both unit profiles include the six Go test targets, with nonempty case-level XML.
- Swift fixtures and resource bundles are declared; CLI/parser tests use native products. Scratch selection and same-directory atomic publication keep temporary work on the SSD without changing persistent state locations.
- A narrowly isolated local-only permission-bit test retains its assertions outside Darwin's sandbox, which strips those bits from the source fixture. All other migrated tests remain sandboxed.
- Declarative consumer inventories drive the shared test/coverage validator. Retained coverage export requires authenticated clean source evidence; the separate 90% gate binds the current consumer SHA, runtime profile and policy before checking raw line counts. No tests or builds are repeated for report export.
- Native optimized candidate packaging uses pinned rules_pkg and the existing plugin layout, with declared resources, version/dependency/capability metadata, four product hashes and an exact archive receipt. Real archive smoke tests check paths, modes, hashes, packaged version and local bundled-parser behavior. The retained archive can be restored without a build. Distribution and licence-closure readiness remain explicitly false.
- Release-note fixtures disable background Git maintenance locally before committing, preventing the observed CI temporary-repository cleanup race; their focused suite now runs under Bazel as well.
- Native dependency notices reuse fetched Go/Swift/SDK inputs with metadata-only overlays. Exact Go module/version/text inventory validation, deterministic text hashing and archive assertions prevent silently incomplete Go notice bundles; all Swift root notice variants are retained. No product pins change. Complete nested-vendor/legal closure remains explicitly unclaimed.
- Stock now executes 1,056 runtime-neutral Core cases rather than omitting the entire suite. Shared project/data/orchestration fixtures no longer require enhanced provider imports; concrete clients/models live in separate enhanced-only support files. Both build manifests retain an identical five-file provider-coupled inventory and explicit profile guards in mixed suites, while enhanced retains all 1,246 Core cases. The stock evidence policy requires the additional target and executed normalizer/process/up/run/API-socket production probes.
- Native address/thread sanitizer targets instrument the Swift/C graph, while a separate Go race target selects rules_go's race mode. A narrow declared rules_go context patch prevents incompatible Clang sanitizer runtimes from being injected through CGo. No Swift/C instrumentation, assertion or test is suppressed.
- Add internal, default-off per-run cancellation observations without changing signal/queue/termination semantics. Existing cancellation tests retain the two-second bound and both PID-absence checks, and now publish payload-free monotonic phase JSON. A normal-completion check validates observer phases and uniqueness; the new neutral test brings current Core counts to 1,057 stock and 1,247 enhanced.

## Validation

Paired native Sonar analysis `bce5b5df-4ff7-42e8-b979-4443316e4fb8` passes its gate with zero unresolved issues/hotspots at `0347ce6b4d7cec871fb98ba0e6faa73237bd9f35`, but identifies the literal Unicode replacement character in two logging-oracle assertions as an encoding warning. The assertions now spell the same scalar as `\u{fffd}`; expected message bytes, NUL handling, oracle fixtures and production behavior are unchanged. No encoding setting, exclusion or quality threshold is relaxed. Focused enhanced invocation `4a492d73-1ace-4592-a409-05c6b9c6a27e` passes all 17 logging-oracle cases with no failures or skips; independent review and strict touched-file style checks pass. Updated analysis is still required; the earlier passed scan is not evidence for the later head.

The configuration-output follow-up replaces three Foundation atomic-write calls with the existing same-directory private writer. Eleven additional native CLI cases prove config/convert output, private permissions, successful staging cleanup, variables/defaults, interpolation opt-out, stable service hashes, quiet mode, service projection and explicit build-info JSON without a project. All 42 cases pass on stock (`48540380-6142-4b09-9d28-a42ee0bd0fa0`) and enhanced (`c2ce8c1b-059b-460e-91d5-e902070aaade`); both inventory floors are raised. The guide preserves the failed regression and distinguishes fixture-supplied metadata from packaged provenance. README and DocC describe the unreleased output-file behavior. SwiftFormat passes; direct strict SwiftLint still reports existing violations in the legacy-excluded CLI source, so a clean whole-file lint result is not claimed. No style exclusion or release gate changes.

The enhanced image adapter retains all live API calls while exposing its deterministic metadata and transformer projection internally. Six new test methods verify complete metadata mappings, platform choice/refusal, defaults, healthcheck units, sorted volume/port declarations, transformer labels and registry-port/digest references. Native target `a665e4b5-6fc8-4d9c-87f5-24d88ea2f5d1` passes 39 runtime methods in eight suites; strict touched-source checks and independent review are clean. The evidence policy raises the discovery floor to 39 and requires this source to execute. This is no-runtime component evidence, not complete coverage or parity. README and native test guidance record that scope; public CLI, DocC API, installation and stable support claims are unchanged.

The CLI boundary follow-up fixes guest help/option interception in both the custom dispatcher and the root argument parser, and prevents guest `--dry-run` or literal `alpha dry-run` from suppressing installed-runtime preflight. Eleven new Swift methods cover the root parser, all documented help paths, aliases, explicit/literal terminators, option values and preflight. Native CLI cases expand from six to twelve independent JUnit cases. Red regressions and corrected stock/enhanced checks are recorded in [the boundary evidence](guides/BAZEL.md#guest-command-argument-boundary); independent final source/test/policy review is clean. README, embedded help and DocC source describe the unreleased candidate semantics. Installation, runtime capability schema, examples, dependency pins and stable-support claims are unchanged. No runtime service, installed release or approval state was changed.

Clean implementation commit `9545a0be72916d449c398921b96aafe50b8a6f35` passes both complete eleven-target coverage aggregates. Stock `45f948c6-1068-4c7f-8a6e-bf7f667a39f2` measures 26,344/29,986 production lines, 87.8543%, up 544 exercised lines from 86.0402%. Enhanced `6064b7c7-73f9-4df9-b505-ce339196b94e` measures 28,406/32,466 lines, 87.4946%. Both exact-context 90% checks still fail correctly. These are historical measurements for that exact implementation, not coverage or release qualification for the later CLI changes; timings and scope are in the guide.

The portable-filesystem follow-up moves 34 unchanged copy/export/commit/port methods into both profiles and adds three parameterized commit-boundary methods covering 13 scenarios. All 56 original bodies remain unchanged; 22 concrete archive/provider methods stay enhanced-only. Core XML inventories rise to 1,094 stock / 1,250 enhanced, with policy floors and executed production-file probes updated accordingly. Focused stock `c491c5c3-ba9c-4a3b-80c1-8e4634d1d1b9` and enhanced `e8625b69-444a-4ca3-a8d0-4d00cabe9971` pass. [The scope/evidence record](guides/BAZEL.md#portable-filesystem-orchestration-coverage) distinguishes provider-neutral transaction proof from actual stock runtime or archive conformance, and records formatting/static-warning and measurement boundaries. No production, dependency, installed-release or compatibility behavior changes. Public CLI help, installation, DocC, examples and stable-support documentation therefore remain unchanged; native test guidance and this handoff are the affected documentation surfaces.

Three further reviewed source attributions now accompany the filename-discovered notices: Swift NIO's WIDE Project SHA1 licence header, its pinned uSHET licence and Yams' LibYAML licence. The offline inventory binds selected package revision, exact source URL/revision/range and text digest; candidate identity records the separate fragment evidence. Sixteen package-unit cases pass at `983f67f8-1b6a-45e3-bf80-a2f37824cfd3`; actual optimized archive/CLI/parser smoke passes enhanced `d6298243-16f6-42bd-a788-8e50c70b8dcf` (137 texts, 5.169 seconds) and stock `19d15867-cc96-4685-9508-72afdc721bb5` (135 texts, 9.515 seconds), two cases each. [The attribution record](guides/BAZEL.md#reviewed-source-header-and-external-attributions) explains why the LibYAML source URL is not an exact-vendor-checkout claim and why the complete uSHET licence also includes a jsmn section. Independent source review verified every retained text against upstream bytes. Comprehensive audit and distribution readiness remain unclaimed; public CLI, install/README, DocC, compatibility, runtime and stable-release documentation remain unchanged because only opt-in package metadata changes.

The nested-notice follow-up extends the metadata collector to root plus `Sources/`, preserving five source-local licence texts omitted by root-only discovery. Validation requires their exact headings and tests removal of each independently. Fourteen package-unit cases pass at `e34fc329-cf0a-4c91-9bf6-79a46e9a7dd6`; optimized archive/CLI/parser smoke passes stock `d7481251-1bc8-4bcf-ac6c-79216a22b52d` (132 texts, 13.485 seconds) and enhanced `1e6264eb-b94a-4190-8c41-62bc4dfd3308` (134 texts, 9.578 seconds), two cases each. [The updated notice record](guides/BAZEL.md#nested-source-notice-validation) distinguishes conservative source-tree collection from comprehensive file-header/legal audit. Public product documentation, help, DocC, installation, compatibility and release claims remain unchanged; this is opt-in package metadata, without runtime or dependency changes.

The embedded BoringSSL notice follow-up now includes both distinct upstream root licence texts, bound by SHA-256 and reviewed containing-package revisions in a declared offline inventory. Changed pins, malformed revision inventories, missing/duplicate inputs, inconsistent provenance and altered bytes fail packaging. Thirteen package-unit cases pass at `509134b0-62b7-4c5d-9e85-1cd5cd903fb4`; real optimized archive/CLI/parser checks pass enhanced `cc09dcef-c7d1-4cd5-bd79-e01361a4110e` (129 texts, 5.419 seconds) and stock `79e8758e-6234-4b29-95df-25acdf5721b2` (127 texts, 8.636 seconds), two archive cases each. [The notice record](guides/BAZEL.md#embedded-boringssl-texts) names exact package/vendor revisions and sources. Both distribution-readiness flags stay false: this closes a known omission, not the complete vendor audit. No production code, dependency pin, public help, README installation instructions, DocC API, compatibility claim or stable release changes; those documentation surfaces were reviewed and remain unchanged. No runtime was started and no quiet benchmark is claimed.

Native `test-cli` now includes six actual CLI/parser contracts in addition to help smoke. Declared Bazel products replace source-fallback compilation inside the command deadline; original foreground dry-run assertions and the 30-second bound are unchanged. The harness adds config/error checks, case-level monotonic JUnit timing and verified same-session descendant cleanup, including the parser's distinct process group. Independent review caught and corrected initial group-only termination; its regression verifies the helper disappears without touching an unrelated session. Named private logs remain available after unverified cleanup. Final-source stock passes at `e8c6c430-4945-4b63-9ab5-3570611f39f2` (3.837 seconds) and enhanced at `fe5e5009-d1da-40eb-b192-0ce13f519106` (9.947 seconds); shell checks and final review are clean. The [CLI contract evidence](guides/BAZEL.md#native-cliparser-contracts) preserves development failures and the separate hosted stock timeout. This is no-runtime integration evidence, not Docker parity, a quiet benchmark, or proof that the legacy hosted failure is fixed. README, public CLI help, DocC, compatibility and stable-release claims remain unchanged; only the opt-in Bazel testing surface changes.

The OpenTelemetry security update aligns `otel`, `metric`, `sdk` and `trace` at 1.45.0 and their required `logr` at 1.4.4, with checksum-database-verified sums and matching Bazel licence overlays/inventory. A regression exercises batch and simple span-processor diagnostic marshalling using a fictional endpoint: both fail with the old matched pins (`48b18657-06c4-49e6-9078-47c5b54aad2f`) and pass with the patched pins. All six native Go race targets pass with 308 XML cases including subtests, alongside seven package-unit cases (`b6b6c2c9-8756-4580-ada6-739850317df1`, 6.351 seconds; unchanged targets reused). Optimized real-archive CLI/parser smoke passes in enhanced (`eaf39c95-b6d7-4c1d-8c55-54340c404180`, 65.401 seconds) and stock (`20822660-93a9-4a3f-86fc-248e6626ff46`, 16.565 seconds), two tests each. Independent source/dependency review is clean. These development timings are not quiet benchmarks. [The security evidence](guides/BAZEL.md#opentelemetry-diagnostic-redaction) records scope and the retained failed invocation. Dependabot alert 28 remains open until the fix reaches the default branch; this draft does not claim a released fix.

The cancellation-diagnostic source passes the full affected stock Core target (`2762b313-1df7-46e0-8311-e3c2b80ceaea`, 1,057 tests / 38 suites, 39.277 seconds build/test) and enhanced Core target (`471cfacf-96c2-4a54-a41d-73233c41d4dc`, 1,247 tests / 40 suites, 70.349 seconds build/test). Strict changed-file Swift lint/format and independent source review are clean. The preceding focused prototype also passes 38 process-runner tests at `368456f7-987c-4d8f-806e-8325050c1ce4`; final-source evidence above supersedes it. The [diagnostic report](guides/BAZEL.md#cancellation-diagnostic-evidence) records raw total observations and the meaning of each phase. This does not reproduce, explain or fix the preserved 2.543-second failure: it adds the evidence needed when that delay recurs, without increasing the bound or changing scheduling. No live container services were used and no quiet benchmark or new coverage percentage is claimed.

The matched dependency follow-up pins Container `000a77c6b6ad8505e5742fae4c518a9a5b3a7dfb` and Containerization `a13f5d347ac0723714d68e6f7802019673d04d97`, both through canonical Stephen GitHub URLs. The public loader still selects detected host byte order; its internal selection boundary now permits both original branches to be tested at every offset for 16/32/64-bit scalars. Existing public-path assertions remain. The full affected runtime target passes all 33 tests in seven suites under native TSan (`88505381-2f6c-4a8d-b73b-6777006a4824`, 51.066 seconds build/test, 3.4 seconds target elapsed). Source consistency and independent complete-diff review pass; stock pins are unchanged. This is unit branch simulation, not big-endian hardware, quiet benchmark, full parity or overall-coverage proof. Fresh lower-fork coverage/quality checks remain required. The temporary local Git URL used only for pre-publication proof is removed.

The shared-tooling lock adopts devcontainer `0a20370`'s guest-image test correction: invalid archive creation is outside the asserted validation failure, preventing fixture setup from satisfying that assertion. The 70 release-input tests pass under native Bazel (`09a22878-9e22-4b80-9c78-d044185b864b`); exact helper-byte/mode admission and independent lock review pass. No Compose product, pin, assertion or gate changes. At the preceding `ac6ef809` checkpoint, GitHub ASan and enhanced runtime checks pass, while the stock job fails the existing two-second cancellation bound at 2.543 seconds (`35299032455`, job `105458020296`, `ProcessRunnerTests.swift:954`). That failure remains retained and unqualified; this tooling-lock update does not fix it or authorize a retry to erase it. Its cause needs timing-phase evidence before any cancellation implementation change.

Complete stock and enhanced unit aggregates pass at `bc40aa15-8df6-4a96-84d8-9666bedd54be` and `3b3d3252-3ddd-4de3-b82f-d2aced9f1622`; the enhanced repeat reuses all eleven test results. All six Go targets report 305 XML cases, including subtests. The four native product binaries build at `180a2464-4721-4f59-ace7-4d6c3ff368d1`. Instrumented enhanced tests pass and retain combined LCOV at `c43d7fac-4949-4b67-9808-525fe08146cf`, without yet claiming consumer report validation or a coverage gate. Independent source review is clean. The preceding GitHub source check identified a missing full licence header in the opt-in Makefile; the header is corrected. Prior failures remain retained. See [BAZEL.md](guides/BAZEL.md) for scope, timing observations and remaining exact-head validation.

Native package metadata (seven cases) and archive/version/bundled-parser smoke tests (two cases) pass in stock at `dbddbd92-5e1c-42fc-a052-35268afb5b5b` and enhanced at `18aea4b3-0dbe-474a-b534-03cc997da6b9`. A dropped wrapper argument was caught by inspecting actual XML and corrected; earlier metadata-only runs are explicitly not archive proof. The enhanced archive is retained and restores twice without compilation with the same SHA-256. Debug packaging is rejected (`e88475f7-6666-46d0-8922-7cf07c474e8e`). Release-note tests pass all 28 cases under Bazel (`ffd87e8b-cedf-4295-b513-89acedfcf9de`). Shell, licence-header and changed Markdown checks pass; independent final packaging review is clean.

## Compatibility, parity and remaining risks

Native CLI command-to-plan coverage expands from twelve to 31 independently timed cases, with unchanged runtime traps, sanitized environment, SSD profiles, 30-second command deadline and verified session cleanup. Nineteen additions assert guest option boundaries, quoted values, replicas, dependency selection, lifecycle ordering/timeouts, log/copy/port arguments and build context/overrides. All original cases remain. Policy minima increase in both profiles. The final stock focused run passes at `bab9bcdf-478d-477c-8b7c-d4a3bdf7864b`; [the guide](guides/BAZEL.md#command-to-plan-coverage) preserves prototype failures and the correction of an unsupported image-de-duplication assumption. This changes the test harness only, not production behavior or Docker compatibility claims. Public CLI help and DocC API behavior remain unchanged; README and testing docs describe the expanded scope.

External config/secret coverage now separates three provider-neutral orchestration methods from four enhanced concrete-store methods without changing any original test body. Both profiles gain unavailable-config/secret failure-before-create and config dry-run no-read/no-file assertions. The stock focused scope passes five methods, including two unavailable-store variants, at `7b6aee60-33b7-484e-9954-745576c12b3d`; independent review and strict changed-test formatting/lint pass. Bazel and Package.swift keep the same five enhanced-only files, now naming the concrete store file. Discovery floors become 1,099 stock / 1,252 enhanced Core methods and require executed materialization source. This changes test availability, not stock runtime capabilities, public CLI/DocC behavior or installed releases.

The coverage follow-up declares the CLI binary for LLVM export and preserves a unique SSD-only profile destination through each sanitized child invocation. The twelve-case component suite requires a nonempty profile from every native CLI invocation; two cases test Python process cleanup without launching Compose. Separate `coverage_stock` / `coverage_enhanced` aggregates add those components to the unchanged unit inventories. Schema-2 policy and the shared launcher bind `unit-cli` through validation, receipt and the 90% gate; unit-only reports remain available and cannot substitute. The guide records the preceding 88.6526% stock / 88.2481% enhanced unit-only checkpoint and collector red/green evidence. No production exclusions, thresholds, runtime support claims or installed releases change.

The stock Core migration passes at `67a505d8-1e99-4a1a-b28b-9c9f814821f7`; its eleven-target coverage aggregate at `3528175e-6dfd-4ac5-9c06-4f7d1fb5e6ba` reports 13,134/29,971 lines (43.8224%). Enhanced Core retains all 1,246 passing cases at `cd552bc4-05dd-457f-b62d-2f63ab39336e`. Independent review found no actionable issue; SwiftLint, SwiftFormat and whitespace checks pass for the changed neutral helper/manifest.

The expanded stock aggregate passes at `3b4bc918-7943-453c-8a46-ef3c885b8b95` with 1,056 Core cases and 25,780/29,971 covered lines (86.0165%). All eleven cached results are reused after correcting a provisional inventory count to the observed XML count; the prior policy-validation failure remains retained. See the guide for exact development timings and intermediate evidence. Four native ContainerizationError cases remain enhanced-only because their error-protocol conformance lives in the enhanced adapter; the concrete archive-extraction assertion also remains enhanced-only. This does not claim equivalent stock-provider integration coverage.

Enhanced ASan passes all eleven targets (`45a1052a-7e6b-4f8e-b271-009930f7fb2e`); Go race detection executes and passes all six targets (`2101ab46-46f5-4722-814a-cd6cd2b8af33`). The initial full TSan failure is preserved. A deterministic ordinary alignment regression reproduces its separate EXT4 dependency crash (`421c2b24-261b-4133-b2a6-5d4f87b5bc2e`), and the enhanced candidate now pins the generic fix from [Containerization PR 103](https://github.com/stephenlclarke/containerization/pull/103) at `ea5ff3b97bd1a12c4054f262ce9fbb7207e6f443`. All 32 affected runtime-unit tests pass under TSan (`7040f032-1f97-4660-8ec0-237c8e701dd8`), followed by all eleven enhanced targets (`87d7bbd6-8aa2-4c20-8920-9aeb5230965b`). Independent sanitizer and dependency-fix reviews found no actionable issue. No stock Apple pin changes.

No installed binary, released asset, runtime service or existing release workflow changes. The enhanced source dependency pin changes only for the generic EXT4 fix; lower-PR review/merge and coordinated matched-stack admission are still required before any release. Temporary-file defaults now honor absolute TMPDIR; fresh staging is exclusive and atomic publication stays within the destination directory. This is an unfinished opt-in migration: measured unit coverage remains below 90%, and 156 enhanced-provider-coupled Core methods still need equivalent stock proof. Full sanitizer qualification, complete dependency licence closure, signed candidate admission, complete downloaded-release integration/parity, quiet benchmarks, quality gates and publication recovery remain open. No stable build or full-parity claim is made. Source promotion and eventual release remain PR-only under the existing exact-head review policy.

[Container PR 286](https://github.com/stephenlclarke/container/pull/286), source `1b68690e99e86c8dc4425ff7fe4dd8270308a068`, now aligns the transitive exact dependency as well. The preceding GitHub source/runtime/ASan jobs rejected mixed `51bf8a`/`ea5ff3b` requirements; this was a real cross-manifest failure, not a test waiver. Local stack consistency passes with the exact Container checkout and all eleven matched enhanced TSan targets pass at `b0d45ce7-10a2-416b-bd95-b87de5657115`. Independent matched-pin review is clean. The Container pin/Compose manifest/lock/stack refs move together; remote checks and coordinated lower-PR merge remain pending.
