# feat(build): migrate Compose to the shared native Bazel workflow

Pull request: [708](https://github.com/stephenlclarke/container-compose/pull/708).

## Motivation

Implement the operator-approved [recoverable Bazel build request](ISSUE-build-bazel-workflow.md) without another coordinator or reference rebuilds.

## Implementation

- Native Swift product targets preserve both Package.resolved profiles and Swift package-access boundaries.
- The executable links the same ComposePlugin module intended for CLI tests; no SwiftPM build subprocess is involved.
- The family launcher and evidence/cache/storage implementation are reused from devcontainer PR [83](https://github.com/stephenlclarke/devcontainer/pull/83), with every shared executable helper bound by the consumer's byte/mode lock.
- The separate opt-in Makefile avoids legacy Makefile parsing, signing discovery or runtime lifecycle operations during native graph qualification.
- Native Go helper and Linux ARM64/AMD64 initializer products use the existing Go version and dependency pins. Both unit profiles include the six Go test targets, with nonempty case-level XML.
- Swift fixtures and resource bundles are declared; CLI/parser tests use native products. Scratch selection and same-directory atomic publication keep temporary work on the SSD without changing persistent state locations.
- A narrowly isolated local-only permission-bit test retains its assertions outside Darwin's sandbox, which strips those bits from the source fixture. All other migrated tests remain sandboxed.
- Declarative consumer inventories drive the shared test/coverage validator. Retained coverage export requires authenticated clean source evidence; the separate 90% gate binds the current consumer SHA, runtime profile and policy before checking raw line counts. No tests or builds are repeated for report export.
- Native optimized candidate packaging uses pinned rules_pkg and the existing plugin layout, with declared resources, version/dependency/capability metadata, four product hashes and an exact archive receipt. Real archive smoke tests check paths, modes, hashes, packaged version and local bundled-parser behavior. The retained archive can be restored without a build. Distribution and licence-closure readiness remain explicitly false.
- Release-note fixtures disable background Git maintenance locally before committing, preventing the observed CI temporary-repository cleanup race; their focused suite now runs under Bazel as well.
- Native dependency notices reuse fetched Go/Swift/SDK inputs with metadata-only overlays. Exact Go module/version/text inventory validation, deterministic text hashing and archive assertions prevent silently incomplete Go notice bundles; all Swift root notice variants are retained. No product pins change. Complete nested-vendor/legal closure remains explicitly unclaimed.
- Stock now executes 1,056 runtime-neutral Core cases rather than omitting the entire suite. Shared project/data/orchestration fixtures no longer require enhanced provider imports; concrete clients/models live in separate enhanced-only support files. Both build manifests retain an identical five-file provider-coupled inventory and explicit profile guards in mixed suites, while enhanced retains all 1,246 Core cases. The stock evidence policy requires the additional target and executed normalizer/process/up/run/API-socket production probes.
- Native address/thread sanitizer targets instrument the Swift/C graph, while a separate Go race target selects rules_go's race mode. A narrow declared rules_go context patch prevents incompatible Clang sanitizer runtimes from being injected through CGo. No Swift/C instrumentation, assertion or test is suppressed.

## Validation

Complete stock and enhanced unit aggregates pass at `bc40aa15-8df6-4a96-84d8-9666bedd54be` and `3b3d3252-3ddd-4de3-b82f-d2aced9f1622`; the enhanced repeat reuses all eleven test results. All six Go targets report 305 XML cases, including subtests. The four native product binaries build at `180a2464-4721-4f59-ace7-4d6c3ff368d1`. Instrumented enhanced tests pass and retain combined LCOV at `c43d7fac-4949-4b67-9808-525fe08146cf`, without yet claiming consumer report validation or a coverage gate. Independent source review is clean. The preceding GitHub source check identified a missing full licence header in the opt-in Makefile; the header is corrected. Prior failures remain retained. See [BAZEL.md](guides/BAZEL.md) for scope, timing observations and remaining exact-head validation.

Native package metadata (seven cases) and archive/version/bundled-parser smoke tests (two cases) pass in stock at `dbddbd92-5e1c-42fc-a052-35268afb5b5b` and enhanced at `18aea4b3-0dbe-474a-b534-03cc997da6b9`. A dropped wrapper argument was caught by inspecting actual XML and corrected; earlier metadata-only runs are explicitly not archive proof. The enhanced archive is retained and restores twice without compilation with the same SHA-256. Debug packaging is rejected (`e88475f7-6666-46d0-8922-7cf07c474e8e`). Release-note tests pass all 28 cases under Bazel (`ffd87e8b-cedf-4295-b513-89acedfcf9de`). Shell, licence-header and changed Markdown checks pass; independent final packaging review is clean.

## Compatibility, parity and remaining risks

The stock Core migration passes at `67a505d8-1e99-4a1a-b28b-9c9f814821f7`; its eleven-target coverage aggregate at `3528175e-6dfd-4ac5-9c06-4f7d1fb5e6ba` reports 13,134/29,971 lines (43.8224%). Enhanced Core retains all 1,246 passing cases at `cd552bc4-05dd-457f-b62d-2f63ab39336e`. Independent review found no actionable issue; SwiftLint, SwiftFormat and whitespace checks pass for the changed neutral helper/manifest.

The expanded stock aggregate passes at `3b4bc918-7943-453c-8a46-ef3c885b8b95` with 1,056 Core cases and 25,780/29,971 covered lines (86.0165%). All eleven cached results are reused after correcting a provisional inventory count to the observed XML count; the prior policy-validation failure remains retained. See the guide for exact development timings and intermediate evidence. Four native ContainerizationError cases remain enhanced-only because their error-protocol conformance lives in the enhanced adapter; the concrete archive-extraction assertion also remains enhanced-only. This does not claim equivalent stock-provider integration coverage.

Enhanced ASan passes all eleven targets (`45a1052a-7e6b-4f8e-b271-009930f7fb2e`); Go race detection executes and passes all six targets (`2101ab46-46f5-4722-814a-cd6cd2b8af33`). The initial full TSan failure is preserved. A deterministic ordinary alignment regression reproduces its separate EXT4 dependency crash (`421c2b24-261b-4133-b2a6-5d4f87b5bc2e`), and the enhanced candidate now pins the generic fix from [Containerization PR 103](https://github.com/stephenlclarke/containerization/pull/103) at `ea5ff3b97bd1a12c4054f262ce9fbb7207e6f443`. All 32 affected runtime-unit tests pass under TSan (`7040f032-1f97-4660-8ec0-237c8e701dd8`), followed by all eleven enhanced targets (`87d7bbd6-8aa2-4c20-8920-9aeb5230965b`). Independent sanitizer and dependency-fix reviews found no actionable issue. No stock Apple pin changes.

No installed binary, released asset, runtime service or existing release workflow changes. The enhanced source dependency pin changes only for the generic EXT4 fix; lower-PR review/merge and coordinated matched-stack admission are still required before any release. Temporary-file defaults now honor absolute TMPDIR; fresh staging is exclusive and atomic publication stays within the destination directory. This is an unfinished opt-in migration: measured unit coverage remains below 90%, and 190 enhanced-provider-coupled Core cases still need equivalent stock proof. Full sanitizer qualification, complete dependency licence closure, signed candidate admission, complete downloaded-release integration/parity, quiet benchmarks, quality gates and publication recovery remain open. No stable build or full-parity claim is made. Source promotion and eventual release remain PR-only under the existing exact-head review policy.

[Container PR 286](https://github.com/stephenlclarke/container/pull/286), source `1b68690e99e86c8dc4425ff7fe4dd8270308a068`, now aligns the transitive exact dependency as well. The preceding GitHub source/runtime/ASan jobs rejected mixed `51bf8a`/`ea5ff3b` requirements; this was a real cross-manifest failure, not a test waiver. Local stack consistency passes with the exact Container checkout and all eleven matched enhanced TSan targets pass at `b0d45ce7-10a2-416b-bd95-b87de5657115`. Independent matched-pin review is clean. The Container pin/Compose manifest/lock/stack refs move together; remote checks and coordinated lower-PR merge remain pending.
