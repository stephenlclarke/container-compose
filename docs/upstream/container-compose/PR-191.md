# Pull Request

## Summary

- Restore the active Compose source tree to the published Container launch, attachment, logging-request, dry-run, and runtime-capability contracts.
- Pin `Package.swift`, `Package.resolved`, and release stack metadata to remotely fetchable Container `6c3f7d3701cf9400855849fa0e29dd75d7b9c45d`.
- Retain the complete advanced logging implementation at signed Compose checkpoint `d6361aab21f61c5771eaefef0c5d436ddf3b925b` and its matching signed local Container checkpoints.
- Keep the deterministic Docker logging oracle, current implementation evidence, and remaining-gap documentation on public `main`.
- Remove `logging-drivers.v1` from the public required capability set until the matching Container dependency is remotely reproducible.

Closes the tracking record in [ISSUE-191.md](ISSUE-191.md). This repair is landing directly on `main` at the user's request, so no GitHub pull request is being created; this file retains the equivalent review and validation handoff.

## Type of Change

- [x] Bug fix
- [ ] New feature
- [ ] Breaking change
- [x] Documentation update

## Motivation and Context

GitHub-hosted CI cannot fetch an Apple-shaped Container commit intentionally retained only on the programme MacBook Pro. The broken push did not invalidate the logging implementation; it exposed an ordering constraint between local multi-repository development and public dependency publication. The repair keeps every signed implementation checkpoint available while making the active public tree independently buildable again.

## Design choices and compatibility

- Use the existing published Container checkpoint rather than publishing incomplete Apple-shaped work early.
- Restore the public Compose execution surface through focused signed inverse commits, preserving the original implementation commits in ancestry.
- Keep oracle fixtures and status evidence because they are dependency-neutral and describe real work completed on this Mac.
- Fail closed by omitting the advanced logging capability from the public manifest. A public build cannot negotiate a contract absent from its pinned runtime.
- Document the active-tree/checkpoint distinction wherever an implementation claim could otherwise be mistaken for a released support claim.

## Testing

- [x] Tested locally
- [x] Added/updated tests
- [x] Added/updated docs

```text
swift package resolve
swift test --filter DockerLoggingDriverOracleFixtureTests
make lint
make check
git diff --check
```

The focused oracle executes 14 tests and passes. The release-tool lane executes 196 tests, the CI-tool lane executes 128 tests, and the coverage-tool lane executes five tests. Signed public repair head `38e0a7ee3c88b9eb6137d4398d2322c93a77b05d` passed hosted CI, Quality, Documentation, and prebuilt-binary workflows; issue #191 carries the exact run links and is closed. Current public baseline `1f0f944b3d918a39ad97d1f12bb7b7c5ef6146a0` also has green CI and prebuilt-binary workflows.

## Documentation inventory

| Surface | Disposition | Rationale |
| --- | --- | --- |
| `STATUS.md` | Updated | Separates local signed logging implementation evidence from the active remotely reproducible public baseline. |
| Logging design | Updated | Records public and preserved revisions and removes links that would be absent from the active tree. |
| Runtime capability contract | Updated | States that `logging-drivers.v1` is deliberately absent from the public required manifest. |
| Docker logging oracle | Retained | The versioned Engine and Compose evidence is dependency-neutral and remains part of the parity programme. |
| Build/install and CLI help | Reviewed unchanged | The repair restores the previously published behavior and adds no new command or installation surface. |
| Upstream Apple handoffs | Reviewed unchanged | No Apple repository is pushed or otherwise modified. |

## Remaining risks

Advanced logging remains unavailable in the active public build until the matched Container authority can be published as part of the complete parity programme. The signed implementation remains preserved, but reactivation must be done as a coherent multi-repository release with hosted dependency resolution and the outstanding provider, migration, security, and paired-performance evidence.

The implementation is now reactivated only on this MBP at signed local `main` commit `784aacaa26f2ad698460044a573adae30665628a`. That local checkpoint is deliberately not pushed because its Container manifest revision is still unreachable from hosted SwiftPM resolution.

## container-compose Checks

- [x] I updated `STATUS.md` and `docs/upstream/` for the runtime capability and release-integrity change.
- [x] This repair is focused on issue #191.
- [x] Runtime, release, and cross-repository decisions have review notes and local validation here.
- [x] Every repair commit uses a Conventional Commit message.
- [x] `Release-Note: none` records that the change restores the existing public baseline.
- [x] No Apple upstream write or submission is involved.
- [x] Every repair commit uses a GitHub-supported signature method.
- [x] No credentials, tokens, private keys, personal data, or private registry details are included.

Release-Note: none
