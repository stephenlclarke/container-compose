# Pull request 421: pin release Xcode and run DocC last

## Summary

Pull request [`#421`](https://github.com/stephenlclarke/container-compose/pull/421) closes tracking issues [`#420`](https://github.com/stephenlclarke/container-compose/issues/420) and [`#422`](https://github.com/stephenlclarke/container-compose/issues/422).

- Pin the stable gate to `/Applications/Xcode.app/Contents/Developer`.
- Preserve the configured developer directory through the clean Nextflow environment.
- Authenticate the DocC executable during release preflight.
- Record DocC's resolved path and content digest before host readiness, then hold execution behind the complete tool-preflight set.
- Split sibling documentation into dedicated stages that are gated on all functional release receipts.
- Reject release execution when a documentation selector omits any functional validation stage.
- Run the independent release-tool and CI-tool suites on parallel hosted runners while preserving one aggregate validation gate.

## Validation

- [x] Focused recoverable release-stage policy regressions
- [x] Existing stable-gate release-policy regression
- [x] Nextflow lint
- [x] Selected release-hosted plan
- [x] Selected full-Xcode documentation preflight
- [x] Workflow YAML parse
- [x] Licence checks
- [x] `git diff --check`
- [x] Signed Conventional Commit
- [x] Handoff registry validation
- [ ] Exact-head CI
- [ ] Exact-head review
- [ ] Resumed stable release gate

## Compatibility and risk

The functional validation commands still cover the same checks, builds, debug symbols, examples, and coverage. Only the two DocC invocations move to a final serialized lane. Full Xcode is now explicit instead of depending on mutable host-wide `xcode-select` state. Focused preflight and plan actions can still select a documentation stage in isolation; only an executing release must include the complete validation dependency set.

Implementation commits: `7b4e0056`, `33e8cf5b`.
