# Pull request 417: preserve Git history for history-sensitive release stages

## Summary

Pull request [`#417`](https://github.com/stephenlclarke/container-compose/pull/417) closes tracking issue [`#416`](https://github.com/stephenlclarke/container-compose/issues/416).

- Preserve the exact reachable Git history for full-repository stages that request commit identity.
- Opt builder, Containerization, and Container hosted release validation into verified Git-bundle reconstruction.
- Retain deterministic tree archives for partial-source stages.
- Terminate the graph on the first failed repository stage instead of waiting indefinitely after all task barriers pass.
- Add a focused regression for the source-format contract.

## Validation

- [x] Focused release-stage Git-history policy regression
- [x] Focused fail-fast strategy regression
- [x] Python compilation
- [x] `git diff --check`
- [x] Signed Conventional Commit
- [x] Handoff registry validation
- [ ] Exact-head review
- [ ] Resumed stable release gate

## Compatibility and risk

Runtime behavior, release artifacts, and repository validation commands are unchanged. Full-repository validation payloads are larger because they retain reachable history, but only the three history-sensitive hosted release stages opt into that cost. Verified bundle creation, `git fsck`, exact-commit checkout, and content-addressed Nextflow caching remain in force.

Implementation commit: `7673294e8979d7cb3554ba3c3bcd3787c6cbc259`.
