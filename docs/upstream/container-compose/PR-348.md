# Pull request 348: pin the final 0.14.0 Container runtime

## Summary

- Advance Container from `6a094cd6acb5` through the reviewed pull request 179
  prewarm fix to the current Apple-aligned pull request 181 merge
  `a14c55a66bcd`.
- Consume the dedicated-prewarm block-mount fix found by the strict 0.14.0
  lifecycle-hook parity gate.
- Refresh the SwiftPM origin hash, Container revision, and checked-in stack
  manifest while preserving every unrelated dependency pin.
- Register this active release handoff in the checked-in handoff registry.

Tracking pull request:
[`stephenlclarke/container-compose#348`](https://github.com/stephenlclarke/container-compose/pull/348).
Release issue:
[`stephenlclarke/container-compose#332`](https://github.com/stephenlclarke/container-compose/issues/332).
Lower-runtime issue and fix:
[`stephenlclarke/container#178`](https://github.com/stephenlclarke/container/issues/178)
and
[`stephenlclarke/container#179`](https://github.com/stephenlclarke/container/pull/179).
Final upstream synchronization:
[`stephenlclarke/container#180`](https://github.com/stephenlclarke/container/issues/180)
and
[`stephenlclarke/container#181`](https://github.com/stephenlclarke/container/pull/181).

## Functional reason

Container background prewarming reserved a created service container's
block-backed volume before a Compose `pre_start` helper could attach it.
Container pull request 179 makes block-backed configurations ineligible for
speculative prewarming while retaining the optimization for block-free and
VirtioFS-only workloads. Container pull request 181 then merges Apple's richer
system-status output while retaining the fork's Engine and builder-shim
provenance, bounds the new image-service probe, and preserves independently
successful resource counts.

## Evidence

- Package resolution succeeds with automatic updates disabled.
- The release Compose product builds against Container `a14c55a66bcd` and its
  embedded identity reports the exact Container and Containerization revisions.
- Strict lifecycle-hook parity passes with the signed fixed runtime candidate,
  including inherited volumes, scale, restart, stop/start, hook failure gating,
  and one-off run hooks.
- No unrelated SwiftPM pin or stack-manifest changes.
- The handoff registry renders and validates with pull request 348 active.

## Release boundary

This pull request changes only the lower runtime authority consumed by Compose.
Release issue 332 retains ownership of the remaining exact-head validation,
packaging, Homebrew, quality, publication, and final DocC gates.
