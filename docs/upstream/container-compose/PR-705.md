# Pull request 705: pin the Container debug-symbol packaging repair

## Change

This pull request advances only Compose's exact Container dependency authority
to the reviewed `main` revision produced by Container pull request
[#284](https://github.com/stephenlclarke/container/pull/284). The SwiftPM
declaration, resolved graph, and stable stack manifest remain one coherent
revision: `353c7b3784fa790c413c5b5ef02431fbdb817690`. Containerization and
builder-shim pins are unchanged.

## Release boundary

The pin is a prerequisite rather than a substitute for stable promotion. The
merged Compose revision must pass exact-main CI and SonarQube analysis and
publish a matched Current Container/Compose archive plus both Current Homebrew
formulae. Only then may the retained 0.15.2 transaction recover, run its local
and hosted release gates, create its signed immutable tag, publish stable
assets, and atomically promote the stable formula pair.

## Validation

- stack manifest, `Package.swift`, and `Package.resolved` consistency
- source, formatting, licence, and dependency-resolution checks
- exact-head automated review and hosted pull-request gates
- exact-main Current package and paired Homebrew verification after merge

Closes
[#704](https://github.com/stephenlclarke/container-compose/issues/704).
Submitted as
[pull request #705](https://github.com/stephenlclarke/container-compose/pull/705).
