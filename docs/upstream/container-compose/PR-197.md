# Pull Request: preserve complete scaled log identifiers

## Summary

- Extract a sourceable `assert_scaled_identifiers` helper from the live reliability harness.
- Capture the complete non-whitespace hostname rather than assuming hexadecimal Docker IDs.
- Add deterministic Docker-style, candidate-style, and duplicate regression cases.
- Include the harness test in the repository lint target.

## Type of Change

- [x] Bug fix
- [ ] New feature
- [ ] Breaking change
- [x] Test update

## Motivation and Context

The previous regular expression truncated valid candidate hostnames and produced a false parity failure. Docker's hexadecimal IDs happened to conceal the defect.

Fixes [#197](https://github.com/stephenlclarke/container-compose/issues/197).

## Testing

- [x] Three deterministic harness tests pass: Docker hexadecimal accepted, candidate hyphenated accepted, duplicates rejected.
- [x] `bash -n` and ShellCheck pass for the live harness.
- [x] The exact Docker Compose 5.3.1/candidate three-replica foreground/history fixture passes with complete unique identifiers.
- [x] Final Markdown, registry, and diff checks pass at the immutable checkpoint.

## container-compose Checks

- [x] The change affects test evidence only and does not alter product hostname semantics.
- [x] The helper is sourceable without starting Docker or Container.
- [x] No credentials, private data, or Apple publication are involved.
- [x] The implementation commits are signed and Conventional Commits compliant.

## Compatibility and Risk

The assertion still requires exactly three unique identifiers and the fixture's expected line shape. It broadens only the identifier alphabet from a Docker-specific hexadecimal subset to the full non-whitespace value actually emitted by the container.
