# Refresh the exact Current Container dependency pin

## Motivation

The Current release controller correctly rejected Compose `003a648fbced` because its Container dependency identity no longer matched the sibling fork's `main` head. Refreshing all maintained pin surfaces together restores one coherent, auditable graph and allows the next exact-main controller run to decide publication from current evidence.

## Implementation

- Advance the enhanced Container revision from `272aeed9c70f79ddf69eb5826afdc96d8be4e3a7` to `e54e1681eb713f6a9bce28afcc6877c67d5ba29a` in `Package.swift` and `Package.resolved`.
- Record the same revision in `Tools/release/stack-refs.json`.
- Regenerate the README fork-divergence block from fetched fork and Apple upstream refs. The snapshot reports Container as 1 commit behind and 877 commits ahead, and the three support forks as 1,284 commits ahead in total.

## Validation

- `swift package resolve`
- `make stack-consistency CONTAINER_STACK_REPO=/Users/sclarke/github`
- `python3 Tools/ci/update-readme-upstream-metrics.py --check`
- `npx --yes markdownlint-cli2 README.md docs/upstream/container-compose/ISSUE-695.md docs/upstream/container-compose/PR-695.md`
- `git diff --check`
- Exact-head GitHub review and protected checks before merge.
- Exact-main runtime, SonarQube, package-controller, release-asset, and paired Homebrew closure after merge.

## Compatibility

Runtime behavior is unchanged because the newly selected Container range contains documentation and workflow-presentation changes only. The enhanced profile continues to use the Stephen-owned Container fork; the stock Apple profile is unchanged.

## Linked work

- Resolves the release-authority mismatch documented in [`ISSUE-695.md`](ISSUE-695.md).
- Follows [Container pull request #279](https://github.com/stephenlclarke/container/pull/279).
- Repairs the failure recorded by [package run 35018887574](https://github.com/stephenlclarke/container-compose/actions/runs/35018887574).

## Remaining risks

The fetched snapshot shows the Container fork one commit behind Apple upstream. That upstream reconciliation is separate work and is reported explicitly rather than folded into a dependency-identity repair. A sibling head that advances again before publication will make the controller fail closed and require another reviewed stack capture.
