# Pull request 382: reserve CodeQL for stable releases

## Summary

Pull request [`#382`](https://github.com/stephenlclarke/container-compose/pull/382) closes tracking issue [`#381`](https://github.com/stephenlclarke/container-compose/issues/381).

- Restrict the package-graph CodeQL job to stable tag publication.
- Permit Current packaging to continue when CodeQL is intentionally skipped.
- Require successful fail-fast preflight in both lanes and successful CodeQL for stable tags.

## Failure boundary

Current packaging cannot bypass failed preflight. Stable packaging cannot bypass either failed preflight or failed/skipped CodeQL. Only the Current branch lane accepts the deliberate CodeQL skip.

## Validation

- [x] Focused release-workflow contract tests
- [x] Actionlint
- [x] Upstream handoff registry check
- [x] `git diff --check`
- [x] Signed Conventional Commit

## Compatibility and risk

Package contents and stable release evidence are unchanged. The change removes a non-release hosted analysis job while retaining the exact-tag security gate and manual published-release recovery path.
