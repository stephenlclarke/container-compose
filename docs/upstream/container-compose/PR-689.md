# Pull request: refresh the Current stack pins

## Summary

- Advance the Container, Containerization, and builder-shim source revisions
  together.
- Pin the immutable builder image digest published for the exact builder-shim
  revision.
- Refresh SwiftPM resolution and the README's generated upstream snapshot.

## Type of change

- [x] Bug fix
- [ ] New feature
- [ ] Breaking change
- [x] Documentation update

## Motivation and context

The `Prebuilt Binaries` workflow failed closed because the checked-in release
manifest no longer matched current protected component branches. The red badge
therefore represented a real publication stop, not a display problem.

## Testing

- [x] Stack consistency and source preflight pass against the exact component
  revisions.
- [x] SwiftPM resolves the exact checked-in runtime revisions.
- [x] Release-tool, CI-tool, workflow, Markdown, and whitespace checks pass.
- [x] Exact-head pull-request checks and review pass.
- [x] The merged-main Prebuilt Binaries workflow failed closed at the newly
  exposed Xcode 27 release diagnostic; no incomplete package was published.

## Docker Compose parity

This change advances already reviewed runtime and quality commits. It does not
change Compose parsing or claim new Docker Compose behavior.

## Compatibility impact

The Current matched stack advances as a unit. Stable 0.15.1 and its immutable
release assets are unchanged.

## Remaining risks

Pull request 689 did not complete publication because it exposed a separate
release-compiler defect in Container. That defect was fixed by Container pull
request 274 and consumed by the follow-up Container Compose pull request 690.
The final matched publication succeeded there.

## Closure evidence

- Pull request 689 merged on 15 September 2026.
- Its exact-head checks and review passed.
- Its merged-main package run stopped safely without publishing stale or
  incomplete assets.
- Pull request [#690](https://github.com/stephenlclarke/container-compose/pull/690)
  completed the correction and final publication.

## container-compose checks

- [x] Added matching issue and pull-request handoff records.
- [x] Kept the release safety comparison intact.
- [x] Preserved Apple repositories as read-only upstreams.
- [x] Used a signed Conventional Commit and Conventional Commit pull-request
  title.
- [x] Declared `Release-Note: none` because the correction changes release
  metadata only.
- [x] Included no credentials, private-key material, passwords, personal data,
  or private registry details.

Closes [#688](https://github.com/stephenlclarke/container-compose/issues/688).
