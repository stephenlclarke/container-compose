# Pull request 334: pin the repaired 0.14.0 runtime graph

## Summary

- Pin Container to signed pull request 174 merge `ba9566840b08`.
- Pin Containerization to signed pull request 60 merge `e5a92e86bf03`.
- Refresh current 0.14.0 runtime metadata, fork classifications, and snapshot
  counts without changing unrelated dependencies or reopening parity scope.

Tracking pull request:
[`stephenlclarke/container-compose#334`](https://github.com/stephenlclarke/container-compose/pull/334).

## Functional reason

The earlier candidate exposed three VZ pod lifecycle failures in the full
release gate. The final graph provides pidfd-backed repeated-stop and NBD
cleanup plus mount-ID-bound `openat2` resolution for hotplugged block root
filesystems. Compose must publish the repaired lower authorities, not the
superseded candidate pins.

## Lower-stack evidence

- Containerization exact head `524035569679` passed clean connector review,
  all review threads were answered and resolved, and it merged as
  `e5a92e86bf03` after 3/3 focused VZ contracts, macOS/Linux builds and units,
  guest initfs build, and CodeQL passed.
- Container exact head `11789d240818` changed only its Containerization pin,
  lockfile origin hash, and release handoff. It passed focused local compile,
  clean connector review, signature checks, and its hosted build/unit gate
  before merging as `ba9566840b08`.

## Release boundary

This pull request records the corrected matched graph and its current release
metadata. Stable tagging, full release-gate execution, immutable package and
Homebrew publication, and the final documentation build remain owned by the
release helper after this exact head is reviewed and merged.
