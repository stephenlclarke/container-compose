# Pull request 447: use the portable Sonar deadline runner

## Purpose

Pull request
[#447](https://github.com/stephenlclarke/container-compose/pull/447)
completes issue
[#446](https://github.com/stephenlclarke/container-compose/issues/446) by
removing the undeclared Homebrew `gtimeout` dependency that stopped managed
macOS CI after all build, test, coverage, and CLI validation had passed.

## Scope

- Run the normal SonarQube scan through the existing repository-owned
  process-group deadline runner.
- Use the same portable boundary for canonical-main restoration.
- Preserve the 1,500-second deadline and 30-second graceful cleanup period.
- Prevent either workflow from reintroducing `gtimeout`.

## Evidence

- Three focused workflow and deadline-runner tests pass.
- Both changed workflow files parse as YAML.
- The handoff registry and changed Markdown pass validation.
- Main CI run
  [33721423678](https://github.com/stephenlclarke/container-compose/actions/runs/33721423678)
  is the retained before-fix evidence: every preceding gate passed and the
  scan failed solely because `gtimeout` was absent.
- Exact-head review is required before merge.

## Related work

- Pull request
  [#444](https://github.com/stephenlclarke/container-compose/pull/444) moved
  non-VM runtime validation to managed macOS after repeated self-hosted runner
  lease loss.
- Pull request
  [#445](https://github.com/stephenlclarke/container-compose/pull/445) aligned
  the release-policy regression with that managed runner contract.
