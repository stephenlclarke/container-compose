# Pull Request 314: prevent repeated macOS privacy prompts

## Summary

- Require a stable Developer ID Application identity for unattended runtime
  candidates.
- Stage packaged runtime executables and marker-protected state on the internal
  volume when their source paths are privacy-controlled.
- Use anonymous access for public GHCR fixtures and reject unsafe staging roots.

## Motivation and context

Repeated Local Network, removable-volume, and public-GHCR Keychain dialogs can
block an unattended matched-stack validation indefinitely. The safe boundary
is to fail before launch when the runtime identity is unstable and avoid paths
or credential lookup that require interactive macOS approval.

Closes [#312](https://github.com/stephenlclarke/container-compose/issues/312).

## Testing

- [x] Four focused staging, signing, localization, and negative security tests
- [x] Bash syntax validation
- [x] Markdownlint and `git diff --check`
- [x] Complete runtime-wrapper component test file: 48 tests in 255.730 seconds
- [x] Hosted-Linux portability correction after the first source-check result;
  the four focused privacy-boundary tests pass on the corrected exact head
- [x] Eight focused tests cover the review security pass: private
  per-user roots, non-destructive rejection, packaged symlinks, localized-root
  symlinks, strict signature verification, explicit registry-policy opt-out,
  portable validation, and serialized candidate replacement
- [x] Thirteen focused exact-head tests cover candidate aliases, complete-package
  generation pinning, canonical privacy-path classification, and the surrounding
  staging and localization boundaries, including bounded signature inspection
  and recovery after an interrupted replacement; required packaged binaries
  reject non-Mach-O artifacts by default
- [x] Six focused latest-review tests cover automatic local-package pinning,
  launchable plugin verification, nested managed-wrapper lock reuse, staging
  serialization, generation mutation, and interrupted replacement recovery
- [x] Focused canonical-path coverage includes exact, symlinked, and
  differently cased privacy-controlled paths
- [x] Upstream handoff registry source and generated reader are current

## Compatibility and risk

Existing runtime behavior is unchanged after preflight succeeds. Explicit
opt-outs remain available only for deliberate host-privacy-policy validation.
The staging directory is persistent by source package identity under a private
per-user root. Its replacement is serialized and can proceed only after
ownership, permissions, marker shape, and process use are validated. Cleanup
revalidates the root and is enabled only after that boundary has passed. Every
complete source package is staged by default, and a nested managed wrapper can
reuse the owner's immutable generation only when its exact root, marker,
fingerprint, signatures, ownership, and permissions match.

## Release note

Prevent unattended runtime validation from blocking on recurring macOS privacy
and public-GHCR credential dialogs.
