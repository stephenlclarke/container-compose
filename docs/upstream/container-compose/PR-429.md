# Pull request 429: publish the 0.14.1 regression-fix stack

## Summary

Pull request [`#429`](https://github.com/stephenlclarke/container-compose/pull/429) references tracking issue [`#383`](https://github.com/stephenlclarke/container-compose/issues/383).

- Retain stopped service containers and their durable log histories after foreground exit-control completes.
- Align the embedded version and release metadata with stable maintenance release 0.14.1.
- Pin the reviewed Containerization and Container runtime integrations that recover interrupted cleanup, preserve immediate exits, and route runtime cleanup through managed clients.
- Keep issue 383 open until the immutable 0.13.1 and 0.14.1 artifacts have been compared and any overlapping stock-Apple handoffs carry isolated evidence.

## Validation

- [x] Nine focused Compose exit-control retention regressions
- [x] Final pinned package graph resolves and compiles
- [x] Stack consistency
- [x] Markdown lint
- [x] `git diff --check`
- [x] Signed Conventional Commits
- [ ] Exact-head CI
- [ ] Exact-head review
- [ ] Stable 0.14.1 package and publication
- [ ] Published-artifact comparison of 0.14.1 with 0.13.1

## Compatibility and risk

The behavioral change is limited to foreground exit-control: Compose no longer removes service containers merely because their processes have stopped, matching Docker Compose retention and preserving post-exit inspection and logs. Explicit removal paths remain unchanged. Runtime dependency changes are pinned to exact reviewed commits, and the maintenance release remains blocked on the normal full release gate.

Implementation commits: `9a3f9c14`, `65eb0f86`, `1d62032a`, `204a0939`, `d8a90f5c`.
