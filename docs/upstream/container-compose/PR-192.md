# Pull Request

## Summary

- Use the non-deprecated `ComposeLogConfiguration.standard` spelling in ordinary logging request coverage.
- Isolate tests that deliberately exercise legacy logging initializers/properties and archive request forwarding inside explicit deprecated contexts.
- Preserve both the production deprecation annotations and the strict warnings-as-errors compiler gate.

Closes the tracking record in [ISSUE-192.md](ISSUE-192.md) and [GitHub issue #192](https://github.com/stephenlclarke/container-compose/issues/192). No GitHub pull request is created while the enclosing logging checkpoint consumes an unpublished matched Container revision; this file retains the equivalent review and validation handoff.

## Type of Change

- [x] Bug fix
- [ ] New feature
- [ ] Breaking change
- [x] Test maintenance

## Motivation and Context

Warnings-as-errors correctly rejected new calls to deprecated compatibility APIs, but the test suite also needs to prove those APIs keep forwarding existing user data. The fix makes that distinction explicit rather than disabling diagnostics or removing compatibility coverage.

## Design choices and compatibility

- Keep `ComposeLogConfiguration.default` and legacy request forms deprecated in production.
- Use the current API in all ordinary tests.
- Scope diagnostic suppression only to the individual tests whose purpose is deprecated compatibility.
- Add no runtime behavior, argument, persistence, or wire change.

## Testing

- [x] Tested locally
- [x] Added/updated tests
- [x] Added/updated handoff documentation

```text
swift build --disable-automatic-resolution --build-tests -Xswiftc -warnings-as-errors
swift test --skip-build --disable-automatic-resolution --no-parallel
go test ./...
make lint core-runtime-neutrality upstream-handoff-registry-check programme-progress-check check-licenses
git diff --check
```

Current MBP evidence:

- warnings-as-errors build passed;
- 1,360 Swift tests in 57 suites passed;
- Go normalizer tests passed;
- lint, runtime-neutrality, handoff/progress, licensing, shell, and Markdown gates passed;
- signed implementation checkpoint: `784aacaa26f2ad698460044a573adae30665628a`.

## Remaining risks

No functional risk remains from this test-only correction. Publication waits for a remotely fetchable matched Container revision because the fix shares the local logging implementation checkpoint; public `main` remains independently reproducible.

## container-compose Checks

- [x] The change is focused on issue #192.
- [x] Production deprecation behavior is unchanged.
- [x] Strict compiler validation remains enabled.
- [x] The implementation commit uses a Conventional Commit message and a GitHub-supported signature method.
- [x] No Apple upstream write or submission is involved.
- [x] No credentials, tokens, private keys, personal data, or private registry details are included.

Release-Note: none
