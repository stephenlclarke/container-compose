# Pull Request

## Summary

- Require a literal `@codex review` and a clean decision for the exact Compose promotion PR head.
- Surface and fail on unanswered, unresolved, new, malformed, truncated, stale, or timed-out review evidence.
- Wait for checks only after clean review and revalidate every invariant immediately before merge.
- Match both normal and checked-admin merge operations to the reviewed full commit.
- Reject the retired direct-main mode and remove its push path.
- Add deterministic review-evidence and release-helper regressions.
- Mark Initial Workflow Enablers 13 and 14 complete and archive PR #181 evidence.

Closes the local tracking record in [ISSUE-182.md](ISSUE-182.md).

## Type of Change

- [x] Bug fix
- [ ] New feature
- [ ] Breaking change
- [x] Documentation update

## Motivation and Context

The stable release helper currently asks GitHub to enable auto-merge immediately after opening its Compose source-promotion pull request, before an exact-head Codex pass. If that attempt is unavailable it waits for checks, but never posts or validates the required review command. A separate legacy mode can push `main` directly. Both paths violate the programme's exact-head review boundary and could carry source into signed tags and packages without the same review authority required of ordinary slices.

## Design choices and compatibility

- Capture the full locally gated commit and require the promotion PR to remain open at that head before posting and after posting the literal `@codex review`.
- Fetch all top-level review-thread pages with GitHub GraphQL. The evaluator rejects an incomplete top-level connection and any nested thread whose first 100 comments are not complete.
- Treat every connector-authored review-thread comment as a query. Each requires a later response by repository owner `stephenlclarke` and a resolved thread. Any query at or after the current request invalidates the request even if subsequently answered; rerunning posts a fresh review.
- Support both clean connector behaviours observed by GitHub: `+1` on the exact request, and a no-major-issues issue comment created after the request whose `Reviewed commit` value is the expected commit prefix. Reject stale or too-short commit identifiers.
- Poll only while the PR head is unchanged and open. Missing, malformed, stale, or timed-out evidence returns nonzero without reaching checks or merge.
- Wait for PR checks after clean review, then immediately repeat exact-head, query, and clean-signal validation. Use `--match-head-commit` for normal merge. If GitHub reports `REVIEW_REQUIRED` and checked-admin mode is selected, repeat checks and review validation before a head-matched admin merge.
- Never request auto-merge. Accept an externally completed merge only after this invocation already obtained clean exact-head authority and passed checks, and only if the merged PR still names the same expected head.
- Keep the promotion mode variable for compatibility with scheduled configuration, but accept only `pr`; reject `direct` explicitly and remove the direct push implementation.
- Make no runtime, Compose semantic, stack-pin, package-content, Docker-oracle, or performance-threshold change. Executable release performance is not applicable because this slice changes only release-control parsing and GitHub orchestration.

## Testing

- [x] Tested locally
- [x] Added/updated tests
- [x] Added/updated docs

```text
python3 -m unittest Tools.release.test_compose_promotion_review Tools.release.test_container_stack_release
bash -n scripts/CONTAINER_STACK_RELEASE.sh
python3 -m py_compile Tools/release/compose_promotion_review.py Tools/release/test_compose_promotion_review.py
make upstream-handoff-registry-update
make upstream-handoff-registry-check
CONTAINER_STACK_REPO=/Users/sclarke/github/worktrees/container-stack-6c3f7d3701cf make check
make coverage
make sonar-scan SONAR_SCAN_ATTEMPTS=1
git diff --check
```

The focused suite currently passes 77 tests, including the deterministic stale-head, unanswered-query, timeout, clean-comment merge, connector-reaction, and direct-mode cases. The evaluator also successfully consumed the real paginated review threads and exact clean issue comment from merged PR #181. The repository-wide exact-stack gate passes 196 release-tool tests, 91 CI-tool tests, and five coverage-tool tests plus runtime neutrality, stack consistency, handoff-registry validation, and Hawkeye against `container@6c3f7d3701cf` / `containerization@77f06d4c4434`. Final exact-head CodeQL, Sonar, hosted-check, and review evidence will be appended after the stable signed PR head exists.

## Remaining risks

The helper depends on the connector's documented reaction or explicit clean-comment shapes and fails closed if GitHub changes them. GitHub's solo-maintainer `REVIEW_REQUIRED` decision still requires checked-admin mode, but that mode cannot bypass the independently verified Codex decision or checks. This slice does not complete the remaining workflow enablers or any runtime parity wave.

## container-compose Checks

- [x] I updated `docs/upstream/` for this release-control change.
- [x] This pull request is focused on one coherent promotion-safety boundary.
- [x] Review notes and local validation are included here.
- [x] The commit and pull-request title use Conventional Commits.
- [x] `Release-Note: none` records that this internal workflow change has no user-visible Compose behaviour.
- [x] No Apple upstream write or submission is involved.
- [x] The branch remains review-only until required applicable checks pass.
- [x] The commit uses a GitHub-supported signature method.
- [x] No credentials, tokens, private keys, personal data, or private registry details are included.

Release-Note: none

@codex review
