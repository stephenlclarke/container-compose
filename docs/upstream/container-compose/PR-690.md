# Pull request: consume the release-compatible Container runtime

## Summary

- Advance the Current Container pin to the protected-main merge of Container
  pull request 274.
- Refresh SwiftPM resolution and the generated README upstream snapshot.
- Preserve the already verified Containerization and builder-image authority.

## Motivation and context

The synchronized stack-pin publication passed its fail-fast checks and then
exposed an Xcode 27 release-compiler diagnostic in the matched Container
runtime. The runtime correction is merged, release-build tested, and reviewed;
Container Compose must consume its immutable protected-main commit before
retrying Current publication.

## Testing

- [x] Stack consistency passes against exact component protected-main commits.
- [x] SwiftPM resolves the exact checked-in Container revision.
- [x] Source, Markdown, and whitespace checks pass.
- [ ] Exact-head pull-request checks and review pass.
- [ ] The merged-main Prebuilt Binaries workflow publishes successfully.

## Compatibility impact

The Container change clarifies weak callback capture ownership for Swift 6.4.
It has no public API or runtime-behaviour impact. Stable 0.15.1 and its
immutable assets remain unchanged.

## Remaining risks

Hosted packaging remains authoritative for signing, notarization, release
asset closure, and atomic Homebrew publication.

## container-compose checks

- [x] Updated the existing issue and handoff registry instead of weakening the
  release gate.
- [x] Preserved Apple repositories as read-only upstreams.
- [x] Used a signed Conventional Commit and Conventional Commit pull-request
  title.
- [x] Declared `Release-Note: none` because this advances release metadata only.
- [x] Included no credentials, private-key material, passwords, personal data,
  or private registry details.

Closes [#688](https://github.com/stephenlclarke/container-compose/issues/688).

Implementation pull request:
[#690](https://github.com/stephenlclarke/container-compose/pull/690).
