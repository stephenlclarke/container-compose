# LOGGING-SYSLOG-UNIX-REST-01

| Field | Record |
| --- | --- |
| State | `Blocked` — the create-time correction and strict Docker fixture are integrated, but the normal package graph cannot execute the current focused test or produce an exact public-socket candidate. |
| Behaviour | A missing `syslog-address` Unix stream (`unix://`) or datagram (`unixgram://`) path is rejected at Docker container-create time, before a container exists or any logger transport can run. |
| Pinned Docker oracle | Same-MBP Docker CLI `29.7.1` / Engine `29.2.1`: `docker create --log-driver syslog --log-opt syslog-address=unix:///definitely-missing/syslog.sock alpine:3.20 true` returns `stat /definitely-missing/syslog.sock: no such file or directory`, exit `1`, and no inspectable container. The `unixgram://` form has the same create-time result. The strict fixture reran both cases at `/private/tmp/container-rest-syslog-unix.reference.4FUUag/result.json` (API `1.53`). |
| Affected repositories and inputs | Container source signed `f4878c7a927c811fe5938d5a6f9027c89e64a818` integrates the correction. Its stable patch ID `aa617b84dca40f1331d1a42aa133dab49572aa50` exactly matches earlier signed correction `52da6f47afe89c58df9e42f960aa52775ae865a4`; Containerization is `38d9c695e7a6915e5ce45d12c893dc323a661af7`; Engine API is `afb8a8f68ed56829b669c95cbddb488a68dc9175`. Compose adds `Tools/parity/check-docker-rest-syslog-unix-create-validation.sh` (SHA-256 `57652260c2c0a0ec27faa2bdd0cd2288cf3ca67fd7a85f896dd94858c961f889`). |
| Implemented change | `SyslogDriverConfiguration.resolve` now checks existence for Unix stream/datagram paths immediately after Docker-compatible address parsing. It maps a missing path to the already-declared `unixSocketDoesNotExist` error and intentionally leaves connection and socket-type failures to transport start, matching the observed Docker `stat` boundary. |
| Focused proof | The prior exact patch's `swift build --target ContainerLoggingProvidersTests` proof covers the changed production file plus existing/missing stream/datagram regressions. The current integration passes strict formatting and `git diff --check`, and its stable patch ID proves no behavioral divergence. A current focused test is deliberately not claimed: the full resolver was stopped before compilation to avoid an ENOSPC failure. |
| Blocker evidence | The package-wide focused-test command deterministically fails before the Syslog test begins when its normal graph lacks `WorkloadNetworkEndpoint` from the pinned Containerization graph. Direct Git fetch can retrieve historical compatible SHAs, but SwiftPM rejects Containerization `38d9c695` because no remote ref advertises it. The current source uses matched local paths for focused development, but a clean full resolution materializes the complete AWS graph and was stopped before disk exhaustion. No local cache is treated as normal-graph or candidate proof. |
| Required change to close | Publish or advertise one signed coherent Containerization/Engine API ref, update Container's declared default revisions and lockfile to it, rebuild the changed Container binary, then run the current focused test and a fresh exact-fingerprint public-socket candidate with `check-docker-rest-syslog-unix-create-validation.sh --host … --strict` for both schemes. It must return Docker-matched rejection with no container residue. |
| Tracking | [Container issue #94](https://github.com/stephenlclarke/container/issues/94) contains the Docker reproduction and implementation checkpoint. It remains open until the focused test and public-socket certificate pass. |
| Safe handoff | Preserve `/private/tmp/container-rest-syslog-unix.reference.4FUUag` (`.container-rest-syslog-unix-root`), source commit `f4878c7a927c811fe5938d5a6f9027c89e64a818`, and Slack START thread `1786274959.034909`. The discarded historical candidate-image root was marker-protected and yielded no behavior result. Do not reuse the fresh Docker root for a candidate. |

Performance is deliberately not a gate here. No candidate timing is retained because the candidate never reached the create boundary; the only blocking condition is the deterministic source-graph compilation failure, not speed.
