# LOGGING-SYSLOG-UDP-REST-01 Handoff

| Section | Record |
| --- | --- |
| State | `Verified` as a narrow Docker CLI/public-socket certificate for cache-disabled Syslog UDP delivery through the Docker host alias. It does not certify every Syslog transport, provider, lifecycle, external client, or release-performance behavior. |
| User-visible contract | An unmodified Docker CLI can create and start a cache-disabled `syslog` driver container with `syslog-address=udp://host.docker.internal:<port>` and `syslog-format=rfc5424micro`. A local UDP receiver sees ordered stdout, stderr, and binary records with Docker-compatible RFC 5424 microsecond framing, `local1` facility/severity, and Docker tag expansion. Inspect, unsupported remote history, native-authority visibility, and cleanup follow the same public socket contract. |
| Docker oracle | The marker-protected Docker reference root `/private/tmp/container-syslog-udp-reference-v3.DzcKyp` records Docker CLI `29.7.1`, Engine `29.2.1`, API `1.53`, `alpine:3.20`, the exact harness, and a `0.662001833s` passing result. |
| Candidate fingerprint | The marker-protected candidate root `/private/tmp/container-syslog-udp-candidate-v3.u86DXn` contains `FINGERPRINT-CANDIDATE-RUN-1-PREFLIGHT.json`, `FINGERPRINT-CANDIDATE-RUN-2-PREFLIGHT.json`, and `FINGERPRINT-COMPLETE.json`; the source/dependency graph, signed archive, binary hashes, guest/init images, public harness, wrapper, two candidate results, and cleanup evidence agree. |
| Focused source proof | The exact local graph passes `NIOSyslogTransportLoopbackTests.productionDockerHostAliasRoutesTCPAndUDPToNativeLoopback` using `CONTAINERIZATION_PACKAGE_PATH=/Users/sclarke/Documents/container/containerization-engine-sandbox`, `CONTAINERIZATION_REF=38d9c695e7a6915e5ce45d12c893dc323a661af7`, `CONTAINER_ENGINE_API_PACKAGE_PATH=/Users/sclarke/github/container-engine-api`, `swift test --skip-build --disable-automatic-resolution`, scratch path `/private/tmp/container-syslog-udp-candidate-v1.rBK0pZ/focused-syslog-build`, and that filter. |
| Source and artifact | Signed Container correction `5ebce8a2aa90bf96775285e1a79ac48b375f69c5` maps the alias case-insensitively to loopback for the native connection target while preserving configured TCP/TLS server identity. Its release archive SHA-256 is `cf1486caded4ab7959308908ab4a9ebe991dde090b15886e2e48a4f581ea7e4f`; both `container` and `container-apiserver` passed `codesign --verify --strict`. |
| Candidate results | Candidate one passed in `2.039981834s` (3.081535023× Docker) and candidate two in `1.529630292s` (2.310613379× Docker). These figures are retained for post-functional optimisation; they do not establish comparable-or-better programme performance. |
| Correction evidence | The pre-fix candidate `/private/tmp/container-syslog-udp-candidate-v1.rBK0pZ` created its object but failed `docker start` with `container logging operation failed`. The Syslog NIO transport did not map `host.docker.internal` at the native macOS connection boundary, unlike the existing GELF transport. The focused TCP/UDP regression and both public candidates prove the correction; the failed v1 and packaging/invocation v2 roots remain retained. |
| Upstream disposition | This is a local Container-fork defect. Apple `origin/main` at `ddaf2ca5ac5629d0555e4a5e04e491de665fd958` lacks both the Syslog transport and its test provider path, so there is no matching Apple upstream bug or Apple-shaped PR handoff. [Container issue #78](https://github.com/stephenlclarke/container/issues/78) records the local defect, evidence, and closure; it does not publish the local source commit. |
| Remaining boundary | Syslog TCP, TLS, Unix sockets, retry/failure/backpressure, the full option matrix, external clients, Testcontainers, devcontainer adoption, release publication, and counterbalanced comparable-or-better release performance remain `Queued`. Do not extend this narrow UDP certificate to those behaviors. |
| Safe resumption | Keep `/private/tmp/container-syslog-udp-reference-v3.DzcKyp`, `/private/tmp/container-syslog-udp-candidate-v1.rBK0pZ`, `/private/tmp/container-syslog-udp-candidate-v2.wk53mN`, and `/private/tmp/container-syslog-udp-candidate-v3.u86DXn` marker-protected; retain the signed source commit, Compose fixture, and ledger. The fingerprint's `Implemented` value reflects its pre-documentation capture; this clean signed Compose checkpoint makes the contract `Verified`. Post the END reply in Slack START thread `1786002918.928579` after the silent instruction poll, then select one independent queued contract. |
