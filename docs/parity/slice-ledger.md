# Parity Slice Ledger

This ledger is the durable, compact evidence record for one active Container-family vertical contract. It is not a capability claim or a second backlog: [STATUS.md](../project/STATUS.md) describes current stable functionality, while [BACKLOG.md](../project/BACKLOG.md) and the linked GitHub hierarchy own planned parity work. A contract becomes `Verified` only after its stated executable acceptance evidence and a clean checkpoint exist; incomplete work is `Implemented`, `Blocked`, or `Handed off`.

## KERNEL-ARCHIVE-FRESH-RUNTIME-01

| Field | Record |
| --- | --- |
| State | `Verified` — lower-runtime bootstrap prerequisite; it does not claim logging-driver parity. |
| Behaviour | A fresh marker-protected runtime root installs the configured kernel, serves an isolated Docker Engine socket to the ordinary Docker CLI, and stops cleanly. |
| Exact inputs | Container `1f6b0057bdd1bfe0594c9b26391db388f54ff341`, Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, Engine API `afb8a8f68ed56829b669c95cbddb488a68dc9175`, package SHA-256 `2f806a9743da434ce41ac5a36564c3f48f3806252e33d801961e10dd853140ec`, and the exact guest/bootstrap/wrapper hashes in [the handoff](handoffs/KERNEL-ARCHIVE-FRESH-RUNTIME-01.md). |
| Focused proof | Fresh roots `/private/tmp/ctr-kernel-v13` and `/private/tmp/ctr-kernel-v14` both installed `vmlinux-6.18.15-186`, returned the isolated Docker socket version, and completed within a bounded 180-second run; v14 records `exit_status=0`. |
| Outcome | The apparent archive failure occurred with only `117M` free. Removing only five marker-protected superseded build/cache roots recovered 25 GB, after which two fresh roots passed. No maintained source changed; timing remains informational and no hang occurred. |
| Safe handoff | The GELF delayed-retry contract is unblocked but remains `Handed off` until its two fresh full fixture candidates succeed. Preserve the evidence root and use new roots for that contract. |

## LOGGING-GELF-WIRE-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker-reference and Container provider-component contract; it is not candidate-runtime or external-client certification. |
| Behaviour | Docker-compatible GELF remote delivery: Docker Engine creates the container, resolves the `gelf` log configuration, starts the receiver connection at the Docker-matched phase, emits stdout, stderr, and binary payload records to UDP and TCP receivers with Docker-compatible framing/compression and metadata, then exits and leaves no receiver or container residue. The matching Container provider component encodes, frames, reconnects, and cleans up through its existing focused suite. |
| Explicit non-goals | Candidate runtime/public-socket/external-client certification, devcontainer adoption, Testcontainers, cloud drivers, logging plugins, release publication, and the broader provider/failure/migration/security/performance matrix remain `Queued` or separately `Blocked`; this contract does not close the overall `logging` gap. |
| Pinned Docker oracle | Same-MBP `colima` context: Docker Engine `29.2.1`, API `1.53`, Docker CLI `29.7.1`, arm64 Mac17,9/macOS 26.5.2, preloaded `alpine:3.20`. This is a direct Engine/CLI contract and deliberately does not invoke the currently installed Docker Compose `5.4.0`; Docker Compose `5.3.1` remains the separate programme oracle. The oracle uses a unique, marker-protected Colima receiver root and direct Engine API capture so create/start phase, inspect state, wire bytes, exit, timing, and cleanup are observable. |
| Affected repositories and inputs | This signed `container-compose` checkpoint adds the Engine oracle and fixture assertions on top of base `1a68ba278ff6416d6e16bc923acb0f1ab9eef7e9`. Matched `container` source is `259878a427de7021b52e40e759d3b261150cc514` on local `upstream/logging-driver-parity`; it supplies `ContainerLoggingProviders` GELF configuration, encoder, session, and transport implementation. No dependency pin moves in this contract. |
| Focused proof | Capture normalized Docker UDP default-gzip and TCP NUL-framed messages through a VM-local receiver; verify create/start phase, selected metadata/tag fields, stdout/stderr/binary payload order, framing/compression, inspect projection, exit, exact receiver cleanup, and fixture comparison. Run the matching `ContainerLoggingProviders` GELF configuration/encoder/session/transport suite against the exact local Container, Containerization, and Engine API graph. |
| Completion criteria | The versioned Docker oracle and maintained focused fixture test pass on the pinned reference; the candidate provider regression passes; no source-level mismatch remains; and the contract has a clean, signed local checkpoint with the evidence location recorded. No Container production implementation changed in this contract, so the 90% new-code target applies to the new oracle/fixture path rather than inventing low-value provider tests. |
| Blocker criteria | The reference is unavailable or drifted; a required Docker behavior cannot be captured safely through the isolated receiver; or two evidence-based correction attempts leave an unexplained candidate/runtime failure. Record exact fingerprints, raw evidence, and the next command, then mark `Blocked` or `Handed off` rather than retrying indefinitely. |
| Safe handoff | Preserve the local signed checkpoint(s), raw reference/candidate evidence under a marker-protected temporary root or versioned fixture, the exact source/dependency fingerprint, and the Slack START thread `1785956977.491379`. Resume with the first unrun focused command from this record. |

### Wire Verification Evidence

- Marker-protected evidence root: `/private/tmp/container-gelf-wire-evidence.tJDxNU` (`.codex-gelf-wire-root`). Its `candidate-build-preflight.json` records Compose base `1a68ba278ff6416d6e16bc923acb0f1ab9eef7e9`, Container `259878a427de7021b52e40e759d3b261150cc514`, Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, Engine API `4949e743675f00ec102f7acacdb4e990409e383f`, the isolated Swift build roots, and the focused-fixture `guestInitImage: not-applicable` boundary.
- `python3 Tools/parity/capture-docker-logging-driver-oracle.py --gelf-wire-only --strict` passed against the same-MBP Docker Engine 29.2.1/API 1.53 reference in 5.682384 seconds (1.03× the captured baseline). The direct Engine trace is retained under `coverage/` in that evidence root.
- `python3 Tools/parity/test_docker_gelf_wire_oracle.py` passed 3 deterministic normalizer tests; `swift test --filter 'DockerLoggingDriverOracleFixtureTests/\`pins GELF UDP gzip and TCP NUL wire contracts\`()'` passed 1 focused fixture test.
- With identity-preserving local package paths, `swift test --disable-automatic-resolution --filter GELF` passed 50 tests in 6 `ContainerLoggingProviders` GELF suites after the matching 66.47-second local build. The first graph used released Engine API 0.3.5 and failed only because its missing `ProviderHandoff*` types are a known release-composition gap; using the recorded matched local Engine API revision resolved that dependency mismatch without source changes.
- A fresh capture initially exposed a false assumption in the new fixture: Docker's valid millisecond-or-coarser timestamp may render with two or three fractional digits. The normalizer now validates the precision ceiling and freezes `timestampPrecision: at-most-milliseconds`, so a timing-format fluctuation cannot create a false semantic difference.

## LOGGING-GELF-CONFIG-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker-reference and Container provider-configuration contract; it is not candidate-runtime or external-client certification. |
| Behaviour | Docker-compatible `gelf` option handling for `gelf-address`, `gelf-compression-type`, `gelf-compression-level`, `gelf-tcp-max-reconnect`, and `gelf-tcp-reconnect-delay`: exact create validation phase, inspect projection, UDP/TCP transport boundaries, accepted numeric/string forms, and no container residue from rejected creation. |
| Explicit non-goals | GELF wire framing already covered by `LOGGING-GELF-WIRE-01`; candidate runtime/public-socket/external-client proof, devcontainer/Testcontainers, cloud/plugin drivers, and broad failure/migration/security/performance matrices remain `Queued` or separately `Blocked`. |
| Pinned Docker oracle | Same-MBP `colima` Docker Engine `29.2.1`/API `1.53`, Docker CLI `29.7.1`, arm64 Mac17,9/macOS 26.5.2, and preloaded `alpine:3.20`. This direct Engine contract intentionally skips the separately pinned Docker Compose 5.3.1 oracle. |
| Affected repositories and inputs | This signed `container-compose` checkpoint adds the direct Engine config fixture and focused assertions on top of signed `a35ebc38978f2b141ce219344a100bf7869def61`. Matched `container` is `259878a427de7021b52e40e759d3b261150cc514`, Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, and Engine API `4949e743675f00ec102f7acacdb4e990409e383f`. No pin moves are authorised. |
| Focused proof | A marker-protected direct Docker Engine fixture captures seven rejected and four accepted UDP/TCP combinations through create, inspect, start, wait, receiver delivery, and cleanup. The matching `GELFConfigurationTests` component regression runs on the exact local graph. |
| Completion criteria | The versioned direct Engine fixture and focused Compose assertion pass; the exact matched provider configuration regression passes; the evidence root records source/dependency/build fingerprints; and this clean signed local checkpoint records the remaining runtime/external-client boundary. |
| Blocker criteria | The Engine reference rejects safe capture, a valid/invalid phase cannot be observed without unsafe external infrastructure, or two evidence-based corrections leave an unexplained mismatch. Record raw evidence and hand off rather than retrying. |
| Safe handoff | Preserve the exact marker-protected test root, source/dependency fingerprint, versioned fixture, and Slack START thread `1785959305.125009`. Resume with a distinct queued contract; do not treat this narrow configuration proof as a runtime or external-client certificate. |

### Configuration Verification Evidence

- Marker-protected evidence root: `/private/tmp/container-gelf-config-evidence.QGPjBO` (`.codex-gelf-config-root`). Its source/dependency/build fingerprint records Compose base `a35ebc38978f2b141ce219344a100bf7869def61`, the focused source digest, Container `259878a427de7021b52e40e759d3b261150cc514`, Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, Engine API `4949e743675f00ec102f7acacdb4e990409e383f`, isolated Swift build roots, and the `guestInitImage: not-applicable` boundary.
- `python3 Tools/parity/capture-docker-logging-driver-oracle.py --gelf-config-only --strict` passed against the same-MBP Docker Engine 29.2.1/API 1.53 reference in 3.370173 seconds (0.99× the captured 3.420637-second baseline); the versioned fixture SHA-256 is `b746f15c32ea3f8661ec466d086a57cbb26b3c443182e9744e6e1fd93fcc52fd`.
- `python3 Tools/parity/test_docker_gelf_wire_oracle.py` passed 5 deterministic wire/config normalizer tests. `swift test --filter 'DockerLoggingDriverOracleFixtureTests/\`pins GELF option validation phases and transport boundaries\`()'` passed its one focused Compose fixture test after a 128.19-second isolated build.
- With the matched local Container, Containerization, and Engine API graph, `swift test --disable-automatic-resolution --filter GELFConfigurationTests` passed 6 configuration tests after a 173.86-second isolated build. No Container source change was necessary because the existing provider already matches the captured option grammar.

## LOGGING-GELF-METADATA-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker-reference and exact provider-component metadata contract; it is not candidate-runtime or external-client certification. |
| Behaviour | Docker-compatible GELF `tag`, `env`, `env-regex`, `labels`, and `labels-regex` semantics: create versus start validation, Go-template expansion and rejection, RE2-compatible selection, selected environment/label projection, metadata precedence, wire extras, inspect projection, and cleanup. |
| Explicit non-goals | `LOGGING-GELF-WIRE-01` and `LOGGING-GELF-CONFIG-01` already cover framing and core transport options. Candidate runtime/public-socket/external-client certification, devcontainer/Testcontainers, cloud/plugin drivers, and the broad failure/migration/security/performance matrix remain `Queued` or separately `Blocked`. |
| Pinned Docker oracle | Same-MBP `colima` Docker Engine `29.2.1`/API `1.53`, Docker CLI `29.7.1`, arm64 Mac17,9/macOS 26.5.2, and preloaded `alpine:3.20`. This direct Engine contract deliberately does not invoke the separately pinned Docker Compose 5.3.1 oracle. |
| Affected repositories and inputs | `container-compose` is `622fd1d6d76d6d954f45244d66bd5891f9887b05`. The tested `container` source is `259878a427de7021b52e40e759d3b261150cc514`; its applied regression is signed `f3662f2f69d7af31e851b69fec07f51b63eb75dc`. Containerization is `38d9c695e7a6915e5ce45d12c893dc323a661af7` and Engine API `4949e743675f00ec102f7acacdb4e990409e383f`. No dependency pin moves occurred. |
| Focused proof | A marker-protected direct Docker Engine receiver fixture captures valid metadata/tag expansion and start-time-invalid tag/selection cases through create, inspect, start, wire delivery, wait, and cleanup. An identity-preserving SwiftPM editable-dependency overlay runs the matching `GELFConfigurationTests` regression against exactly those three local package identities. |
| Completion criteria | Met: the versioned direct Engine fixture matches; the exact matched provider regression passes; source/dependency/build/runtime fingerprints are retained; and both Compose and Container have clean signed local checkpoints. |
| Blocker criteria | The reference cannot be captured safely through an isolated receiver, a phase cannot be observed without unsafe external infrastructure, or two evidence-based corrections leave an unexplained mismatch. The prior clean-root resolver mismatch is resolved by SwiftPM editable dependencies that retain the required package identities. |
| Safe handoff | Preserve the versioned fixture, the two marker-protected evidence roots named below, signed Container `f3662f2f69d7af31e851b69fec07f51b63eb75dc`, and Slack START thread `1785967440.383639`. This verified contract needs no further work; resume on an independent queued contract. |

### Metadata Verification Evidence

- `/private/tmp/container-gelf-metadata-overlay.lDcAcV` (`.container-gelf-metadata-overlay-root`) retains the exact local identity graph and focused-test fingerprint. The disposable worktree and SwiftPM scratch builds were removed after the production checkpoint; the fingerprint records the isolated Container source `259878a427de7021b52e40e759d3b261150cc514`, Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, Engine API `4949e743675f00ec102f7acacdb4e990409e383f`, the two editable SwiftPM dependencies, and the tested additive regression diff.
- `swift test --disable-automatic-resolution --scratch-path /private/tmp/container-gelf-metadata-overlay.lDcAcV/swift-build-editable --filter GELFConfigurationTests` passed 7/7 tests with zero failures after the exact local-identity build. The final source file is byte-identical to the tested worktree copy and is signed in Container `f3662f2f69d7af31e851b69fec07f51b63eb75dc`.
- `/private/tmp/container-gelf-metadata-runtime.9yKJCm` (`.container-gelf-metadata-runtime-root`) records the current Compose source, Docker binary, Engine/containerd/runc/init revisions, guest, image digest, fixture and script hashes. `python3 Tools/parity/capture-docker-logging-driver-oracle.py --gelf-metadata-only --strict` matched the versioned fixture against same-MBP Docker Engine 29.2.1/API 1.53 in 3.225985 seconds (1.05× the captured baseline).
- The earlier resolution routes that selected a remote Container revision or released Engine API 0.3.5 remain historical blocker evidence only. The editable overlay is the non-retrying correction: it retains the real `containerization` and `container-engine-api` package identities while using the recorded local paths.

## LOGGING-GELF-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker CLI/public-socket certificate for Docker-compatible GELF UDP delivery. It is not a certificate for every remote driver, all GELF transports, all Docker routes, or comparable-or-better release performance. |
| Behaviour | An unmodified Docker CLI creates a cache-disabled `gelf` container over the public Container Engine socket, with UDP default-gzip framing, selected environment and label metadata, a rendered tag, and an unqualified `alpine:3.20` image. It observes Docker-matched ordered stdout, stderr, and binary records; priorities and metadata; exited inspect state with blank public `LogPath`; the unreadable remote-log error; native authority visibility; and exact container/receiver/runtime cleanup. |
| Explicit non-goals | This UDP contract does not certify TCP reconnect/failure behavior; one bounded public-socket reconnect path is separately `Verified` in [LOGGING-GELF-TCP-RECONNECT-REST-01](#logging-gelf-tcp-reconnect-rest-01). Every option/error phase, other remote drivers, dual-cache pressure, slow/failing sinks, migration/security/failure matrices, Testcontainers/devcontainer clients, release publication, and the full performance matrix remain `Queued`. [DOCKER-CREATE-ID-REST-01](#docker-create-id-rest-01) now verifies Docker's 64-hex ID shape and aliases separately; this UDP certificate only reuses that behavior. |
| Pinned Docker oracle | Same-MBP `colima`: Docker Engine `29.2.1`/API `1.53`, Docker CLI `29.7.1`, and `alpine@sha256:d9e853e87e55526f6b2917df91a2115c36dd7c696a35be12163d44e6e2a4b6bc`. `Tools/parity/check-docker-rest-gelf-contract.sh --strict --reference --gelf-address-host host.docker.internal` passed in `0.680846750s`; `host.docker.internal` is required only because the Docker reference receiver is outside the Colima VM. |
| Affected repositories and inputs | Compose main `b790d76273062f66508f9de9807b0d9a182707d4` supplies `Tools/parity/check-docker-rest-gelf-contract.sh` at SHA-256 `b008357823cb474973bc911a4044a3730acc04c12d5f215c7bf5589a8baacff2`. Signed local Container `9999d994ed45e06ed2c3144bb175230427116fbc` serializes output-pipe readiness and uses bounded POSIX reads. The exact graph records local Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, local Engine API `4949e743675f00ec102f7acacdb4e990409e383f`, and transitive Containerization `77f06d4c44341e04241941072fb69e2b85a6f5c1`; no published pin moved. |
| Focused proof | The new Authority regression `AuthorityRemoteLogDriverPlaneTests.gelfProductionPlaneForwardsForegroundAndProviderBytes` passes three successive focused runs at the signed Container head. The release archive is built from an explicit SwiftPM scratch root, codesign-verified, and bound with the exact OCI init/bootstrap archives, binary hashes, wrapper, harness, Docker oracle, and marker-protected runtime root before a fresh reference and three fresh public-socket candidate runs. |
| Completion criteria | Met: the Docker reference and all three exact candidate runs pass the same CLI contract; the ordering regression has a focused regression; source/dependency/binary/init-image/root fingerprints are retained; and wrapper cleanup stops the candidate public socket and services. |
| Blocker criteria | Not reached. An earlier candidate repeat established a real cross-stream ordering defect; the first serial-poller correction still allowed Foundation pipe buffering to defer stderr, and the bounded POSIX-read correction produced new focused and three-run public-socket evidence. The first v4 reference invocation used the wrong Colima receiver address and is retained as an invocation correction, not a semantic mismatch. |
| Safe handoff | Retain `/private/tmp/container-gelf-rest-v4.AR1wW6` (`.container-gelf-rest-evidence-root`), `FINGERPRINT-PREFLIGHT.json`, the three result JSON files, the signed Container checkpoint, [the structured handoff](handoffs/LOGGING-GELF-REST-01.md), and Slack START thread `1785980213.576879`. Resume only on an independent queued contract. |

### GELF REST Verification Evidence

- `/private/tmp/container-gelf-rest-v4.AR1wW6/FINGERPRINT-PREFLIGHT.json` ties source, both local dependency paths, the transitive checkout, `Package.resolved`, workspace state, signed archive, every candidate binary, helper/journald assets, retained OCI init image, bootstrap image, harness, wrapper, and Docker oracle to the one marker-protected root.
- The fresh Docker reference passed in `0.680846750s`. The three independent candidate public-socket passes were `1.567608333s`, `1.526945791s`, and `1.461681958s` (2.30×, 2.24×, and 2.15× the same-reference duration). Those raw timings are below the 10× functional regression guard but remain evidence for the separate comparable-or-better performance backlog.
- The production archive SHA-256 is `6d1c3ac2c39c2021a944efd870a1d64e5555828dee39cace502666875f1131ba`; `FINGERPRINT-PREFLIGHT.json` records the individual signed binary and guest-image hashes. `container-candidate.log`, `container-candidate-repeat-01.log`, and `container-candidate-repeat-02.log` retain the controlled runtime start, certificate, and shutdown evidence.
- The historical candidate create response used the generated name where Docker returned a 64-hex ID. That correction is retained as evidence and is now verified separately by [DOCKER-CREATE-ID-REST-01](#docker-create-id-rest-01).

## LOGGING-GELF-TCP-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker CLI/public-socket certificate for Docker-compatible GELF TCP delivery. It does not itself certify the separately verified bounded reconnect path, the remaining retry/failing-sink matrix, every remote driver, all Docker routes, or comparable-or-better release performance. |
| Behaviour | An unmodified Docker CLI creates a cache-disabled `gelf` container over the public Container Engine socket, with uncompressed TCP NUL-delimited GELF JSON records, selected environment and label metadata, a rendered tag, and an unqualified `alpine:3.20` image. It observes Docker-matched ordered stdout, stderr, and binary records; priorities and metadata; primary-peer EOF after exit; exited inspect state with blank public `LogPath`; the unreadable remote-log error; native authority visibility; and exact container/receiver/runtime cleanup. |
| Explicit non-goals | The bounded TCP reconnect path is separately `Verified` in [LOGGING-GELF-TCP-RECONNECT-REST-01](#logging-gelf-tcp-reconnect-rest-01); the complete retry/failing-sink matrix, every option/error phase, other remote drivers, dual-cache pressure, slow/failing sinks, migration/security/failure matrices, Testcontainers/devcontainer clients, release publication, and the full performance matrix remain `Queued`. [DOCKER-CREATE-ID-REST-01](#docker-create-id-rest-01) now verifies Docker's 64-hex ID shape and aliases separately; this TCP certificate only reuses that behavior. |
| Pinned Docker oracle | Same-MBP `colima`: Docker Engine `29.2.1`/API `1.53`, Docker CLI `29.7.1`, and `alpine@sha256:d9e853e87e55526f6b2917df91a2115c36dd7c696a35be12163d44e6e2a4b6bc`. `Tools/parity/check-docker-rest-gelf-contract.sh --strict --reference --transport tcp --gelf-address-host host.docker.internal` passed in `0.931894667s`; `host.docker.internal` is required only because the Docker reference receiver is outside the Colima VM. |
| Affected repositories and inputs | Compose base `414c04c0cb4d47d59e78c483f481270dc869a6fa` supplies the transport-selecting Bash certificate at pre-documentation SHA-256 `bc14f05007a2da1940042153b6bc20d7871a8b0664b5827b7cb4cf30ef7887eb`. Signed local Container `9999d994ed45e06ed2c3144bb175230427116fbc` supplies the existing TCP GELF session/transport. The exact graph records local Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, local Engine API `4949e743675f00ec102f7acacdb4e990409e383f`, and transitive Containerization `77f06d4c44341e04241941072fb69e2b85a6f5c1`; no published pin moved. |
| Focused proof | `bash -n` and ShellCheck pass for the transport-selecting certificate; direct Docker Engine wire/component coverage already proves TCP NUL framing. The code-signed release archive is bound with exact dependency revisions, binary hashes, OCI init/bootstrap archives, wrapper, harness, Docker oracle, and a marker-protected runtime root before one fresh reference and three fresh public-socket candidate runs. The shared UDP branch was rerun once against Docker after the refactor. |
| Completion criteria | Met: the Docker reference and all three exact candidate runs pass the same CLI contract; each run observes NUL framing, ordered records, metadata, lifecycle, unreadable history, native authority, and cleanup; source/dependency/binary/init-image/root fingerprints are retained; and wrapper cleanup removes the candidate public socket and services. |
| Blocker criteria | Not reached. The initial reference fixture closed its TCP listener after the primary EOF, then Docker's later `logs` logger initialization returned connection refused. Retaining the bounded listener through that expected call produced reference and candidate passes; this is a harness-lifecycle correction, not a Container mismatch. |
| Safe handoff | Retain `/private/tmp/container-gelf-tcp-rest-v3.Zd0bfm` (`.container-gelf-tcp-rest-evidence-root`), `/private/tmp/container-gelf-tcp-rest-v2.8GcpKZ/docker-reference-result.json`, `FINGERPRINT-PREFLIGHT.json`, the three result JSON files, [the structured handoff](handoffs/LOGGING-GELF-TCP-REST-01.md), and Slack START thread `1785987039.525399`. Resume only on an independent queued contract. |

### GELF TCP REST Verification Evidence

- `/private/tmp/container-gelf-tcp-rest-v3.Zd0bfm/FINGERPRINT-PREFLIGHT.json` ties source, both local dependency paths, the transitive checkout, `Package.resolved`, the code-signed release archive, every candidate binary, retained OCI init/bootstrap archives, harness, wrapper, Docker oracle, and the marker-protected runtime root to the one test root.
- The fresh Docker reference passed in `0.931894667s`. The three independent candidate public-socket passes were `1.603390667s`, `1.700857709s`, and `1.975864916s` (1.72×, 1.83×, and 2.12× the same-reference duration). The raw timings remain evidence for the separate post-functional comparable-or-better performance backlog.
- After the transport refactor, the default UDP Docker-reference regression also passed in `0.669117458s`; its marker-protected root is `/private/tmp/container-gelf-udp-regression-v1.s4goZJ` and its result SHA-256 is `88f904a9bfd707c8f35627635baf76bc8c6e7aa11d2b8f0574f844d6ec7761e8`.
- The historical candidate create response used a generated name where Docker returned a 64-hex ID. That correction is retained as evidence and is now verified separately by [DOCKER-CREATE-ID-REST-01](#docker-create-id-rest-01).

## LOGGING-GELF-TCP-RECONNECT-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker CLI/public-socket certificate for one bounded TCP GELF peer-loss and reconnect path. It does not certify every retry/failing-sink outcome, every remote driver, all Docker routes, or programme-wide comparable-or-better release performance. |
| Behaviour | An unmodified Docker CLI creates a cache-disabled TCP `gelf` container through the public Container Engine socket with Docker's selected reconnect options (`gelf-tcp-max-reconnect=1`, `gelf-tcp-reconnect-delay=0`). The bounded receiver accepts the first NUL-framed GELF record, closes that peer, then requires a valid second stream containing ordered recovery bytes and `reconnect-complete` without duplication or corruption. The certificate also proves metadata/tag projection, inspect state, unsupported remote history, native authority visibility, and exact container/receiver/runtime cleanup. |
| Explicit non-goals | Sustained failing sinks, the complete retry-budget/delay matrix, slow-sink/backpressure, dual-cache pressure, migration/security/provider-failure matrices, Testcontainers/devcontainer clients, release publication, and the complete release-performance matrix remain `Queued`. The distinct public create name-as-ID discrepancy is verified separately in [DOCKER-CREATE-ID-REST-01](#docker-create-id-rest-01). |
| Pinned Docker oracle | Same-MBP `colima`: Docker Engine `29.2.1`/API `1.53`, Docker CLI `29.7.1`, and preloaded `alpine:3.20`. `/private/tmp/container-gelf-tcp-reconnect-reference-v2.yjav2A/docker-reference-result.json` passed in `5.524841000s` with `host.docker.internal`, a first-peer close, recovery on a second connection, and exact cleanup. |
| Affected repositories and inputs | Compose `ecddcb06a33728e99a1471fbbf2701d892dbbc60` supplies `Tools/parity/check-docker-rest-gelf-contract.sh` at SHA-256 `87c5294fefc890ee5aa651112d5b768504334f7bb7a5acdacd3e844b2acac365`. Signed local Container `2a60d0ad0341ef6947d30289488e3a7c8eac56ed` maps the Docker VM alias to loopback only at the native macOS GELF connection boundary. The exact graph records local Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7` and Engine API `4949e743675f00ec102f7acacdb4e990409e383f`; no published pin moved. |
| Focused proof | `GELFTransportLoopbackTests.productionDockerHostAliasRoutesTCPAndUDPToNativeLoopback` passes against the exact local graph. A fresh code-signed release archive is bound to the source, dependencies, binary hashes, OCI init/bootstrap archives, harness, wrapper, Docker oracle, and marker-protected root before two fresh public-socket runs. |
| Completion criteria | Met: the retained Docker reference and both independent exact candidate runs pass the same forced reconnect contract; the alias correction has a live TCP/UDP component regression; source/dependency/binary/init-image/root fingerprints are retained; and wrapper cleanup removes the candidate socket and services. |
| Blocker criteria | Not reached. The first public candidate exposed a specific macOS placement gap: the native GELF provider attempted to resolve Docker's VM-only `host.docker.internal` directly. Signed Container `2a60d0ad0341ef6947d30289488e3a7c8eac56ed` corrects only that connection target; its focused regression and both fresh public runs pass. |
| Safe handoff | Retain `/private/tmp/container-gelf-tcp-reconnect-candidate-v2.nmMgi1` (`.container-gelf-tcp-reconnect-evidence-root`), its `FINGERPRINT-PREFLIGHT.json`, `ARCHIVE-VERIFICATION.json`, `FINGERPRINT-COMPLETE.json`, two result JSON files and runtime logs, the Docker reference result, signed Container checkpoint, [the structured handoff](handoffs/LOGGING-GELF-TCP-RECONNECT-REST-01.md), and Slack START thread `1785988164.118569`. Resume only on an independent queued contract. |

### GELF TCP Reconnect Verification Evidence

- `/private/tmp/container-gelf-tcp-reconnect-candidate-v2.nmMgi1/FINGERPRINT-COMPLETE.json` cross-links the source/dependency graph, code-signed archive, preflight and archive-verification hashes, guest images, harness, runtime wrapper, Docker reference, and both candidate results.
- The Docker reference passed in `5.524841000s`. Candidate public-socket passes took `6.508964500s` and `6.340717583s` (1.18× and 1.15×). Both complete below the 10× functional regression guard; this one lane is close to Docker but does not close the complete comparable-or-better release-performance requirement.
- The focused native alias regression passed with Containerization `38d9c69` and Engine API `4949e743`; its marker-protected log is `/private/tmp/container-gelf-native-alias-unit-matched.D850uv/swift-test.log`.
- [Container issue 75](https://github.com/stephenlclarke/container/issues/75) tracked the discovered native alias defect and was closed after the final clean source/evidence comment; Apple publication remains explicitly deferred.

## LOGGING-GELF-TCP-FAILURE-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker CLI/public-socket certificate for one initially unavailable TCP GELF endpoint and one zero-budget retry-exhaustion path. It does not certify the complete retry/failing-sink matrix, every remote driver, all Docker routes, or programme-wide comparable-or-better release performance. |
| Behaviour | An unmodified Docker CLI creates a cache-disabled TCP `gelf` container through the public Container Engine socket. For an initially unreachable endpoint, Docker create succeeds while start fails with the configured endpoint and GELF initialization diagnostic; inspect remains `created`, records exit code `128`, keeps zero timestamps, and retains that error across authority restart. For a receiver that resets two peers with zero reconnect budget, current records fail without duplication or corruption while the final replacement connection delivers the ordered recovery record. Both cases also prove metadata/tag projection, native authority, and exact cleanup. |
| Explicit non-goals | The complete TCP retry-budget/delay and sustained-failure matrix, slow-sink/backpressure, dual-cache pressure, migration/security/provider-failure matrices, every remote driver, Testcontainers/devcontainer clients, release publication, and the complete release-performance matrix remain `Queued`. The candidate unavailable path is 6.38–7.00x Docker and is retained for post-functional optimisation; it does not close comparable-or-better performance. The unrelated create name-as-ID gap is now verified separately in [DOCKER-CREATE-ID-REST-01](#docker-create-id-rest-01). |
| Pinned Docker oracle | Same-MBP `colima`: Docker Engine `29.2.1`/API `1.53`, Docker CLI `29.7.1`, and preloaded `alpine:3.20`. `/private/tmp/container-gelf-tcp-failure-reference-v1.CoOhUO/harness-tcp-unavailable-result.json` passed in `0.072112500s`; `/private/tmp/container-gelf-tcp-failure-reference-v1.CoOhUO/harness-tcp-failure-result-4.json` passed in `12.445434875s`. |
| Affected repositories and inputs | Compose is preflight `fcaeea0e23bcb455d07c3d505a577e35ad7e0744` plus working-diff SHA-256 `f72b921debe302fffc3e14068bf8fb991c589991421ac8de3f565cba80e25136`; its harness SHA-256 is `e427774863e921a384b04f809eba92a68a903391342d51b65f827b67c1c7e2fd`. Signed Container `73cea53bbfa3a8711a1e9320c330e4dd7c2870fa` retains the typed GELF diagnostic and signed Container `5e46d527391fa0830cf553c95e4be5019b82d551` projects Docker exit `128`. Local Containerization is `38d9c695e7a6915e5ce45d12c893dc323a661af7`; Engine API is `4949e743675f00ec102f7acacdb4e990409e383f`. No dependency pin moved. |
| Focused proof | `GELFTransportLoopbackTests.productionTCPConnectionFailurePreservesConfiguredEndpoint` and `ContainerLoggingAuthorityIntegrationTests.dockerRejectedStartErrorIsInspectableAfterAuthorityRestart` pass against the exact local graph. A fresh code-signed archive is bound to source, dependencies, binary hashes, OCI init/bootstrap archives, harness, wrapper, Docker oracle, and one marker-protected root before two unavailable-endpoint and two retry-exhaustion public-socket candidate runs. |
| Completion criteria | Met: both retained Docker references and all four independent exact candidate runs pass their respective CLI contracts; the source diagnostic and durable lifecycle regressions pass; source/dependency/binary/init-image/root fingerprints are retained; and wrapper cleanup removes the candidate socket and processes. |
| Blocker criteria | Not reached. The first candidate exposed two concrete gaps: generic GELF connection diagnostics and exit code `0` for a rejected Docker start. The two signed corrections and focused/public evidence resolve both; the self-matching cleanup probe was discarded before recording evidence. |
| Safe handoff | Retain `/private/tmp/container-gelf-tcp-failure-candidate-v6.bF5VQ7` (`.container-gelf-tcp-failure-evidence-root`), its three fingerprint files, four result JSON files and runtime logs, two focused test logs, both Docker reference results, signed source checkpoints, [the structured handoff](handoffs/LOGGING-GELF-TCP-FAILURE-REST-01.md), Apple-shaped source handoff pair, and Slack START thread `1785992707.844709`. Resume only on an independent queued contract. |

### GELF TCP Failure Verification Evidence

- `/private/tmp/container-gelf-tcp-failure-candidate-v6.bF5VQ7/FINGERPRINT-COMPLETE.json` cross-links the source/dependency graph, code-signed archive, preflight and archive-verification hashes, two focused regressions, guest images, harness, runtime wrapper, Docker references, four candidate results, and cleanup proof.
- The unavailable Docker reference passed in `0.072112500s`; candidate passes took `0.505147750s` and `0.460362292s` (7.00x and 6.38x). The retry-exhaustion Docker reference passed in `12.445434875s`; candidates took `13.568986042s` and `13.622602750s` (both 1.09x). All retain raw timings for the post-functional performance phase; comparable-or-better performance remains an explicit programme gap.
- [Container issue 77](https://github.com/stephenlclarke/container/issues/77) tracked the exit-128 state defect and is closed after the final clean source/evidence comment. Apple publication remains explicitly deferred.

## LOGGING-GELF-TCP-RETRY-DELAY-01

| Field | Record |
| --- | --- |
| State | `Blocked` — direct guest-VSOCK Candidate v26 and fresh reverse-host-VSOCK Candidate v27 both reach a running protected GELF workload but fail the Docker `start` before a receiver peer or fixture result. They exit promptly and clean up; this is a functional bootstrap/dial blocker, not performance evidence. |
| Behaviour | An unmodified Docker CLI creates a cache-disabled TCP `gelf` container with `gelf-tcp-max-reconnect=2` and `gelf-tcp-reconnect-delay=1`. A bounded receiver accepts one framed record on each of two peers, resets them, records Docker's delayed retries, then retains a recovery peer and verifies ordered terminal delivery, inspection, unsupported remote history, native authority visibility, and cleanup. Container routes TCP-only outbound frames through a sealed Engine-Linux service while `GELFSession` remains the sole reconnect/replay authority. |
| Explicit non-goals | All retry budgets/failure modes, slow-sink backpressure, dual-cache pressure, migration/security/provider-failure matrices, plugins/cloud drivers, Testcontainers/devcontainer clients, release publication, and the complete comparable-or-better release-performance matrix remain `Queued` or separately `Blocked`. UDP remains native and is not changed by this relay. |
| Pinned Docker oracle | Same-MBP `colima`: Docker Engine `29.2.1`/API `1.53`, Docker CLI `29.7.1`, and preloaded `alpine:3.20`. `/private/tmp/container-rest-gelf.default-host.wG4HQt/result.json` passed in `22.447226208s` (SHA-256 `ecb6fc023506a605c360f4032a59a26138daa0898d3dfd2db3e1c7ac1f363abe`); the receiver records two forced one-frame resets, intervals `10.019107917`/`9.010752833` seconds, terminal recovery, peer close, and no timeout. |
| Affected repositories and inputs | Compose `main` `35a7557ccb16971df365910b8f25ba381bdbeef2` supplies the fixture (SHA-256 `a0ed0178a62be517b42d4a21070ea73a57689513b7a623c3fcdfcdd6efc94fca`). Signed Container `13e6dc922b3275278fd12eb190cd435beda358a6` selected direct guest VSOCK; signed successor `289c0782e3d973d7303d10c5a001673c319992b6` restores reverse host VSOCK. The exact local graph is Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7` and Engine API `afb8a8f68ed56829b669c95cbddb488a68dc9175`; no published pin moved. |
| Focused proof | `EngineLinuxSandboxGELFTCPServiceTests` passes 17/17 against the exact current local graph. V26's direct configuration and V27's reverse configuration each start the protected workload and record it running in the sealed ledger. V27 records `--connect-host-vsock`, PID 145, no published socket, no service stderr, no receiver peer, no result JSON, and the generic Docker start error. Focused coverage for this transport reversal has not been measured; the 90% target remains a required evidence gap, not completion evidence. |
| Completion criteria | Two independent exact-fingerprint public Docker-socket candidates must match the oracle for retry timing/disposition, lifecycle, inspect, authority, and cleanup. Changed code should retain focused evidence approaching the 90% target. Performance architecture and raw durations are retained for the later optimization phase; comparable-or-better median/P95 is not required for this functional certificate unless liveness fails. |
| Blocker criteria | Reached: two evidence-based transport declarations have the same bounded functional failure. Do not repeat a transport-only candidate. First preserve a concrete protected-service bootstrap/dial diagnostic, then make one correction and run one new exact-fingerprint root. A hang, liveness-bound breach, fingerprint mismatch, or cleanup failure remains immediately blocking; comparative performance is not. |
| Safe handoff | Retain the Docker root, `/private/tmp/ctr-gelf-v24`, `/private/tmp/ctr-gelf-v27`, `/private/tmp/container-rest-gelf.v26`, `/private/tmp/container-rest-gelf.v27`, package roots `...bin-05`/`...bin-06`, and `/private/tmp/ctr-gelf-vsock-build-05` (v26/v27 preflights and run records). Resume only from [the structured handoff](handoffs/LOGGING-GELF-TCP-RETRY-DELAY-01.md), with a new marker-protected namespace-aware root that does not stop the user-owned devcontainer engine. |

## LOGGING-SPLUNK-CREATE-VALIDATION-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — signed local Container `bf2d6de19e0924fa3cd08fe20276c987d785c060` and a clean exact-fingerprint candidate reproduce both deferred Splunk required-option diagnostics. The candidate exited `0`; no hang, timeout, fingerprint mismatch, or cleanup failure occurred. |
| Behaviour | Docker accepts creation of a `splunk` container with missing required options, persists it as `created`, then fails `start` with a driver-specific logging-initialization error. A missing `splunk-url` reports `splunk-url is expected`; a missing `splunk-token` reports `splunk-token is expected`. Both errors must remain secret-safe. |
| Pinned Docker oracle | Same-MBP Colima, Docker CLI `29.7.1`, Engine `29.2.1`, API `1.53`, and `alpine:3.20`. Two guarded unique-name probes created a container, inspected `LogConfig`, failed `docker start` with the exact respective diagnostic, observed `created`, and removed the probe. Retained result: `/private/tmp/container-rest-splunk-reference.Uy0c4u/result.json`, SHA-256 `6763ffe39c1daf5e23d4e137e103aab67d795b3a7a15374da0c7092cee335103`. No timing claim is made. |
| Affected repositories and inputs | Container branch `upstream/docker-wait-acknowledgement-01`, signed source `bf2d6de19e0924fa3cd08fe20276c987d785c060`; detached Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`; detached Engine API `afb8a8f68ed56829b669c95cbddb488a68dc9175`; Compose local `main` fixture `ec3c89b093d3894df3cc6b33c6ed04aa2ccd38a1`. Candidate package SHA-256 values: CLI `9e8f9a509cc1eb6defdcbbd590d690ddc16eedcbba3c8f3ff19ccbca75e47c11`, API server `e0fd8247295233357947fbce5fcfbdff8c858397bbc4091cf0fed28691b476fe`, engine `202ba2ab0e7dd910257fec455892f4db38bc37486a804c7e5ef5ab1da91ed89e`. No pin or remote publication moved. |
| Focused proof | Exact detached graph: `swift test --disable-automatic-resolution --filter ContainerLoggingStartErrorTests` passed 7 tests. Fixture `Tools/parity/check-docker-rest-splunk-create-validation.sh` passed `bash -n` and ShellCheck. Final root `/private/tmp/ctr-splunk-create-validation-final.MZvMRd` retained preflight/complete fingerprints and `fixture/result.json`; candidate exit `0` recorded both expected errors, `created` state, token-safe diagnostics, and owned cleanup. |
| Completion criteria | Met: exact focused test and clean public candidate prove both missing-option paths. Coverage instrumentation was attempted but stopped before completion when the package build reduced free disk from about 11 GiB to 6 GiB, so no percentage is claimed. Coverage toward 90% remains a non-functional evidence gap; a hang or timeout remains a functional blocker. |
| Blocker criteria | No active functional blocker. Any later fingerprint mismatch, hang, timeout, cleanup failure, or reference disagreement blocks a successor contract immediately. Performance optimization and comparative timing are deferred. |
| Safe handoff | Preserve the oracle and final candidate roots, signed source checkpoint, fixture checkpoint, and [structured handoff](handoffs/LOGGING-SPLUNK-CREATE-VALIDATION-REST-01.md). [Container issue #92](https://github.com/stephenlclarke/container/issues/92) has local functional completion evidence but must not be externally commented on or closed without authorization. Do not publish. |

## LOGGING-SPLUNK-HEC-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — signed source `e188f9e8`, its exact focused XCTest, a signed Homebrew package, and a fresh isolated runtime candidate all agree with the pinned Docker HEC contract. |
| Behaviour | A valid cache-disabled Splunk logger sends Docker CLI stdout/stderr to a local HEC-compatible receiver with Docker-measured endpoint, authorization, payload/order, inspect, exit, and owned cleanup behavior. |
| Pinned Docker oracle | Same-MBP Colima, Docker CLI `29.7.1`, Engine `29.2.1`, API `1.53`, and `alpine:3.20`. `/private/tmp/docker-splunk-hec-contract-reference.8yzTk1/result.json` passed one POST to `/services/collector/event/1.0`, Splunk Authorization, two decoded source events, exited/0, inspect, and owned cleanup. |
| Affected repositories and inputs | Compose fixture was checked at pre-record checkpoint `e03ba18af6e104cbf236b68467b890676b38ca57`; fixture SHA-256 is `ae4ad90e7fdd74e6bf99867f7b3b684a546740ac1bd63722d074e3dc31fc569c`; signed Container correction is `e188f9e8`; detached Containerization is `38d9c695e7a6915e5ce45d12c893dc323a661af7`; detached Engine API is `afb8a8f68ed56829b669c95cbddb488a68dc9175`. No remote or pin moved. |
| Focused proof | Docker reference passed. First candidate `/private/tmp/ctr-splunk-hec-final.BUj0a5` correctly captured the pre-fix no-request mismatch. Exact filtered XCTest `SplunkURLSessionTransportLoopbackTests.productionHTTPTransportRoutesDockerHostAliasNatively` passed at `/private/tmp/splunk-hec-package-e188-01.djPh1c/focused-xctest.log`. Exact signed package archive SHA-256 is `6674e402f308e1172bcd56b4c9aebc5a4302e8f83c02e437b679e543bdb6292b`; its retained final candidate passed at `/private/tmp/splunk-hec-package-e188-01.djPh1c/fixture/result.json`. Normalized candidate and Docker results agree on the public contract. |
| Completion criteria | Met: direct regression passed; exact signed package and new marker-protected candidate agree with Docker on endpoint, authorization result, uncompressed ordered events, tag shape, exit/inspect, and cleanup; the candidate stayed within its five-minute liveness boundary. Coverage remains an improvement target, not a functional gate. Comparative performance is deferred. |
| Blocker criteria | No active functional blocker. Any future fingerprint mismatch, behavior disagreement, hang, timeout, or cleanup failure blocks a successor. Performance optimization is deferred unless it reveals a liveness failure. |
| Safe handoff | Retain the Docker root, first candidate, final candidate `/private/tmp/splunk-hec-package-e188-01.djPh1c`, stopped marker-protected runtime `/tmp/checr-e188-01.lDidfM`, signed source/fixture checkpoints, and [the structured handoff](handoffs/LOGGING-SPLUNK-HEC-REST-01.md). Do not touch the user-owned devcontainer runtime or publish. |

### Docker wait acknowledgement prerequisite

The generic liveness correction has a clean signed Container checkpoint
`dc3436ded945ad9a88ac51775e9fcd8dcaad40ca` and Engine API checkpoint
`8aa75ac5e42b0d0399f9ca7d48f0cfad00d3c711` after its source commits. Engine
API's 23-test focused suite and Container's native-lifecycle wait regression
pass. The marker-protected candidate
`/private/tmp/container-docker-wait-ack-01.ckwusl` proves
/wait?condition=removed -> HTTP 200 -> /start, expected output, and exit zero.
Container issue [#91](https://github.com/stephenlclarke/container/issues/91)
contains the evidence and remains open until the ordinary dependency graph can
reproduce it after the Engine API and Containerization prerequisites publish.
The debug candidate's performance warning is non-blocking; a hang or
liveness-bound breach would not be.

## VOLUME-DRIVER-FAIL-CLOSED-01

| Field | Record |
| --- | --- |
| State | `Blocked` — signed generic Container and Engine adapter checkpoints with focused proof exist, but the exact normal graph cannot assemble a full-service candidate: Container `8b7c0ef` requires `WorkloadNetworkEndpoint` and its locked Containerization `77f06d4` lacks it. |
| Behaviour | A request using an unavailable volume driver fails before Container persists a volume, creates a directory or ext4 image, or mutates a container. Omitted and explicit `local` drivers resolve to the built-in local provider. |
| Explicit non-goals | Docker plugin discovery/lifecycle, remote mounts, NFS/cloud drivers, driver-option semantics, Compose/devcontainer projection, migration, and release publication remain `Queued`. Performance design and raw duration recording remain required, but optimization and comparable-or-better timing are post-functional; a hang, timeout, or liveness breach still blocks acceptance. |
| Pinned Docker oracle | Same-MBP Docker CLI `29.7.1`, Engine `29.2.1`, API `1.53`: a unique unavailable driver fails with `plugin "<driver>" not found`; a subsequent inspect returns `no such volume`; no volume is created and the approximately twelve-second reference completes without a hang. |
| Affected repositories and inputs | The original signed Container checkpoint is `fcec20e20a34b9e8b9a8cf2b23823ce8a065cdb4`; its public integration successor is signed Container `8b7c0ef8a911288783883b18b2519225829c4e21` plus signed Engine API `987f05119c0fd6cc8e17c707ffd0c94fbd7d997e`. The current checked-in Containerization pin remains `77f06d4c44341e04241941072fb69e2b85a6f5c1`; no pin moved. |
| Focused proof | The original component suite remains green. The successor adds seven native authority/route/CLI tests, two adapter tests, three Engine volume tests, and five gateway tests using the exact matched local source graph; see [ENGINE-VOLUME-CREATE-ROUTE-01](handoffs/ENGINE-VOLUME-CREATE-ROUTE-01.md). Fresh no-mutation normal-graph proof at `/private/tmp/volume-normal-graph-blocker-01.kFETSt` records the missing pinned `WorkloadNetworkEndpoint` and the matching overlay type. |
| Completion criteria | A fresh full-service candidate must bind source/dependency/binary/guest/init/root into one fingerprint and prove the public Docker CLI and REST route fail before allocation, leave no inspectable/native residue, and satisfy the liveness bound. Both source/evidence checkpoints must be clean and signed. |
| Blocker criteria | The normal graph is currently blocked by the missing `WorkloadNetworkEndpoint` at locked Containerization `77f06d4`; do not move a pin or use an unrecorded overlay to bypass it. A future candidate hang, timeout, fingerprint mismatch, or cleanup failure also blocks immediately. |
| Safe handoff | Preserve signed Container `fcec20e`, `8b7c0ef`, Engine API `987f051`, the original Apple-shaped handoff pair and issue #90, blocker root `/private/tmp/volume-normal-graph-blocker-01.kFETSt`, and the two structured handoffs: [fail-closed component](handoffs/VOLUME-DRIVER-FAIL-CLOSED-01.md) and [public route](handoffs/ENGINE-VOLUME-CREATE-ROUTE-01.md). |

## ENGINE-VOLUME-CREATE-ROUTE-01

| Field | Record |
| --- | --- |
| State | `Blocked` — the public `POST /volumes/create` route, provider declaration, controller, native authority adapter, fail-closed driver resolution, and focused Docker CLI proof are committed and signed. The required full-service candidate cannot assemble from the normal resolved graph because locked Containerization `77f06d4` lacks Container `8b7c0ef`'s required `WorkloadNetworkEndpoint`. |
| Behaviour | A declared provider routes Docker volume creation through the one native `VolumesService`. Omitted/empty drivers resolve to `local`; an unavailable driver returns Docker `404 {"message":"plugin \"<driver>\" not found"}` before any name record, local directory, ext4 image, or container mutation. An undeclared route remains gateway fail-closed. |
| Explicit non-goals | Volume inspect/list/remove/prune, autogenerated names, existing-volume idempotence, full local-driver options, plugins/non-local providers/mounts, Compose/devcontainer projection, `use_api_socket`, release publication, and performance optimization remain `Queued`. A hang or timeout remains functional; raw timings are otherwise informational. |
| Pinned Docker oracle | Same-MBP Docker CLI `29.7.1`, Engine `29.2.1`, API `1.53`, Colima. The retained CLI oracle records unavailable-driver failure, no inspectable volume, and no hang. A guarded raw fake-plugin socket probe on 8 August received no response within five seconds, so it supplies no replacement raw-envelope claim. |
| Affected repositories and inputs | Signed Engine API `987f05119c0fd6cc8e17c707ffd0c94fbd7d997e`; signed Container `8b7c0ef8a911288783883b18b2519225829c4e21`; matched Containerization `ecb2ac5099a8521f02c248870fa6dd7aa7518465`. Container’s checked-in pin is still `77f06d4c44341e04241941072fb69e2b85a6f5c1`; no pin or publication moved. |
| Focused proof | Engine API’s `volume` filter passes three tests and `ContainerEngineGatewayTests` passes five. Container’s exact-overlay `VolumeDriverResolutionTests` passes seven—including unmodified `docker --host unix://<candidate socket> volume create`—and `ContainerDockerVolumeBackendTests` passes two. The disposable 0700 marker-protected root, public/private socket cleanup, and zero native residue are asserted. `git diff --check` and strict formatting pass for all new and other changed files; pre-existing `VolumesService.swift` lint debt is exactly unchanged. |
| Completion criteria | Build one normal-graph, marker-protected, full `container-engine` candidate with a fingerprint binding source SHA, dependency revisions, binary/archive, guest/init images, fixture, and root. Its public Docker CLI/REST path must prove no pre-allocation residue and bounded liveness; refresh a responsive Docker oracle before asserting additional raw error-envelope detail. |
| Blocker criteria | The exact normal pin/type mismatch recorded at `/private/tmp/volume-normal-graph-blocker-01.kFETSt` blocks `Verified` but does not invalidate the exact-overlay implementation proof. Hang, timeout, liveness-bound breach, fingerprint mismatch, or cleanup failure also block immediately. |
| Safe handoff | Preserve the clean dedicated worktrees, signed commits, marker-protected blocker evidence, and [the structured handoff](handoffs/ENGINE-VOLUME-CREATE-ROUTE-01.md). Do not delete/reset them or reuse a disposable test root. |

## LOGGING-FLUENTD-TCP-ACK-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow cache-disabled Fluentd TCP public-socket behavior. Two fresh namespace-isolated candidates match the Docker wire, ACK, identity, history, lifecycle, authority, and cleanup contract. This is not complete Fluentd-driver or comparable-or-better release-performance evidence. |
| Behaviour | An unmodified Docker CLI uses cache-disabled Fluentd TCP with `host.docker.internal`, selected tag/environment/label metadata, chunk acknowledgement, and sub-second timestamps. The receiver sees Docker-compatible Fluent Forward MessagePack EventTime records for ordered stdout, binary stdout, and stderr output; Docker remote history remains unreadable; and cleanup completes. The native candidate must dial loopback only at the macOS transport boundary while retaining the Docker alias for configuration and TLS identity. |
| Explicit non-goals | Fluentd TLS trust, Unix transport, async/retry/buffer/backpressure, dual-cache behavior, full failure/migration/security matrices, other remote drivers, external clients, synchronized publication, and release-grade comparable-or-better performance remain `Queued` or separately `Blocked`. |
| Pinned Docker oracle | Same-MBP Docker CLI `29.7.1` / Engine `29.2.1` / `alpine:3.20`. Retained `/private/tmp/container-rest-fluentd.default-host-v2.vGmC8z/result.json` passes in `0.42431754095014185` seconds (SHA-256 `ffd4198d884af60244d747017aac0aae5ae60c9eefba9b24a76e82595046556e`); its receiver result SHA-256 is `51ed8065d8427c94f4273986814cc9feb318f306d18b64c195d2473ecee0c69b`, with two connections, three records, three ACKs, selected tag/metadata, peer close, no timeout, and no errors. |
| Affected repositories and inputs | Compose `acae2e3dfe5ddda86a2bf498160aab630bef16ce` supplies the Bash fixture (SHA-256 `34d61d128ef9f000c2b94158ceb5ecc5e264603ed633ec0609e8bc58309a18a2`) and wrapper (SHA-256 `7a396d8626a0e37c1b7f71e732674baebd1b3752bedc3378a7e4510e3323987f`). Local Container `upstream/logging-fluentd-tcp-ack-rest-01` is `359e14e4f991db0f3729d44651b9f82d9ab1b0ed`; it contains the alias correction and leading-slash Forward record normalization. Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7` built the guest, and Engine API is `f5d0d120bb139675e96a4ef9f7b0ac800827c295`; no published pin moved. |
| Focused proof | The retained alias/ACK regression and `FluentdNIOTransportLoopbackTests` `12/12` cover the native dial boundary. The new `FluentdConfigurationTests` focused run passes `8/8`; bare `web` becomes Forward `container_name=/web` while the tag remains `web`. The changed executable line ran 13 times, meeting the >=90% changed-code target. The Bash fixture passes syntax and ShellCheck; Swift formatting and diff checks pass. |
| Completion criteria | Met: two fresh source/dependency/binary/guest/root candidates passed the strict Docker CLI fixture. Candidate 1: `2.768545625s` / `6.525×` Docker; candidate 2: `2.345302459s` / `5.527×` Docker. Each reports three EventTime records, three ACKs, zero receiver errors, Docker-matched unreadable history, one native authority, exact cleanup, and a healthy user runtime after stop. |
| Blocker criteria | Not reached. The two retained early setup failures did not enter the Fluentd fixture; no real wire, ACK, lifecycle, authority, or cleanup mismatch remains. The timings are retained for post-functional optimisation and do not satisfy comparable-or-better release performance. |
| Safe handoff | Preserve Docker `/private/tmp/container-rest-fluentd.default-host-v2.vGmC8z`, source guest `/private/tmp/ctr-fluentd-guest-359-bootstrap.7VWfQA`, candidate roots `/private/tmp/ctr-fluentd-name-359.FGmIiu` and `/private/tmp/ctr-fluentd-name-359.iwMbBz`, fixture roots `/private/tmp/container-rest-fluentd.candidate-name-359.6UnO7v` and `/private/tmp/container-rest-fluentd.candidate-name-359.y9TRQi`, and exact fingerprint certificate `/private/tmp/container-rest-fluentd.candidate-name-359.6UnO7v/FINGERPRINT-COMPLETE.json`. Preserve the signed commits, closed [Container issues #84](https://github.com/stephenlclarke/container/issues/84) and [#86](https://github.com/stephenlclarke/container/issues/86), [the structured handoff](handoffs/LOGGING-FLUENTD-TCP-ACK-REST-01.md), [the verified namespace prerequisite](handoffs/RUNTIME-ISOLATED-PUBLIC-SOCKET-01.md), and Slack START thread `1786123301.422779`. Apple `origin/main` has no Fluentd provider here, so no Apple upstream handoff applies. |

## LOGGING-FLUENTD-TLS-TRUST-FAILURE-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker CLI/public-socket TLS trust-failure certification. Darwin's internal Security.framework verifier now selects BoringSSL `bad_certificate` while public custom-verifier alert semantics remain unchanged. This does not certify trusted Fluentd TLS delivery, all TLS routes, or release performance. |
| Behaviour | An unmodified Docker CLI configures cache-disabled Fluentd `tls://host.docker.internal:PORT` to a bounded self-signed host receiver. Create must accept configuration; start must fail at the Docker logging-initialization phase, retain `created`, preserve inspection, trigger the expected trust alert, and clean up container/receiver/public-socket state. |
| Explicit non-goals | Trusted TLS delivery, custom roots, successful SNI/identity behavior, Unix transport, async/retry/buffer/backpressure, dual cache, full failure/migration/security matrices, external clients, publication, and comparable-or-better release performance remain `Queued`. The two completed candidate durations are retained for the post-functional performance phase and do not block this functional contract. |
| Pinned Docker oracle | Same-MBP Docker CLI `29.7.1` / Engine `29.2.1` / API `1.53` / `alpine:3.20`. Fresh `/private/tmp/container-rest-fluentd-tls.docker.m7QDss/result.json` passed in `0.31440287502482533s` (SHA-256 `ce8ad3a5048ba86f9a162d109e1dd1b2eceda667e1d61ec26558565c584b9d14`): create accepted, start rejected at HTTP `500` with Docker's exact trust diagnostic, inspect retained `created`, exact cleanup passed, and the bounded receiver saw `SSLV3_ALERT_BAD_CERTIFICATE`. |
| Affected repositories and inputs | Compose signed `17822a323ac2e7a12078adfd4b8a2921b4169205` supplies the fixture (SHA-256 `bcfca0e581afa8f17f55c48e307c3fd04305b4eb275371b85932c218067e7221`) and wrapper (SHA-256 `7a396d8626a0e37c1b7f71e732674baebd1b3752bedc3378a7e4510e3323987f`). Local Container `upstream/logging-fluentd-tls-alert-control-01` is signed `4933786122ea2e62069d0fbaa6eccc41925bd2ba`; Containerization is `38d9c695e7a6915e5ce45d12c893dc323a661af7`; Engine API is `f5d0d120bb139675e96a4ef9f7b0ac800827c295`; and local `apple/swift-nio-ssl` `upstream/fluentd-tls-alert-control-01` is signed `a9d648535c62e640d1df258a70c9117a8ddea43e`. The signed release archive is SHA-256 `0111caa0d482a387b2da735bb9f7375ddcb4fb997e7daf01c9167e2dfbb89516`; extracted CLI SHA-256 `379cefb05fe1974e1cd4f0d9ba94619671a0ee2a96a70dde596af38848fba9b7`; guest archive SHA-256 `5d4201135affb9bb0ce34ebcb184551689a214d3118b75564a8fa498667d77f6`; and bootstrap OCI archive SHA-256 `c714ab7421c71cebdfd0236c5a1af4b1e9af3da1855946cf3350a384491815f0`. No published pin moved. |
| Focused proof | SwiftNIO SSL's strict-format test and seven focused manager/bridge/Darwin integration cases pass with every new executable Swift line hit; the C shim test confirms TLS alert `42` (`bad_certificate`). The retained public fixture passed through two fresh marker-protected release candidates: `/private/tmp/container-rest-fluentd-tls.candidate-alert-release4.TepPNc/result.json` in `2.637404042063281s` and `/private/tmp/container-rest-fluentd-tls.candidate-alert-release5.EyN9pZ/result.json` in `1.2239124169573188s`. Each binds the exact source/dependency/binary/guest/fixture/root graph, matches Docker create/start/inspect/alert/cleanup assertions, and leaves the user runtime healthy. |
| Completion criteria | Met: the fresh Docker reference and two independent exact-fingerprint candidates match configuration acceptance, start failure phase, `created` state and exit `128`, exact diagnostic, receiver `SSLV3_ALERT_BAD_CERTIFICATE`, container removal, receiver exit, namespace-scoped service cleanup, and unchanged user-runtime health. |
| Blocker criteria | Resolved. The retained historical candidate `/private/tmp/container-rest-fluentd-tls.candidate-66e-1.tBR6n1` showed `SSLV3_ALERT_CERTIFICATE_UNKNOWN` despite the correct diagnostic and state. The narrow local SwiftNIO SSL correction now threads `outAlert` only for the Darwin internal verifier; it does not alter Docker, candidate, or host trust stores. The direct SwiftPM binary and incomplete Docker-format bootstrap archive failures are retained as setup-preflight evidence only, not product failures. |
| Safe handoff | Preserve the Docker reference root, the two passing candidate roots, `/private/tmp/container-tls-alert-stage.5h9L5d/fingerprints/`, and the Apple-shaped SwiftNIO SSL handoff. This verified contract needs no further work; resume only on an independent queued contract. The original TLS start thread is `1786132081.281259`; the alert-control continuation is Slack START thread `1786135745.826539`. |

## RUNTIME-ISOLATED-PUBLIC-SOCKET-01

| Field | Record |
| --- | --- |
| State | `Verified` — a unique candidate Container launchd/Mach namespace and Docker socket completed a marker-protected public-client lifecycle while the user-owned `devcontainer-engine` stayed healthy. This is a lower-runtime prerequisite, not a logging or performance certificate. |
| Behaviour | A candidate starts services under `CONTAINER_SERVICE_NAMESPACE`, exposes the namespace-derived public Docker socket, answers unmodified Docker CLI `docker version`, and stops only its own Engine/APIServer/Machine/Core Images/network/runtime services. |
| Explicit non-goals | Logging-driver semantics, Compose project behaviour, REST surface beyond `docker version`, release performance, and generic safety for legacy/default-namespace callers remain outside this narrow contract. |
| Pinned Docker oracle | Same-MBP Docker CLI `29.7.1`, Docker Engine `29.2.1`, API `1.53`; the user baseline `devcontainer-engine` socket returned `29.7.1\|1.1.0\|linux` before and after the candidate. |
| Affected repositories and inputs | Compose `40d109db071cd98f2b37300e417e478f00213b3c`, Container `c740a8f6a79ce176d03a941f49cdfe7350625a71`, Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, and Engine API `4949e743675f00ec102f7acacdb4e990409e383f`. |
| Focused proof | Container's 19 focused namespace/start/stop tests passed with `ContainerServiceNamespace.swift` at 100% line/function/region coverage; Compose's 14 runner tests passed at 99% branch coverage. The exact candidate staged source and guest, returned `29.7.1\|29.2.1\|linux`, then exited `0` after 722 seconds. |
| Completion criteria | Met: preflight selected `/tmp/container-engine-501-8806c2d9ecceef7b17c5576b/docker.sock` under `io.github.stephenlclarke.container-compose.runtime.a46377784f5464874269b3ca`; postflight found that socket and every candidate service absent, while the user runtime remained running. |
| Safe handoff | Retain `/private/tmp/ctr-isopub4.Id3j22`, `/private/tmp/ctr-isopub4.Id3j22.preflight.txt`, `/private/tmp/ctr-isopub4.Id3j22.run.log`, `/private/tmp/ctr-isopub4-stage-final.cP8TjU`, and [the structured handoff](handoffs/RUNTIME-ISOLATED-PUBLIC-SOCKET-01.md). The next public-socket contract must use a fresh candidate root and must not treat the debug source-build duration as performance evidence. |

## LOGGING-LOCAL-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker CLI/public-socket lifecycle proof for the built-in `local` logging driver. It does not certify all logging providers, all Docker routes, or comparable release performance. |
| Behaviour | An unmodified Docker CLI creates `json-file`, `local`, and `none` containers over the public Container Engine Unix socket. For `local`, it observes Docker's blank public `LogPath`, follows stdout/stderr, re-reads retained history, tails output, performs a second start, sees the same driver through native Container authority, and deletes the exact object without residue. |
| Explicit non-goals | Provider/external-client completeness, remote drivers, cache/rotation pressure, migration/security/failure matrices, Testcontainers/devcontainer adoption, release publication, and comparable-or-better release performance remain `Queued`. This is a Docker Engine CLI contract, not a Docker Compose project contract. |
| Pinned Docker oracle | Same-MBP `colima`: Docker Engine `29.2.1`/API `1.53`, Docker CLI `29.7.1`, and `alpine@sha256:d9e853e87e55526f6b2917df91a2115c36dd7c696a35be12163d44e6e2a4b6bc`. `Tools/parity/check-docker-rest-logging-contract.sh --strict --reference` passed in 16 seconds. |
| Affected repositories and inputs | Compose signed `d6e59843c1c59f7bcff2240aaf5517290f268538` adds the `local` certificate and signed `4cdafffcc9e46e3fcd4d965d897e1feee93ce941` bootstraps a retained init image before the first start. Container signed `c7b4898d4befad75480856305294001bd2eabf37`, `79b79cfe08478fd7fecf9372a6ab654242abebef`, and `e048dc19d54e25aa3887689d0015d5af447d4ad5`; Containerization is `38d9c695e7a6915e5ce45d12c893dc323a661af7`; Engine API is `4949e743675f00ec102f7acacdb4e990409e383f`. No dependency pin moved. |
| Focused proof | Bash syntax validation and both retained-archive harness regressions pass. `SystemStartTests/parsesInitialFilesystemArchive` passes on the exact local graph. The fresh release package uses an explicit SwiftPM scratch root, OCI init archive, and native source-derived initial filesystem before running the same Docker CLI certificate against `unix:///tmp/container-engine-501/docker.sock`. |
| Completion criteria | Met: the Docker reference and exact candidate socket both pass; source/dependency/binary/init-image/root fingerprints are retained; the OCI archive is unpacked before registry fallback; and cleanup proves the public socket plus both launchd services are absent. |
| Blocker criteria | Two evidence-based source/harness corrections that still prevent the exact candidate from assembling or passing the public-socket certificate. Not reached: first candidate failure exposed the bootstrap ordering gap, and the archive path resolved it. |
| Safe handoff | Retain `/private/tmp/container-local-rest-evidence.SLbPgY` (`.logging-local-rest-evidence-root`), its candidate log, source-derived guest/archive hashes, the signed checkpoints above, and Slack START thread `1785968996.613189`. Resume only on an independent queued contract. |

### Local REST Verification Evidence

- The exact source-derived initial filesystem tarball has SHA-256 `a1c0db04322ad9a589d0b941b26671cf5068ab560e9a3b728348cd391d4c2dde`; its arm64 OCI archive is `1e1e45807202cff487690cfd4cb43fef05a22aab163cd7c2ade0cf20f567b448` and names `docker.io/library/vminit:container-compose`.
- The fresh signed release archive has SHA-256 `57067dd534f61e652deb0fb24de09c61efcc0c5d84896131eaa596113f67af85`; its candidate `container`, `container-engine`, and `container-apiserver` hashes are respectively `e166426b3633d280323a0b8f66f3abf51b9093757a320f8f1883824cd5b992e1`, `87e55993e192f52c383d5247205b2d1a6ae463bbc0852edab0a30cd758dbdeed`, and `2eca63ee6b6ce7670e72509887f0ab12a3129fad80df9cd3f472037cc2ac9da8`.
- `candidate-local-rest-v6.log` records initial archive loading, the controlled restart, `Docker REST logging contract passed`, and exact shutdown. Afterward `/tmp/container-engine-501/docker.sock`, `com.apple.container-apiserver`, and `com.apple.container-engine` are absent.

## LOGGING-LOCAL-ROTATION-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker CLI/public-socket certificate for compressed built-in `local` rotation and its deferred invalid-start lifecycle. It does not certify every logging driver, every rotation pressure mode, or broader external-client support. |
| Behaviour | Docker CLI creates `local` containers with `max-size=4k`, `max-file=3`, and `compress=true`; retains the expected three-file history across a second start; returns the expected global tails; exposes an empty public `LogPath`; and keeps a rejected `max-file=1` compression start in `created` state with Docker's reason in both the start response and inspect `State.Error`. |
| Explicit non-goals | Remote-provider rotation, dual-cache pressure, slow-sink/backpressure, migration/security/performance matrices, Testcontainers/devcontainer external clients, release publication, and full-suite execution remain `Queued`. This is an Engine CLI/public-socket contract, not a Compose-project certification. |
| Pinned Docker oracle | Same-MBP `colima`: Docker Engine `29.2.1`/API `1.53`, Docker CLI `29.7.1`, and `alpine@sha256:d9e853e87e55526f6b2917df91a2115c36dd7c696a35be12163d44e6e2a4b6bc`. `Tools/parity/check-docker-rest-logging-contract.sh --strict --reference` passes the compressed rotation, restart, tail, and rejected-start semantics. |
| Affected repositories and inputs | Compose signed `c19ce0f04ba98f7bf133c753f79604885bac4747` extends the public fixture. Container signed `666a99a5df004111b4addd84565a90e7c26f767d`, `9c442be1e175625425f630dba1956c2d348b6104`, and `f16b7de6ceb5ed3dd588bab08b11867a410ef346` align the local diagnostic, start response, durable Docker start error, and inspect projection. Containerization is `38d9c695e7a6915e5ce45d12c893dc323a661af7`; Engine API is `4949e743675f00ec102f7acacdb4e990409e383f`. No dependency pin moved. |
| Focused proof | The reference certificate passes. A fresh signed release archive from an explicit scratch root binds the exact local Container, Containerization, and Engine API source paths, the retained OCI init image, and the disposable runtime root before the candidate public-socket certificate runs. The candidate passes the same fixture, including the previously failing `State.Error` assertion. The added authority-restart regression has syntax/format evidence; SwiftPM filtered execution remains intentionally deferred because it expands the unrelated package graph. |
| Completion criteria | Met: the reference and exact candidate public socket pass, source/dependency/binary/init-image/root fingerprints are retained, rejected local compression is visible in both Docker error surfaces, and wrapper cleanup proves no socket or candidate process remains. |
| Blocker criteria | Not reached. The first candidate exposed a response-mapping mismatch; the next exposed a distinct hard-coded empty inspect error. Each correction produced new evidence and the final candidate passed. |
| Safe handoff | Retain `/private/tmp/container-local-rotation-evidence.6AdU7M` (`.logging-local-rotation-evidence-root`), its two build records, fingerprint, runtime result, the signed Container checkpoints, and Slack START thread `1785975367.993369`. Resume only on an independent queued contract. |

### Local Rotation Verification Evidence

- `/private/tmp/container-local-rotation-evidence.6AdU7M/FINGERPRINT-READY.json` records Compose `c19ce0f04ba98f7bf133c753f79604885bac4747`, Container `f16b7de6ceb5ed3dd588bab08b11867a410ef346`, Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`, Engine API `4949e743675f00ec102f7acacdb4e990409e383f`, direct local dependency paths, the restored `Package.resolved` SHA-256, candidate binaries, retained OCI init archive, and marker-protected root.
- The signed release archive SHA-256 is `ae9ee6fbd3cd693a1e9b31ea09615ec77ff2452d2b7bb163752394bf72700881`; the candidate `container`, `container-engine`, and `container-apiserver` SHA-256 values are respectively `1d990ccf5617fabaaf587335183f0117e91ff2025ef8ac42f496ee134b62a8c0`, `885979356ac46117d6afbe8f2c447203283ebc2df3cd5ef271d379b74879c49c`, and `a7c22b07b1acdd8ff02588ae558fee060a41ffe84d77a519cbd45b728b41ab99`; codesign verification passes.
- `RUNTIME-ATTEMPT-01.json` records the exit-zero `Docker REST logging contract passed` result and the exact public-socket command. After wrapper shutdown, `/tmp/container-engine-501/docker.sock` and candidate process matches are absent.
- `BUILD-ATTEMPT-01.json` retains the first package-only harness failure caused by an omitted trailing slash in the isolated `STAGING_DIR`; the corrected package reused the completed source build and passed. This is build-invocation evidence, not a source or runtime mismatch.

## DEPLOY-JOBS-LOCAL-01

| Field | Record |
| --- | --- |
| State | `Verified` — the exact documented main graph now builds the runtime adapter; the five focused candidate regressions and the strict Docker/CLI certificate pass from one marker-protected root. |
| Behaviour | Docker Compose local-mode handling of `deploy.mode: replicated-job` and `global-job`: preserve the requested mode/configuration, use ordinary local service convergence rather than a job-specific wait or per-node scheduler, and map valid `restart`/`deploy.restart_policy` values through the ordinary container restart projection instead of rejecting them because the service is a job. |
| Explicit non-goals | Swarm scheduling, task-per-node behavior, completion orchestration, generic resource/device reservations, DeviceBroker, and Docker Engine lifecycle changes remain separate contracts. This slice removes only the extra local Compose rejection/wait behavior. |
| Pinned Docker oracle | The original same-MBP Docker Compose V2 `5.3.1` / Docker Engine `29.2.1` / API `1.53` `colima` capture preserves both modes, ordinary `on-failure:3`/`unless-stopped` HostConfig restart policies, detached `up`, and readiness-based `up --wait`. The current Docker Compose V2 `5.4.0` / Docker CLI `29.7.1` / Engine `29.2.1` strict revalidation passed the same fixture against `alpine@sha256:d9e853e87e55526f6b2917df91a2115c36dd7c696a35be12163d44e6e2a4b6bc`, with no project residue. |
| Affected repositories and inputs | The behavior change is signed Compose `a591d70648651b1d33b4ef9425b9c4745f197b3f`. Exact-graph verification additionally uses signed Container `68cd7a6d9d97d3d7cbfe65080799a4779b96a333`, signed Containerization `5338e6685df56b8c15b49d0e7dd272a87abe0865`, and Engine API `4949e743675f00ec102f7acacdb4e990409e383f`. No devcontainer or external-provider change is claimed. |
| Focused proof | `ContainerLaunchAdapterTests` built against the exact main graph; all five named `ComposeOrchestratorTests` passed; and `Tools/parity/check-compose-deploy-job-modes.sh --strict` passed using the exact candidate binary. The certificate compares Docker's real config/detached/`--wait` behavior and HostConfig to the candidate config/dry-run plan, so it does not overclaim a live candidate guest run. `/private/tmp/container-build-main-stack-evidence.Jkdz8E/BUILD-MAIN-STACK-01-fingerprint.json` records the source, dependency, binary, image, and root fingerprints. |
| Completion criteria | Met: the main graph compiles the runtime adapter, the five focused regressions and strict Docker/CLI certificate pass, generated lockfile mutations are restored, and the isolated project cleanup is proven. |
| Blocker criteria | Resolved by `BUILD-MAIN-STACK-01`. The remaining SwiftPM local/remote Containerization identity warning is dependency-harness hygiene tracked separately; it did not prevent this exact proof. |
| Safe handoff | Preserve the original marker-protected Docker capture and the current `/private/tmp/container-build-main-stack-evidence.Jkdz8E` fingerprint, [the structured handoff](handoffs/DEPLOY-JOBS-LOCAL-01.md), and Slack START thread `1785962702.085149`. Do not reopen this verified behavior for Swarm scheduling, task-per-node behavior, or generic reservation work. |

### Capture and Verification Evidence

- `docker-compose-5.3.1-job-local-result.json` retains the original direct Docker config, detached-up, and `--wait` results; `candidate-baseline-dry-run.json` retains the pre-change candidate rejection of a Docker-accepted `global-job` `restart: unless-stopped` fixture.
- `ContainerLaunchAdapterTests` compiled against the exact main graph, the five new Compose regressions all passed, `ContainerFilesystemAdapterTests` passed the staged path-copy fallback, and the strict Bash certificate passed. The candidate's binary and all source/dependency/image/root fingerprints are retained in the current marker-protected root.
- Historical main-stack compiler evidence is preserved: main Container lacked archive-stream APIs and `ContainerLogRequest`. The signed compatibility work moves Compose to the maintained path-copy fallback, adds the narrow Container request/error surface, and lets ArchiveReader honor `preserveOwnership: false`; `Package.resolved` remains at SHA `a61f450629fb45e6e39c66ef725bc390cb413ee61fad60ce518d04952c786ef5`.

## BUILD-MAIN-STACK-01

| Field | Record |
| --- | --- |
| State | `Verified` — the documented main graph compiles `ComposeContainerRuntime`, its focused compatibility tests pass, and the previously blocked deploy-job contract now has complete focused proof. |
| Behaviour | A user can build the local Compose runtime using the documented main `Container`, `Containerization`, and `ContainerEngineAPI` checkouts without an API-only compile rejection. Archive copies fall back safely to the maintained path-copy/archive staging implementation when native archive streams are absent. |
| Explicit non-goals | This does not provide the full logging-provider reader API, change dependency pins, publish any component, or make a general claim about every runtime behavior. The public logging types are deliberately narrow compatibility surfaces required by the current Compose adapter. |
| Pinned Docker oracle | The original Docker Compose `5.3.1` capture is retained and the same fixture was strictly revalidated with Docker Compose `5.4.0` on the same-MBP `colima` Engine `29.2.1` context. |
| Affected repositories and inputs | Compose starts at signed `a591d70648651b1d33b4ef9425b9c4745f197b3f`; verified component checkpoints are Container `68cd7a6d9d97d3d7cbfe65080799a4779b96a333`, Containerization `5338e6685df56b8c15b49d0e7dd272a87abe0865`, and Engine API `4949e743675f00ec102f7acacdb4e990409e383f`. |
| Focused proof | Exact-main-stack `ContainerLaunchAdapterTests`, `ContainerFilesystemAdapterTests`, five deploy-job `ComposeOrchestratorTests`, two Container compatibility unit suites, one ArchiveReader unit test, and `Tools/parity/check-compose-deploy-job-modes.sh --strict` all pass. `/private/tmp/container-build-main-stack-evidence.Jkdz8E/BUILD-MAIN-STACK-01-fingerprint.json` binds the source, dependency, binary, Docker image, and disposable root. |
| Completion criteria | Met: no unrelated lockfile change remains, project cleanup is proven, and GitHub issue #198 is updated with the signed local proof. Its closure is deferred until the explicitly authorised programme-wide publication exposes the fixes on remote main. |
| Blocker criteria | Not reached. The exact graph still emits a SwiftPM local/remote Containerization identity warning that SwiftPM says may become an error; it is a separate dependency-harness gap and is not hidden by this verification. |
| Safe handoff | Preserve `/private/tmp/container-build-main-stack-evidence.Jkdz8E` and its marker/fingerprint, issue #198, and Slack START thread `1785964981.520989`. Queue all unrelated parity gaps until a new user-visible contract is selected. |

## DOCKER-CREATE-ID-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker CLI/public-socket certificate for canonical Docker container create identity. It does not certify every Docker object route, event, rename, migration, external client, or comparable-or-better release performance. |
| Behaviour | An unmodified Docker CLI receives a canonical immutable 64-character lowercase-hex ID from `docker container create`; the requested name remains separately visible; and the requested name, full ID, and unique 12-character prefix resolve to the same object for inspect, start, stop, delete, and logs. GELF records and Docker plug-in start inputs use that same canonical ID/name rather than native resource identity. |
| Explicit non-goals | Broader Docker lifecycle actions, events, rename, exec, every REST route, provider/external-client matrices, release publication, and programme-wide comparable-or-better performance remain `Queued`. This is a Docker Engine public-socket identity certificate, not a complete Compose project or object-model certification. |
| Pinned Docker oracle | Same-MBP Docker CLI `29.7.1`, Engine `29.2.1`, API `1.53`, and `alpine:3.20`. The marker-protected Docker reference root `/private/tmp/container-create-id-reference-v2.DRU21z` passed in `0.700619291s` and returned a canonical 64-hex ID. |
| Affected repositories and inputs | Container implementation is signed `9d2257a81176a895a31388124bd6a7b0b74d10e6`; its Apple-shaped handoff is signed `b580b2ee43540189e764db293c2a74a531123d26`. The candidate Compose input is `eef6f8d9` plus retained public-harness diff SHA-256 `3462048f1b26070bb0e99dbaffe1a691caaa329ea5f3ed1a2f1c728269a8ebf4`. Local Containerization is `38d9c695e7a6915e5ce45d12c893dc323a661af7`; Engine API is `4949e743675f00ec102f7acacdb4e990409e383f`; no dependency pin moved. |
| Focused proof | `ContainerLogsTests.dockerContainerIdentityUsesCanonicalIDAndNameAliases` and `AuthorityRemoteLogDriverPlaneTests.dockerLogInfoUsesCanonicalDockerIdentityWhenAvailable` pass on the exact local graph. The owned Bash Docker CLI harness asserts canonical create ID/name/full/short aliases and runs twice against one fresh code-signed candidate whose source, dependencies, binaries, guest/init images, script, wrapper, Docker reference, and root fingerprints agree. |
| Completion criteria | Met: Docker reference and both exact candidates pass; direct source regressions pass; source/dependency/binary/init-image/root evidence is retained; cleanup removes the candidate socket and processes; and signed Container plus Compose documentation checkpoints preserve the result. |
| Blocker criteria | Not reached. The first candidate revealed that GELF metadata still used native resource identity. `AuthorityRemoteLogDriverPlane.dockerLogInfo` became the single canonical logging identity projection; the corrected source regression and both public candidates then passed. |
| Safe handoff | Preserve `/private/tmp/container-create-id-reference-v2.DRU21z` (`.container-create-id-reference-root`), `/private/tmp/container-create-id-candidate-v1.ga0khK` (the retained failed metadata-identity evidence), and `/private/tmp/container-create-id-candidate-v2.9lSjrX` (`.container-create-id-evidence-root`), the signed source handoff pair, this handoff, and Slack START thread `1785998717.472209`. Owned [Container issue #74](https://github.com/stephenlclarke/container/issues/74) is commented and closed as completed after this signed Compose checkpoint. |

### Docker Create-ID Verification Evidence

- `/private/tmp/container-create-id-candidate-v2.9lSjrX/FINGERPRINT-COMPLETE.json` binds the source/dependency graph, code-signed archive, binary hashes, guest/init archives, harness, wrapper, Docker reference, two candidate result files, and cleanup proof. Its preflight and archive-verification files are retained beside it.
- Candidate one passed in `1.591354583s` with canonical ID `5d59f15fee984ae0b4351e6823babb0a92795dbd358f470a81b588ed5b8f3f29`; candidate two passed in `1.574481541s` with canonical ID `69093b4c680048738d1d7e79511898f5e7c6009e135843e79f41f6dadf052837`. They are 2.27x and 2.25x Docker and are retained for post-functional optimisation, not comparable-or-better performance proof.
- The v1 candidate is retained rather than discarded: it routed name/full/short IDs correctly but exposed GELF `_container_id` and `_container_name` as native resource identity. The v2 fingerprint contains the corrective source/test evidence and exact successful candidate runs.
- Owned [Container issue #74](https://github.com/stephenlclarke/container/issues/74) has the exact local evidence comment and is closed as completed; no source or evidence is pushed or published by that issue close-out.

## LOGGING-SYSLOG-UDP-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker CLI/public-socket Syslog UDP certificate. It does not certify every Syslog transport, remote provider, external client, failure, migration, security, or comparable-or-better release-performance behavior. |
| Behaviour | The same unmodified Docker CLI creates a cache-disabled Syslog UDP container against `host.docker.internal`, emits Docker-compatible `rfc5424micro` datagrams for stdout, stderr, and binary output in order, maps `local1` facility/severity, expands Docker tags, projects inspect and unreadable remote history correctly, uses one native authority, and cleans up the receiver/container/socket state. |
| Explicit non-goals | Syslog TCP, TLS, Unix, reconnect/failure/backpressure, every Syslog option, Fluentd/cloud/plugin drivers, Testcontainers/devcontainer adoption, release publication, and counterbalanced comparable-or-better performance remain `Queued`. This is a direct Docker Engine public-socket certificate, not a complete Compose project or logging-driver certification. |
| Pinned Docker oracle | Same-MBP Docker CLI `29.7.1`, Engine `29.2.1`, API `1.53`, and preloaded `alpine:3.20`. The marker-protected Docker reference root `/private/tmp/container-syslog-udp-reference-v3.DzcKyp` passed in `0.662001833s`. |
| Affected repositories and inputs | The signed Container source correction is `5ebce8a2aa90bf96775285e1a79ac48b375f69c5` on local `upstream/logging-driver-parity`; Compose began at `61e85232114c00c3d9dcc0fca9cb6cacd927253a` and supplies harness SHA-256 `a0060157e0f87c84d2b7b838537499bf689e9016d93cca4ecaf96545986e927d`. Local Containerization is `38d9c695e7a6915e5ce45d12c893dc323a661af7`; Engine API is `4949e743675f00ec102f7acacdb4e990409e383f`; no dependency pin moved. |
| Focused proof | `NIOSyslogTransportLoopbackTests.productionDockerHostAliasRoutesTCPAndUDPToNativeLoopback` passes on the exact local graph. The public Bash Docker CLI harness passed once against the Docker reference and twice against one code-signed candidate. `FINGERPRINT-COMPLETE.json` binds the source/dependency graph, archive/binary hashes, guest/init images, harness, wrapper, disposable roots, result files, and cleanup. |
| Completion criteria | Met: the source regression, Docker reference, and two exact candidates pass; the v1 failing start has an evidence-based source correction; exact fingerprints and marker-protected roots are retained; cleanup removes candidate wrappers/residue; and signed Container plus Compose checkpoints preserve the evidence. |
| Blocker criteria | Not reached. The pre-fix v1 candidate started the object but failed `docker start` with `container logging operation failed`; `NIOSyslogTransport` now maps the Docker host alias only at the macOS connection boundary while retaining the configured TLS identity. The corrected source test and both public candidates passed. |
| Safe handoff | Preserve `/private/tmp/container-syslog-udp-reference-v3.DzcKyp` (`.container-syslog-udp-reference-root`), `/private/tmp/container-syslog-udp-candidate-v1.rBK0pZ` (failed-start and focused-test evidence), `/private/tmp/container-syslog-udp-candidate-v2.wk53mN` (retained packaging/invocation evidence), and `/private/tmp/container-syslog-udp-candidate-v3.u86DXn` (`.container-syslog-udp-evidence-root`), the source commit, this handoff, and Slack START thread `1786002918.928579`. Owned [Container issue #78](https://github.com/stephenlclarke/container/issues/78) records the evidence and is closed after this signed Compose checkpoint. Apple `origin/main` `ddaf2ca5ac5629d0555e4a5e04e491de665fd958` lacks this fork-only Syslog provider, so no Apple upstream handoff or PR is applicable. |

| Evidence | `/private/tmp/container-syslog-udp-candidate-v3.u86DXn/FINGERPRINT-COMPLETE.json` has `Implemented` because it was captured before the documentation checkpoint; this ledger's clean signed checkpoint is the state transition to `Verified`. |
| Evidence | Candidate one passed in `2.039981834s` (3.081535023× Docker) and candidate two in `1.529630292s` (2.310613379× Docker). Both durations are retained for post-functional optimisation; neither proves comparable-or-better programme performance. |
| Evidence | The source test log is `/private/tmp/container-syslog-udp-candidate-v1.rBK0pZ/focused-syslog-test.log` with SHA-256 `c74195c40b6d648307591d9fe8f762da4eb01fe380166ec912c036eeca835440`. It covers both lower- and upper-case Docker host aliases over TCP and UDP. |
| Evidence | Owned [Container issue #78](https://github.com/stephenlclarke/container/issues/78) is commented and closed after the final signed Compose checkpoint. It neither pushes the local source commit nor represents an Apple upstream report. |

## LOGGING-SYSLOG-TCP-REST-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker CLI/public-socket certificate for stopped cache-disabled Syslog TCP `docker logs`. It does not certify TLS, Unix sockets, retries, backpressure, every Syslog option, external clients, migration, security, or comparable-or-better release performance. |
| Behaviour | The same unmodified Docker CLI creates and starts a cache-disabled Syslog TCP container against `host.docker.internal`, emits ordered Docker-compatible `rfc5424micro` stdout, stderr, and binary records, then invokes `docker logs` after exit. Both Docker and the candidate open a second empty TCP logger connection before returning `configured logging driver does not support reading`; inspect, native authority, and cleanup remain correct. |
| Explicit non-goals | Running/paused follow behavior, TLS, Unix sockets, connection retries, failing-sink/backpressure behavior, complete option grammar, every provider, Testcontainers/devcontainer adoption, publication, and counterbalanced comparable-or-better performance remain `Queued`. This is a direct Docker Engine public-socket certificate, not a complete Compose or Syslog-driver certification. |
| Pinned Docker oracle | Same-MBP Docker CLI `29.7.1`, Engine `29.2.1`, API `1.53`, and preloaded `alpine:3.20`. The marker-protected reference `/private/tmp/container-syslog-tcp-reference-v8.YBX82T` passed in `0.967602000s`, with two total connections and one empty post-`logs` connection. |
| Affected repositories and inputs | Signed Container source `b82e34874b944d2b9ecc65d4068aee5d7b46905e` on local `upstream/logging-driver-parity`; Compose base `603bc8d5536fc176e745d034fbc42e386bafbbba` plus harness SHA-256 `69bd8acd61e5986304e88dc9f15b1bf883c485ef4aad4a5336bf312af92dbf5d`; local Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`; Engine API `4949e743675f00ec102f7acacdb4e990409e383f`. No dependency pin moved. |
| Focused proof | `SyslogProviderTests` passes 8/8 on the exact local overlay; `recreateStoppedLogger` has 10/10 executable focused lines covered. The public Bash Docker CLI harness passes once against Docker and twice against one code-signed candidate. `/private/tmp/container-syslog-tcp-candidate-v4.OZHeoC/FINGERPRINT-COMPLETE.json` binds the source/dependency graph, archive/binary hashes, guest/init image, kernel, harness, wrapper, disposable roots, result files, and cleanup. |
| Completion criteria | Met: the source regression, fresh Docker reference, and two exact candidates pass; the new harness asserts the observable reconnect; marker-protected roots retain the source/dependency/binary/guest/kernel/result fingerprint; cleanup removes the candidate socket; and signed Container plus this Compose documentation checkpoint preserve the result. |
| Blocker criteria | Not reached. Before `b82e348`, the candidate returned the public unreadable-history error without recreating a Syslog logger, so it lacked Docker's second empty TCP connection. The source correction and two public candidates now match the observed behavior. |
| Safe handoff | Preserve `/private/tmp/container-syslog-tcp-reference-v8.YBX82T` (`.container-syslog-tcp-reference-root`) and `/private/tmp/container-syslog-tcp-candidate-v4.OZHeoC` (`.container-syslog-tcp-evidence-root`), signed Container `b82e348`, source issue/PR handoffs, this handoff, and Slack START thread `1786006125.498149`. Owned [Container issue #79](https://github.com/stephenlclarke/container/issues/79) is commented and closed after this clean checkpoint. Apple `origin/main` lacks this fork-only Syslog provider path, so no Apple upstream handoff or PR is applicable. |

| Evidence | Candidate one passed in `2.003332125s` (2.070409244× Docker) and candidate two in `1.660303875s` (1.715895456× Docker). Both durations are retained for post-functional optimisation; neither proves comparable-or-better programme performance. |
| Evidence | The fresh Docker reference records `connectionCount=2`, `postExitConnectionCount=1`, and `postExitStreamsHex=[""]`; both candidate result files record the same values. |
| Evidence | The local issue [#79](https://github.com/stephenlclarke/container/issues/79) is the Stephen-owned fork tracker, not an Apple upstream report. |

## NET-IPAM-MODEL-01

| Field | Record |
| --- | --- |
| State | `Verified` — a non-mutating `config`/`convert` projection contract for advanced top-level network IPAM. It is not runtime network or IPAM certification. |
| Behaviour | A Compose file with explicit `ipam.driver`, ordered IPv4 and IPv6 `ipam.config` pools, `subnet`, `ip_range`, `gateway`, named `aux_addresses`, IPAM options, and tri-state `enable_ipv4`/`enable_ipv6` retains those values and order through Docker Compose `config` and Container Compose `config`/`convert` outputs without contacting an Engine. |
| Explicit non-goals | Provider registry, network create/attach/data-plane behavior, custom network/IPAM drivers, endpoint addresses/options, namespace joins, migration, external clients, and performance remain `Queued` behind `NET-WP-02` through `NET-WP-09`. |
| Pinned Docker oracle | Same-MBP Docker Compose `5.4.0`; the static oracle uses an isolated marker-protected Compose YAML and no Docker daemon mutation. |
| Affected repositories and inputs | `container-compose` `main` began at signed `c27e9f2e53e846802a65fd2da4437a76bb56c711`. The exact local source-model build overlays Container `969c5e42d5404d6d158da70a6d5d3fed86262342` at `/Users/sclarke/Documents/container/container-logging` and Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7` at `/Users/sclarke/Documents/container/containerization-engine-sandbox`; no lower-stack behavior or pin moves are in scope. |
| Focused proof | Docker Compose 5.4.0 `config` JSON is captured from the isolated Compose YAML. Focused Go and Swift source tests preserve every requested IPAM property; the retained `Tools/parity/check-compose-network-ipam-model.sh --strict` Bash CLI fixture compares Docker `config` with candidate `config` and `convert`, without running `up`. The changed Go helper and both executable Swift production additions each have 100% focused line coverage. |
| Completion criteria | Met: Docker `config` and exact candidate `config`/`convert` preserve the declared advanced top-level IPAM fields and pool order; focused tests and the CLI fixture pass; the source/docs checkpoint is signed and clean; runtime support remains truthfully gated. |
| Blocker criteria | If Docker 5.4.0 cannot supply a stable static oracle, the candidate cannot consume its source model, or two evidence-based corrections leave a source/output mismatch unexplained, retain the marker-protected evidence and mark `Blocked` or `Handed off` rather than retrying. |
| Safe handoff | Preserve `/private/tmp/container-net-ipam-model-reference.VR8XD8` (`.container-net-ipam-model-reference-root`), its reference/candidate JSON and fingerprint records, [the focused handoff](handoffs/NET-IPAM-MODEL-01.md), Stephen-owned [issue #199](https://github.com/stephenlclarke/container-compose/issues/199), and Slack START thread `1786011892.286669`. Do not activate runtime network behavior from this static contract. |

| Evidence | Docker Compose 5.4.0 has no `convert` command, so Docker `config --format json` is the canonical static oracle. Candidate `config` and `convert` produce the same SHA-256 `4fa5d912777abe146c5d9018d2ed2dfa6797ec111eb23ace2c961b77d3991e74` and retain the full source model. |
| Evidence | `go test -count=1 -run '^TestNormalizeRetainsLosslessIPAMSourceModel$' -coverprofile=...` passes and records `normalizeIPAM` at 100.0% focused line coverage. Instrumented Swift coverage records both executable additions in `NormalizedProject.swift` as covered (100.0%) while `ComposeNormalizerTests/normalizerPreservesSourceOrderedAdvancedIPAMModel` passes. |
| Evidence | The final marker-protected Bash CLI certificate passes in 1.957022334 seconds, including Docker `config` plus candidate `config` and `convert`. This debug static-model timing is not a paired release-performance result and does not close the programme performance gap. |
| Evidence | Docker Compose `5.4.0` has no public `docker compose convert` command. Its `config` JSON is therefore the pinned canonical oracle; candidate-only `convert` must preserve the same model. The Engine API overlay was excluded from focused source proof after it reintroduced the unavailable historical Container revision `2a79b4553a342e33411666a88ad20ccd2ce46551`; the declared Container and Containerization overlays pass and `Package.resolved` is restored unchanged. |

## NET-IPV6-ONLY-RUNTIME-02

| Field | Record |
| --- | --- |
| State | `Verified` — the narrow IPv6-only Compose runtime contract now has an exact source-matched candidate certificate against the pinned Docker oracle. It builds on `NET-IPAM-MODEL-01` and does not claim custom providers, arbitrary pool semantics, or general advanced-network closure. |
| Behaviour | A Compose project declaring `enable_ipv4: false`, `enable_ipv6: true`, and an IPv6 IPAM pool creates, starts, inspects, connects, and removes an IPv6-only service network with no hidden IPv4 endpoint, Docker-matched errors/cleanup, and visible IPv6 connectivity. |
| Explicit non-goals | Custom network/IPAM drivers, IPv4 allocation ranges, named auxiliary reservations, repeated pools, endpoint options, namespace joins, durable multi-provider reconciliation, devcontainer adoption, and comparable-or-better release performance remain `Queued`. |
| Pinned Docker oracle | Same-MBP Docker Compose `5.4.0`, Docker Engine `29.2.1`, API `1.53`, and a marker-protected disposable project/root. Docker image selection, inspect fields, connectivity, lifecycle, and cleanup are captured before candidate implementation. |
| Affected repositories and inputs | Compose `19d9256c30475c67db8da33c080377c658290142` (debug binary SHA-256 `0fab111282c0871ac54b3511e80b6de0f350b3a2bbad1671e07e2f1946c2b9d6`); Container `a661e67c8e7713483eb448493c7b4a35f346d9b3`; Containerization `cfb00bbf3523079fe2ab9fb6b8e9b3504eff77e5`; Engine API `4949e743675f00ec102f7acacdb4e990409e383f`; custom Builder shim `418e3c50f1ae664da3637ad81df8fc70ce630fd9`, OCI archive SHA-256 `d8c65304a8248b8e736752ca52a3e75ee8fe1c6339dd9de3601935d7329926ab`. No pin move or external publication is authorised. |
| Focused proof | `shellcheck scripts/run-with-container-runtime.sh` and nine focused wrapper tests pass. Exact source-matched Container `BuildPipelineCompletionTests` pass 2/2 with 100% line coverage of both branches of the changed terminal-completion helper. Builder-shim `go test ./pkg/stream ./pkg/server`, `go vet`, and `git diff --check` pass; changed `Send` and `buildCompletePacket` helpers have 100% coverage. The real `PerformBuild` boundary is not unit-instrumented (0%); the fresh exact CLI certificate is its behavioral proof. |
| Completion criteria | Met: Docker and the exact candidate agree on requested/effective/observed IPv6-only state, endpoint addressing, service-name connectivity, lifecycle cleanup, and preflight/complete fingerprint. A clean signed documentation checkpoint records the result. Diagnostic runtime timing is retained but does not close the programme performance row without a release-grade paired matrix. |
| Blocker criteria | Stop after two evidence-based corrections or two hours without new implementation/blocker evidence; retain the source/dependency/binary/guest/root fingerprint and mark `Blocked` or `Handed off` rather than retrying. |
| Safe handoff | Retain the exact evidence roots, source/dependency revisions, focused results, closed [Container issue #82](https://github.com/stephenlclarke/container/issues/82), and [the structured handoff](handoffs/NET-IPV6-ONLY-RUNTIME-02.md). The START thread is `1786022430.804239`. The next safe contract is a distinct remaining advanced-network/IPAM capability; do not infer custom drivers, pool ranges, auxiliary addresses, reconciliation, or release performance from this proof. |

| Evidence | The fresh marker-protected certificate at `/private/tmp/container-compose-network-ipv6-only-a661e67.deQlGB`, with runtime root `/private/tmp/container-compose-ipv6-runtime-a661e67.2qZ7QI`, records Docker Compose `5.4.0` / Engine `29.2.1` and the candidate creating an IPv6-only network, reporting no IPv4 endpoint, assigning `fd00:2026:806:0:f467:9eff:fe88:ceb/64`, passing service-name `ping -6`, and removing the project network on `down`. |
| Evidence | `FINGERPRINT-PREFLIGHT.json` and `FINGERPRINT-COMPLETE.json` are byte-identical in source/dependency/binary/guest/builder/root identity (`preflight_sha256` `3b8b0b3abf0a66c2dd50eea894e01aeeb0ae58cb3467d46bf22266a2b3973b7c`); every tracked/untracked source-tree digest in the record is empty. The guest is source-built `vminit:container-compose` archive SHA-256 `0c16d06e8e7142205db8cc1dd820c863f0b7c5303295a4b4d6998cc163680873`. |
| Evidence | Candidate diagnostic timings were `up` 3.330545 seconds, IPv6 connectivity 0.990984 seconds, and `down` 6.327614 seconds; Docker reference timings were 0.366235, 0.111466, and 10.232049 seconds. This debug/runtime comparison is neither a release artefact nor a comparable-or-better performance certificate. |
| Evidence | The source failure was a duplex completion cycle after successful BuildKit execution. Container `a661e67` handles terminal `CommandComplete` and finishes its request stream; Builder shim `418e3c5` sends a terminal completion packet and waits for request EOF without incorrectly closing the client-owned status channel. The Stephen-owned issue is closed; both generic Apple-shaped handoffs are local `unsubmitted` registry entries only. |

## NET-IPAM-ALLOCATION-RANGE-01

| Field | Record |
| --- | --- |
| State | `Verified` — one IPv4 VMNet pool’s dynamic allocation-range, gateway, auxiliary-reservation, service-restart retention, and project-network cleanup behavior has a fresh exact release certificate. This does not certify the full advanced network/IPAM design. |
| Behaviour | Given subnet `192.168.200.0/24`, `ip_range` `192.168.200.128/25`, gateway `192.168.200.1`, and auxiliary reservations `.130` and `.10`, dynamic services allocate only from the range, skip the in-range reservation, retain an address through `compose restart`, and leave no project network after `compose down`. |
| Explicit non-goals | IPv6 ranges/auxiliary addresses, static endpoints, multiple pools, custom network/IPAM drivers, global address spaces, concurrent/exhaustion/reuse ordering, provider/controller crash reconciliation, namespace joins, devcontainer adoption, and release-grade comparable-or-better performance remain `Queued`. |
| Pinned Docker oracle | Same-MBP Docker Compose `5.4.0`, Docker Engine `29.2.1`, API `1.53`; Docker's marker-protected reference fixture records range allocation, gateway/reservation exclusion, restart retention, project-network removal, and one timing sample per operation. |
| Affected repositories and inputs | Compose `30d3017112a9b7694aaec2ec80a0cfa411e6004d`, release binary SHA-256 `abce539706c9f91dfaff9b533fbd056b2c7a7c84ef81ad3ad00c9d397db476b1`; Container behavior commit `d7e5f5ddd831e0fe0cf025d606f1d2622eb6a4d2` and focused-test commit `ec286fca9bca2c4d9d732f6f898564d9f23e1eb6`; Containerization `cfb00bbf3523079fe2ab9fb6b8e9b3504eff77e5`; Engine API `4949e743675f00ec102f7acacdb4e990409e383f`; source-built guest archive SHA-256 `0c16d06e8e7142205db8cc1dd820c863f0b7c5303295a4b4d6998cc163680873`. |
| Focused proof | The repository-owned `Tools/parity/check-compose-network-ipam-allocation-range.sh --strict` Bash CLI certificate passes against Docker and the exact release candidate. Instrumented Container `AttachmentAllocatorTest\|DefaultNetworkServiceTest` passes 31 tests in two suites; `AttachmentAllocator.swift` has 92.96% focused line coverage. The real CLI certificate covers the live XPC request/release route that cannot be fabricated by constructing an isolated reply-less XPC message. |
| Completion criteria | Met: preflight and completion fingerprints bind clean exact sources, dependencies, binaries, guest/init input, and marker-protected roots; Docker and candidate pass the bounded behavior; source regressions are signed; the local GitHub issue is reconciled; and this documentation checkpoint records the remaining gaps. |
| Blocker criteria | Two evidence-based corrections without behavior or blocker delta, an inconsistent source/dependency/binary/guest/root fingerprint, or a candidate timeout would retain the evidence and leave this `Blocked` or `Handed off`. None occurred. |
| Safe handoff | Preserve [the focused handoff](handoffs/NET-IPAM-ALLOCATION-RANGE-01.md), `/private/tmp/container-ipam-allocation-range-release.ZgE2Ll/evidence` (`.container-compose-network-ipam-allocation-range-root`), local Apple-shaped handoffs, closed [Container issue #83](https://github.com/stephenlclarke/container/issues/83), and Slack START thread `1786040469.766159`. Do not publish the Apple delta or infer broader IPAM closure. |

| Evidence | Candidate observations were `alpha` `192.168.200.129/24` and `beta` `192.168.200.128/24`; both lie within `192.168.200.128/25`, the auxiliary `.130` is skipped, `alpha` retains `.129` through restart, and the project network is absent after `down`. Docker made the same bounded semantic observations, though service-to-address allocation order is not claimed as a cross-runtime invariant. |
| Evidence | The evidence root's preflight and completion records have SHA-256 `6c5a2b9122ed0059dc795f313f4bb0e38e5e0e37a3f6c82be53ac68f3ddc0cc1` and `7fad85c52397946949456e867ae56e67189aaac1a70bd5f53c685834f0c614c6`; all recorded source-tree tracked/untracked hashes are empty. Candidate `up` is 3.355610 seconds versus Docker 0.363003 (9.24x); candidate restart/down are faster in the single sample. This is post-functional performance evidence, not comparable-or-better performance proof. |

## NET-IPAM-STATIC-ENDPOINT-01

| Field | Record |
| --- | --- |
| State | `Verified` — the retained Docker reference and one exact coherent signed candidate pass the bounded static IPv4 endpoint lifecycle. |
| Behaviour | A static IPv4 endpoint inside the declared subnet but outside `ip_range` must create, inspect, survive restart, and clean up with its project network. |
| Pinned Docker oracle | Same-MBP Docker Compose `5.4.0` / Engine `29.2.1`; `192.168.240.20` on subnet `192.168.240.0/24`, range `192.168.240.128/25`, and gateway `.1` passes all bounded lifecycle observations. |
| Affected repositories and inputs | Compose `cd5f0e453372bcecb1821fab564338d27fc711c1`; Container `c7924e375d98d82af37902f4a0c310ee389eab97`; Containerization `7f62f5b940630811573a34f70cdd6f3fa11d014d`; Engine API `5e6e24d017691596783515285e1ff56d29701235`; SwiftNIO SSL `a9d648535c62e640d1df258a70c9117a8ddea43e`; candidate archive SHA-256 `1d5c7c73272c6addda98639283310e92b2a703a415621bf1dd7dde94b00807c3`. |
| Focused proof | `ComposeRuntimeSmokeTests.runtimeStaticIPv4EndpointLifecycle` passes in 45.492 seconds against the exact candidate. It verifies subnet, range, gateway, the `192.168.240.20` endpoint before/after restart, and project-network cleanup. Evidence: `/Volumes/SSD/github/evidence/container-family-stable-01/net-ipam-static-endpoint-cd5f0e45/runtime-test-with-preflight-path.log`. |
| Completion criteria | Met. The normal graph builds; the exact runtime proof passes without reducing IPv6-only support. The completed duration is retained as functional evidence, not comparable-performance proof. |
| Safe handoff | Preserve [the focused handoff](handoffs/NET-IPAM-STATIC-ENDPOINT-01.md) and exact evidence under `/Volumes/SSD/github/evidence/container-family-stable-01`; reconcile [issue #208](https://github.com/stephenlclarke/container-compose/issues/208) after publication. |

## LOGGING-SYSLOG-UNIX-REST-01

| Field | Record |
| --- | --- |
| State | `Blocked` — Docker's create-time Unix-path behavior is retained and the source correction is committed, but the unchanged normal lower graph cannot run its focused executable proof. |
| Behaviour | Docker refuses missing `unix://` and `unixgram://` Syslog addresses during `create`, leaving no container residue. |
| Exact inputs | Docker CLI `29.7.1` / Engine `29.2.1`; Container base `e188f9e80583d705e57bb91353017df6af3ad1d2`, signed correction `52da6f47afe89c58df9e42f960aa52775ae865a4`; Containerization `38d9c695e7a6915e5ce45d12c893dc323a661af7`; Engine API `afb8a8f68ed56829b669c95cbddb488a68dc9175`. |
| Focused proof | The same-MBP Docker oracle rejects both forms at create with `stat ...: no such file or directory`; `swift build --target ContainerLoggingProvidersTests` compiles the changed code and regression. |
| Blocker | Full focused execution fails before Syslog compilation can link because the locked normal graph lacks `WorkloadNetworkEndpoint`. The historical compatible SHA can be fetched directly but is not advertised by a remote ref, so SwiftPM correctly rejects it and a public-socket candidate would lack one exact reproducible source/dependency/binary/guest/root fingerprint. The root cause is functional graph composition, not performance. |
| Safe handoff | Use [the detailed handoff](handoffs/LOGGING-SYSLOG-UNIX-REST-01.md), open [Container issue #94](https://github.com/stephenlclarke/container/issues/94), and Slack START thread `1786223959.488079`. Publish one coherent lower stack, rebuild, then rerun the unchanged focused test and REST fixture without masking the dependency mismatch. |

## CONTAINER-BUILD-PROGRESS-COMPLETION-01

| Field | Record |
| --- | --- |
| State | `Verified` — a narrow lower-runtime completion-boundary certificate that unblocks `NET-IPV6-ONLY-RUNTIME-02`. It is not a Compose runtime certificate and does not close an advanced-network gap by itself. |
| Behaviour | Native `container build` must return a correct process result after its complete build lifecycle: BuildKit OCI export followed by Container's host-side image load, unpack, and tagging. `#16 DONE` is not itself a terminal native CLI event. |
| Pinned Docker oracle | Same-MBP Docker CLI/Buildx against Docker Engine `29.2.1` / API `1.53`: a marker-protected minimal Dockerfile `buildx build --output type=oci,dest=...` returns after successful OCI export. The oracle freezes the caller-visible completion boundary, not implementation-specific progress text. |
| Affected repositories and inputs | Container branch `upstream/container-build-progress-completion-01` begins at signed `e1855ae21dcf829e0c514435398037b0f91cca8e`; matched Containerization is signed `2f9b44dbb7ce87270ee46f85a4327d7c1e1e57ab`; Compose `main` is signed `0fc88e86502fb5e669cc119bc5a787c12d795855`. The retained failure used builder `ghcr.io/stephenlclarke/container-builder-shim/builder@sha256:6cfb001d6fcf46283526df084351c20fd77e473eabaa9bf55e9327cc1d882f0c`. No pin move, push, hosted CI, Apple issue, or upstream PR is authorised. |
| Focused proof | The Docker Buildx oracle is retained in an isolated marker-protected root. The bounded native source-init candidate at `/private/tmp/container-build-completion-exact-evidence.s0S09P` observes valid OCI export, post-export ingestion, a fresh guest init-image build, runtime restart, and command exit under one exact source/binary/builder/root fingerprint. No source defect appeared, so no source change or coverage obligation was manufactured. |
| Completion criteria | Met: the Docker oracle and bounded native candidate establish the actual completion boundary; [issue #80](https://github.com/stephenlclarke/container/issues/80) is closed as invalid evidence; and this clean checkpoint records the result. Resume one fresh IPv6-only certificate separately. This does not certify programme-wide performance. |
| Blocker criteria | After two evidence-based source corrections or two hours without implementation/new blocker evidence, retain root logs, samples, source/dependency/binary/builder fingerprints, and mark `Blocked` or `Handed off`; do not keep restarting or rebuilding. |
| Safe handoff | Preserve [the verified handoff](handoffs/CONTAINER-BUILD-PROGRESS-COMPLETION-01.md), the queued [network handoff](handoffs/NET-IPV6-ONLY-RUNTIME-02.md), closed issue #80, and roots `/private/tmp/container-compose-network-ipv6-only-candidate.EeSlUg`, `/private/tmp/container-compose-network-ipv6-only-candidate.YqHoeU`, and `/private/tmp/container-build-completion-exact-runtime.fT0PRk`. Resume exactly one fresh IPv6-only candidate certificate; do not reuse a previous root. |

## LOGGING-PERF-C04-MOUNT-ROOT-01

| Field | Record |
| --- | --- |
| State | `Verified` — narrow Docker-reference parity-runner contract. It validates the C04 reference fixture only; it does not certify the container-compose candidate alias/recreate behavior or comparable-or-better performance. |
| Behaviour | The pinned Dev Containers CLI must bind a copied C04 Compose fixture from a Docker-visible source, execute `probe.sh` in the absolute remote workspace returned by `up`, record lifecycle observations, and remove all copied workspace and Docker residue. |
| Explicit non-goals | Candidate C04 alias/recreate semantics, a full devcontainer lane, release artefact performance, and all Docker logging-driver behavior remain `Queued` or separately incomplete. |
| Pinned Docker oracle | Same MBP: `@devcontainers/cli@0.88.0` at reference `f683c29f64a20109b4453e5149807e390ff65133`, Docker Engine `29.2.1`/API `1.53`, and Docker Compose `5.3.1` wrapper SHA-256 `90b2705314905295de430e2e021f490666c959accba18e0a784b32aecc04a034`. |
| Affected repositories and inputs | devcontainer source is signed `3d0b6f9fd2d34b7d46278873996056f9f30517a9`; its evidence and issue handoff are signed `3bd1e6230f7bbd19cc8491d233aa305cb7cecc31`. This Compose ledger checkpoint records the changed oracle boundary only; no Container Compose source or dependency pin moved. |
| Focused proof | `python3 Tools/parity/test_run_lane.py` passes 24/24. The new fixture-copy, error, refusal, cleanup, and engine-fixture bypass paths have focused coverage; only five legacy command-failure lines are uncovered. The marker-protected C04 Docker invocation passed in 4.337 seconds with primary/label selection, recreate, termination, restart, shutdown, and cleanup observations. |
| Completion criteria | Met: the exact Docker reference passes; source, dependency, wrapper, result, and disposable-root evidence are retained; `.build/parity-workspaces` and matching Docker local-folder labels are absent after cleanup; devcontainer issue #46 is documented, commented, and closed; and the local checkpoints are signed and clean. |
| Blocker criteria | A real lifecycle difference after the fixture is visible, an inconsistent fingerprint, or a cleanup refusal would have retained the marker-protected root and left this contract `Blocked` or `Handed off` rather than retried. None occurred after the Docker-visible-root correction. |
| Safe handoff | Preserve `/private/tmp/devcontainer-c04-mount-3d0b6f9.K4fuzR` and [devcontainer issue #46](https://github.com/stephenlclarke/devcontainer/issues/46). Run one fresh container-compose C04 candidate lane from this reference before changing [container-compose #184](https://github.com/stephenlclarke/container-compose/issues/184); do not treat this harness result as candidate parity. |

## C04-CANDIDATE-LIFECYCLE-01

| Field | Record |
| --- | --- |
| State | `Verified` — the exact candidate passes every declared C04 lifecycle observation with no cleanup differences. Functional parity is closed; retained performance is not comparable-or-better. |
| Behaviour | The Container Compose provider must pass the C04 Dev Containers lifecycle observations: primary service and label selection, forced recreation, `term` termination, restart, shutdown, and cleanup. |
| Explicit non-goals | This does not certify logging, a release artefact, or comparable-or-better performance. It does not change the Docker-only C04 runner proof. |
| Pinned Docker oracle | Same-MBP `@devcontainers/cli@0.88.0` reference `f683c29f64a20109b4453e5149807e390ff65133`, Docker Engine `29.2.1`/API `1.53`, and Docker Compose `5.3.1`; the corrected Docker C04 fixture passed in 4.337 seconds. |
| Affected repositories and inputs | Dev Containers `df495818fe4d58c72878022355131022ac3ad43f`; Compose `ff5d74d8c0eaef7c043d5046c4bf397899397153`; exact Container candidate `c7924e375d98d82af37902f4a0c310ee389eab97`; Containerization `7f62f5b940630811573a34f70cdd6f3fa11d014d`; Engine API `5e6e24d017691596783515285e1ff56d29701235`; retained guest archive `vminit-7f62f5b9-c7924e37-skopeo.oci.tar`. |
| Focused proof | The fresh candidate completed in 18.595 seconds with `primary=app`, `primary_label=true`, `recreated=true`, `restart_signal=term`, `restarted=true`, and `shutdown=true`; no cleanup differences remained. Dev Containers' 76 parity-runner tests and seven focused Apple runtime tests pass. |
| Completion criteria | Met: one committed coherent graph builds the engine and Compose wrapper; one fresh marker-protected candidate has an exact source/dependency/binary/guest/root fingerprint and matches every Docker observation. Issue #184 can close from this evidence. |
| Blocker criteria | A future semantic difference, incomplete fingerprint, or cleanup residue must retain its fresh root and reopen the contract. The 4.29x duration ratio is performance work, not a functional blocker. |
| Safe handoff | See [the verified handoff](handoffs/C04-CANDIDATE-LIFECYCLE-01.md) and preserve `/Volumes/SSD/github/evidence/container-family-stable-01/devcontainer-c04-d57c2b6-ff5d74d8-v5/`; do not overwrite the certificate. |
