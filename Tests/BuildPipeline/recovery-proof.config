process {
    executor = 'local'
    cache = 'deep'
    maxForks = 1
    errorStrategy = 'terminate'
    beforeScript = 'exec </dev/null'
    shell = ['/bin/bash', '-euo', 'pipefail']
}

executor {
    queueSize = 1
}
